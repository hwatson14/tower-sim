eDamage_mechanics:
  version: ep_v5.00.01_extract_2026-02-02
  workbook: Copy of Effective Paths v5.00.01.xlsx
  mechanics:
  - id: ep_lambda_bullet_per_second
    name: BULLET_PER_SECOND
    type: excel_named_formula
    excel_definition: LAMBDA(attack_speed, ((0.01413 * attack_speed^2) + (4.01278 * attack_speed) + 92.8885)*1.3)
    notes: From workbook defined name.
  - id: ep_lambda_epd_aspd
    name: EPD_ASPD
    type: excel_named_formula
    excel_definition: "LAMBDA(ws_level, wsp_level, lab_level, has_card, card_level, substat, relic, vault, has_mastery, mastery_level,\
      \ LET(\n  WS, 1 / (1 + ws_level * 0.02),\n  WSP, 1 / (1 + wsp_level * 0.01),\n  Lab, 1 / (1 + lab_level * 0.02),\n  Card,\
      \ IF(has_card, 1 / (1 + card_level * 0.02), 1),\n  Substat, 1 / (1 + substat),\n  Mastery, IF(has_mastery, 1 / (1 + (0.5%\
      \ * (1+mastery_level))), 1),\n  Relics, 1 / (1 + relic),\n  Vault, 1 / (1 + vault),\n\nWS * WSP * Lab * Card * Substat *\
      \ Mastery * Relics * Vault))"
    notes: Updated per user-provided Effective Paths eDamage extract (DJ5 -> EPD_ASPD).
  - id: ep_lambda_epd_bounceshot
    name: EPD_BOUNCESHOT
    type: excel_named_formula
    excel_definition: LAMBDA(bsc, bst, being_annihilator, bsc * bst + being_annihilator)
    notes: Updated per user-provided Effective Paths eDamage extract (DG5/DH5 -> EPD_BOUNCESHOT).
  - id: ep_lambda_epd_bsc
    name: EPD_BSC
    type: excel_named_formula
    excel_definition: "LAMBDA(ws_level, substat, vault, LET(\n  WS, ws_level * 0.8%,\n  VaultBonus, 1 + vault,\n\n(1 + WS +\
      \ substat) * VaultBonus))"
    notes: Updated per user-provided Effective Paths eDamage extract (DG5/DH5 -> EPD_BSC).
  - id: ep_lambda_epd_bst
    name: EPD_BST
    type: excel_named_formula
    excel_definition: "LAMBDA(ws_level, lab_level, substat, LET(\n  WS, ws_level * 1%,\n  Lab, lab_level * 1%,\n\n1 + WS +\
      \ Lab + substat))"
    notes: Updated per user-provided Effective Paths eDamage extract (DG5/DH5 -> EPD_BST).
  - id: ep_lambda_epd_critical
    name: EPD_CRITICAL
    type: excel_named_formula
    excel_definition: "LAMBDA(cc, cf, scc, scm, being_annihilator, LET(\n  CCm, MIN(1, CC),\n  normalCrit, 1 + CCm * (cf -\
      \ 1),\n  superCritChance, MIN(CCm, scc),\n  superCrit, 1 + superCritChance * (scm - cf),\n\n  normalCrit * superCrit + being_annihilator\
      \ ))"
    notes: Updated per user-provided Effective Paths eDamage extract (DD5 -> EPD_CRITICAL).
  - id: ep_lambda_epd_crit_chance
    name: EPD_CRIT_CHANCE
    type: excel_named_formula
    excel_definition: "LAMBDA(ws_level, has_card, card_level, relic, vault, substat, has_mastery, mastery_level, LET(\n  WS,\
      \ ws_level * 0.3%,\n  Card, IF(has_card, card_level * 0.15%, 0),\n  Relics, relic,\n  Vault, vault,\n  Mastery, IF(has_mastery,\
      \ 0.1%*(1+mastery_level), 0),\n  WS + Card + substat + Relics + Vault + Mastery))"
    notes: Updated per user-provided Effective Paths eDamage extract (CZ5 -> EPD_CRIT_CHANCE).
  - id: ep_lambda_epd_dpm
    name: EPD_DPM
    type: excel_named_formula
    excel_definition: "LAMBDA(ws_level, wsp_level, range_dpm, substat, relic, vault, has_rend, rend_mult, LET(\n  WS, 1 +\
      \ (0.03 * ws_level),\n  WSP, 1 + (0.01 * wsp_level),\n  Relics, 1 + relic,\n  Vault, 1 + vault,\n  Rend, IF(has_rend,\
      \ rend_mult, 1),\n  range_dpm * WS * WSP * (1 + substat) * Relics * Vault * Rend))"
    notes: Updated per user-provided Effective Paths eDamage extract (DL5 -> EPD_DPM).
  - id: ep_lambda_epd_maxrend
    name: EPD_MAXREND
    type: excel_named_formula
    excel_definition: "LAMBDA(has_rend, lab_level, substat, wsp_level, LET(\n  Lab, lab_level * 25%,\n  Wsp, 1+(wsp_level\
      \ * 1%),\nIF(has_rend, (8 + Substat + Lab) * Wsp, 1)))"
    notes: From workbook defined name.
  - id: ep_lambda_epd_msc
    name: EPD_MSC
    type: excel_named_formula
    excel_definition: "LAMBDA(ws_level, substat, vault, LET(\n  WS, ws_level * 0.5%,\n  VaultBonus, 1 + vault,\n(1 + WS +\
      \ substat) * VaultBonus))"
    notes: Updated per user-provided Effective Paths eDamage extract (DF5 -> EPD_MSC).
  - id: ep_lambda_epd_mst
    name: EPD_MST
    type: excel_named_formula
    excel_definition: "LAMBDA(ws_level, substat, vault, LET(\n  WS, ws_level * 1%,\n  VaultBonus, 1 + vault,\n(1 + WS + substat)\
      \ * VaultBonus))"
    notes: Updated per user-provided Effective Paths eDamage extract (DF5 -> EPD_MST).
  - id: ep_lambda_epd_multishot
    name: EPD_MULTISHOT
    type: excel_named_formula
    excel_definition: LAMBDA(msc, mst, msc * mst)
    notes: Updated per user-provided Effective Paths eDamage extract (DF5 -> EPD_MULTISHOT).
  - id: ep_lambda_epd_range
    name: EPD_RANGE
    type: excel_named_formula
    excel_definition: "LAMBDA(ws_level, has_card, card_level, lab_level, substat, LET(\n  WS, 30 + 0.5 * ws_level,\n  Card,\
      \ IF(has_card, card_level, 0),\n  Lab, lab_level,\n  WS + Card + Lab + substat))"
    notes: Updated per user-provided Effective Paths eDamage extract (DK5 -> EPD_RANGE).
  - id: ep_lambda_epd_rangedpm
    name: EPD_RANGEDPM
    type: excel_named_formula
    excel_definition: LAMBDA(range, dpm, range_multiplier, dpm / range * range_multiplier)
    notes: Updated per user-provided Effective Paths eDamage extract (DM5 -> EPD_RANGEDPM).
  - id: ep_lambda_epd_rapidfire
    name: EPD_RAPIDFIRE
    type: excel_named_formula
    excel_definition: LAMBDA(rfc, rfd, bps, rfc * rfd * bps)
    notes: Updated per user-provided Effective Paths eDamage extract (DI5 -> EPD_RAPIDFIRE).
  - id: ep_lambda_epd_rfc
    name: EPD_RFC
    type: excel_named_formula
    excel_definition: "LAMBDA(ws_level, substat, vault, LET(\n  WS, ws_level * 0.4%,\n  VaultBonus, 1 + vault,\n(1 + WS + substat)\
      \ * VaultBonus))"
    notes: Updated per user-provided Effective Paths eDamage extract (DI5 -> EPD_RFC).
  - id: ep_lambda_epd_rfd
    name: EPD_RFD
    type: excel_named_formula
    excel_definition: "LAMBDA(ws_level, substat, LET(\n  WS, 0.6 + ws_level * 0.05,\nWS + substat))"
    notes: Updated per user-provided Effective Paths eDamage extract (DI5 -> EPD_RFD).
  - id: ep_lambda_epd_uwcritical
    name: EPD_UWCRITICAL
    type: excel_named_formula
    excel_definition: "LAMBDA(cc, cf, scc, scm, LET(\n  CCm, MIN(1, cc),\n  normalCrit, 1 + CCm * (cf - 1),\n  superCritChance,\
      \ MIN(CCm, scc),\n  superCrit, 1 + superCritChance * (scm - cf),\n\n  normalCrit * superCrit))"
    notes: Updated per user-provided Effective Paths eDamage extract (DE5 -> EPD_UWCRITICAL).
  - id: ep_lambda_epd_supertower_bonus
    name: EPD_SUPERTOWER_BONUS
    type: excel_named_formula
    excel_definition: "LAMBDA(stlvl, st_bonus, IF(stlvl=\" \", 1, 1+st_bonus))"
    notes: Updated per user-provided Effective Paths eDamage extract (DN5 -> EPD_SUPERTOWER_BONUS).
  - id: ep_lambda_epd_supertower_cooldown
    name: EPD_SUPERTOWER_COOLDOWN
    type: excel_named_formula
    excel_definition: LAMBDA(has_mastery, st_mastery, IF(has_mastery, st_mastery, 1))
    notes: Updated per user-provided Effective Paths eDamage extract (DN5 -> EPD_SUPERTOWER_COOLDOWN).
  - id: ep_lambda_epd_supertower_effective_bonus
    name: EPD_SUPERTOWER_EFFECTIVE_BONUS
    type: excel_named_formula
    excel_definition: "LAMBDA(has_st, has_mastery, st_bonus, st_cd, sl_dur, sl_qty, sl_angle, LET(\n  ST, IF(has_st, st_bonus, 1),\n  uptime, IF(has_st, sl_dur / st_cd, 0),\n  effective, 1 + uptime * (ST - 1),\n  effective))"
    notes: Updated per user-provided Effective Paths eDamage extract (DN5 -> EPD_SUPERTOWER_EFFECTIVE_BONUS).
  - id: ep_lambda_epg_assist_sub_cap
    name: EPG_ASSIST_SUB_CAP
    type: excel_named_formula
    excel_definition: LAMBDA(has_assist, stone_cap, lab_cap, IF(has_assist, (1 + stone_cap + lab_cap) / 100, 0))
    notes: From workbook defined name.
  - id: ep_lambda_ep_cl_dps
    name: EP_CL_DPS
    type: excel_named_formula
    excel_definition: 'LAMBDA(cl_damage, cl_quant, cl_chance, multi_rapid_bounce, bullet_per_second, cl_damage*cl_quant*cl_chance

      *multi_rapid_bounce*bullet_per_second)'
    notes: From workbook defined name.
  - id: ep_lambda_ep_dw_dps
    name: EP_DW_DPS
    type: excel_named_formula
    excel_definition: LAMBDA(hasdw, dwdamage, dwquantity, dwcooldown, dwdamageamp, IF(hasdw, DWDamage/DWCooldown* DwQuantity*
      (5+ dwDamageAmp*1.5), 0))
    notes: From workbook defined name.
  - id: ep_lambda_ep_slm_dps
    name: EP_SLM_DPS
    type: excel_named_formula
    excel_definition: LAMBDA(sm_damage, sl_damage, sm_cooldown, sm_heat, sm_aoe, sm_heat * sm_aoe * sm_damage * sl_damage
      / sm_cooldown)
    notes: From workbook defined name.
  - id: ep_lambda_ep_sl_final_bonus
    name: EP_SL_FINAL_BONUS
    type: excel_named_formula
    excel_definition: LAMBDA(sl_coverage, sl_bonus, 1 + (sl_bonus-1) * sl_coverage)
    notes: From workbook defined name.
  - id: ep_lambda_ep_sm_dps
    name: EP_SM_DPS
    type: excel_named_formula
    excel_definition: LAMBDA(sm_damage, sm_quant, sm_cooldown, sm_coverfire, sm_heat, sm_aoe, sm_heat * sm_aoe * (sm_damage
      * sm_quant / sm_cooldown + sm_coverfire))
    notes: From workbook defined name.
  - id: ep_lambda_ep_uw_sl_coverage
    name: EP_UW_SL_COVERAGE
    type: excel_named_formula
    excel_definition: LAMBDA(sl_quant, sl_angle, MIN(100%, sl_quant * sl_angle / 360))
    notes: From workbook defined name.
  - id: ep_lambda_ep_uw_total_damage
    name: EP_UW_TOTAL_DAMAGE
    type: excel_named_formula
    excel_definition: LAMBDA(dw, cl, sm, slm, sl, ps, ilm, uw_damage_boost, st_uw_mastery, uw_crit_card, uw_damage_boost *
      ((DW+SM+CL+PS+ILM) * SL * st_uw_mastery + SLM) * uw_crit_card * st_uw_mastery)
    notes: From workbook defined name.
  - id: ep_lambda_stat_uw_cl_final_ch
    name: STAT_UW_CL_FINAL_CH
    type: excel_named_formula
    excel_definition: LAMBDA(base, substat, (base+substat))
    notes: From workbook defined name.
  - id: ep_lambda_stat_uw_cl_final_dmg
    name: STAT_UW_CL_FINAL_DMG
    type: excel_named_formula
    excel_definition: LAMBDA(base, substat, hasperk, (base+substat)*IF(HasPerk,2,1))
    notes: From workbook defined name.
  - id: ep_lambda_stat_uw_cl_final_qty
    name: STAT_UW_CL_FINAL_QTY
    type: excel_named_formula
    excel_definition: LAMBDA(base, substat, hasdc, FLOOR(base+substat) + IF(HasDC,0.6,0))
    notes: From workbook defined name.
  - id: ep_lambda_stat_uw_dw_final_cd
    name: STAT_UW_DW_FINAL_CD
    type: excel_named_formula
    excel_definition: LAMBDA(base, substat, gcompreduction, (base+substat)*gcompreduction)
    notes: From workbook defined name.
  - id: ep_lambda_stat_uw_dw_final_dmg
    name: STAT_UW_DW_FINAL_DMG
    type: excel_named_formula
    excel_definition: LAMBDA(base, substat, base+substat)
    notes: From workbook defined name.
  - id: ep_lambda_stat_uw_dw_final_qty
    name: STAT_UW_DW_FINAL_QTY
    type: excel_named_formula
    excel_definition: LAMBDA(base, substat, hasperk, FLOOR(base+substat+IF(hasperk,1,0)))
    notes: From workbook defined name.
  - id: ep_lambda_stat_uw_sl_final_angle
    name: STAT_UW_SL_FINAL_ANGLE
    type: excel_named_formula
    excel_definition: LAMBDA(base, substat, (base+substat))
    notes: From workbook defined name.
  - id: ep_lambda_stat_uw_sl_final_dmg
    name: STAT_UW_SL_FINAL_DMG
    type: excel_named_formula
    excel_definition: 'LAMBDA(base, sl_lightrange, substat, relic, vault, has_perk, (base+substat)*(1+relic)*(1+vault)*

      IF(has_perk, 1.5, 1)*sl_lightrange)'
    notes: From workbook defined name.
  - id: ep_lambda_stat_uw_sl_final_lr
    name: STAT_UW_SL_FINAL_LR
    type: excel_named_formula
    excel_definition: LAMBDA(sl_lightrange, dpm, 1+IF(ISNUMBER(sl_lightrange), sl_lightrange * (dpm-1), 0))
    notes: From workbook defined name.
  - id: ep_lambda_stat_uw_sm_final_cd
    name: STAT_UW_SM_FINAL_CD
    type: excel_named_formula
    excel_definition: LAMBDA(base, substat, gcompreduction, (base+substat)*gcompreduction)
    notes: From workbook defined name.
  - id: ep_lambda_stat_uw_sm_final_cf
    name: STAT_UW_SM_FINAL_CF
    type: excel_named_formula
    excel_definition: LAMBDA(sm_damage, cf_time, IF(ISNUMBER(cf_time), sm_damage * (1 / cf_time), 0))
    notes: From workbook defined name.
  - id: ep_lambda_stat_uw_sm_final_dmg
    name: STAT_UW_SM_FINAL_DMG
    type: excel_named_formula
    excel_definition: LAMBDA(base, substat, (base+substat))
    notes: From workbook defined name.
  - id: ep_lambda_stat_uw_sm_final_qty
    name: STAT_UW_SM_FINAL_QTY
    type: excel_named_formula
    excel_definition: LAMBDA(base, substat, has_perk, FLOOR((base+substat) + IF(has_perk, 4, 0)))
    notes: From workbook defined name.
eHP_mechanics:
  version: ep_v5.00.01_extract_2026-02-02
  workbook: Copy of Effective Paths v5.00.01.xlsx
  mechanics:
  - id: ep_lambda_eph_max_rcvr
    name: EPH_MAX_RCVR
    type: excel_named_formula
    excel_definition: "LAMBDA(ws_val, lab_lvl, stone_sac, lab_sac, prim_sub, ass_sub, wse_lvl, vault_pct, LET(\n  WS, ws_val,\n  LAB, 1% * lab_lvl,\n  SAC, (1 + stone_sac + lab_sac) * 0.01,\n  SUBSTAT, prim_sub + ass_sub * SAC,\n  WSE, 1 + 0.01 * wse_lvl,\n  VAULT, 1 + vault_pct,\n\n(WS + LAB + SUBSTAT) * WSE * VAULT))"
    notes: From workbook defined name (user-provided wiki/sheet extract in prompt).
  - id: ep_lambda_eph_health
    name: EPH_HEALTH
    type: excel_named_formula
    excel_definition: "LAMBDA(ws_val, lab_lvl, has_card, card_val, has_mastery, mastery_lvl, wse_lvl, has_perk, spb_lvl, has_cto, has_rto, relic_pct, vault_pct, has_dwhp, dwhp_lvl, LET(\n  WS, ws_val,\n  LAB, 1 + 0.03 * lab_lvl,\n  CARD, IF(has_card, card_val * IF(has_mastery, 1 + 0.2 * (1 + mastery_lvl), 1), 1),\n  WSE, 1 + 0.01 * wse_lvl,\n  PERKHP, IF(has_perk, (1 + 0.2 * 5) * (1 + 1% * spb_lvl), 1),\n  CTO, IF(has_cto, 0.3, 1),\n  RTO, IF(has_rto, 0.4, 1),\n  RELIC, 1 + relic_pct,\n  VAULT, 1 + vault_pct,\n  DWHP, IF(has_dwhp, 500% + 25% * dwhp_lvl, 1),\n\nWS * LAB * CARD * WSE * PERKHP * CTO * RTO * RELIC * VAULT * DWHP))"
    notes: From workbook defined name (user-provided wiki/sheet extract in prompt). Perk/trade-off constants map to perk table entries in tables/perks_v1.csv (x1.20 Max Health, x1.80 coins but Tower Max Health -70%, Tower Health Regen x8.00 but Tower Max Max Health -60%).
  - id: ep_lambda_eph_regen
    name: EPH_REGEN
    type: excel_named_formula
    excel_definition: "LAMBDA(ws_val, lab_lvl, has_card, card_val, has_mastery, mastery_lvl, stone_sac, lab_sac, prim_sub, ass_sub, wse_lvl, has_perk, spb_lvl, has_rgnto, has_rto, ito_lvl, relic_pct, vault_pct, has_swm, swm_lvl, LET(\n  WS, ws_val,\n  LAB, 1 + 0.03 * lab_lvl,\n  CARD, IF(has_card, card_val * IF(has_mastery, 1 + 0.4 * (1 + mastery_lvl), 1), 1),\n  SAC, (1 + stone_sac + lab_sac) * 0.01,\n  SUBSTAT, 1 + prim_sub + ass_sub * SAC,\n  WSE, 1 + 0.01 * wse_lvl,\n  PERKREGEN, IF(has_perk, (1 + 0.75 * 5) * (1 + 1% * spb_lvl), 1),\n  RGNTO, IF(has_rgnto, 0.1, 1),\n  RTO, IF(has_rto, 8 * (1 + 1% * ito_lvl), 1),\n  RELIC, 1 + relic_pct,\n  VAULT, 1 + vault_pct,\n  SWM, IF(has_swm, 1 + 0.9 * (1 + swm_lvl), 1),\n\nWS * LAB * CARD * SUBSTAT * WSE * PERKREGEN * RGNTO * RTO * RELIC * VAULT * SWM))"
    notes: From workbook defined name (user-provided wiki/sheet extract in prompt). Perk/trade-off constants map to perk table entries in tables/perks_v1.csv (x1.75 Health Regen, Enemies Have -50% Health but Tower Health Regen and Lifesteal -90%, Tower Health Regen x8.00 but Tower Max Max Health -60%).
  - id: ep_lambda_eph_def_pct
    name: EPH_DEF_PCT
    type: excel_named_formula
    excel_definition: "LAMBDA(ws_val, lab_lvl, has_card, card_val, has_mastery, mastery_lvl, stone_sac, lab_sac, prim_sub, ass_sub, has_perk, spb_lvl, relic_pct, vault_pct, LET(\n  WS, ws_val,\n  LAB, 0.2% * lab_lvl,\n  CARD, IF(has_card, card_val + IF(has_mastery, 0.7% * (1 + mastery_lvl), 0), 0),\n  SAC, (1 + stone_sac + lab_sac) * 0.01,\n  SUBSTAT, prim_sub + ass_sub * SAC,\n  PERKDEF, IF(has_perk, (4% * 5) * (1 + 1% * spb_lvl), 0),\n  RELIC, relic_pct,\n  VAULT, vault_pct,\n\nMIN(98%, WS + LAB + CARD + SUBSTAT + PERKDEF + RELIC + VAULT)))"
    notes: From workbook defined name (user-provided wiki/sheet extract in prompt). Perk constants map to perk table entries in tables/perks_v1.csv (Defense Percent +4.00).
  - id: ep_lambda_eph_dabs
    name: EPH_DABS
    type: excel_named_formula
    excel_definition: "LAMBDA(ws_val, lab_lvl, has_card, card_val, stone_sac, lab_sac, prim_sub, ass_sub, wse_lvl, has_perk, spb_lvl, relic_pct, vault_pct, LET(\n  WS, ws_val,\n  LAB, 1 + 0.03 * lab_lvl,\n  CARD, IF(has_card, card_val, 1),\n  SAC, (1 + stone_sac + lab_sac) * 0.01,\n  SUBSTAT, 1 + prim_sub + ass_sub * SAC,\n  WSE, 1 + 0.01 * wse_lvl,\n  PERKDABS, IF(has_perk, (1 + 0.15 * 5) * (1 + 1% * spb_lvl), 1),\n  RELIC, 1 + relic_pct,\n  VAULT, 1 + vault_pct,\n\nWS * LAB * CARD * SUBSTAT * WSE * PERKDABS * RELIC * VAULT))"
    notes: From workbook defined name (user-provided wiki/sheet extract in prompt). Perk constants map to perk table entries in tables/perks_v1.csv (x1.15 Defense Absolute).
  - id: ep_lambda_eph_armor
    name: EPH_ARMOR
    type: excel_named_formula
    excel_definition: "LAMBDA(prim_bonus, has_ass, ass_bonus, stone_bac, lab_bac, prim_bonus * IF(has_ass, (((ass_bonus - 1) * (1 + stone_bac + lab_bac) * 0.01) + 1), 1))"
    notes: From workbook defined name (user-provided wiki/sheet extract in prompt).
  - id: ep_lambda_eph_wall_health
    name: EPH_WALL_HEALTH
    type: excel_named_formula
    excel_definition: "LAMBDA(ws_val, lab_lvl, stone_sac, lab_sac, prim_sub, ass_sub, wse_lvl, prim_effect, ass_effect, fort_lvl, LET(\n  LAB, 2% * lab_lvl,\n  SAC, (1 + stone_sac + lab_sac) * 0.01,\n  SUBSTAT, prim_sub + ass_sub * SAC,\n  WSE, 1 + 0.01 * wse_lvl,\n  SF, IF(prim_effect + ass_effect <> 0, prim_effect + ass_effect, 1),\n  WALLHP, (ws_val + LAB + SUBSTAT) * WSE * SF,\n  FORT, 1 + 0.2 * fort_lvl,\n\nWALLHP * FORT))"
    notes: From workbook defined name (user-provided wiki/sheet extract in prompt).
  - id: ep_lambda_eph_wall_regen
    name: EPH_WALL_REGEN
    type: excel_named_formula
    excel_definition: "LAMBDA(lab_lvl, prim_effect, ass_effect, LET(\n  SF, IF(prim_effect + ass_effect <> 0, prim_effect + ass_effect, 1),\n\n(10% * lab_lvl) * SF))"
    notes: From workbook defined name (user-provided wiki/sheet extract in prompt).
survivability_mechanics:
  version: prompt_wiki_excerpt_2026-02-02
  workbook: User-provided wiki excerpt in prompt.
  mechanics:
  - id: boss_wall_thorns_frac_v1
    name: BOSS_WALL_THORNS_FRAC
    type: excel_named_formula
    excel_definition: "LAMBDA(ws_level, wall_thorns_lvl, relic_bonus, substat_bonus, LET(\n  WS, MIN(ws_level, 99) * 1%,\n  WALL, wall_thorns_lvl * 1%,\n  NONRELIC, WS + substat_bonus,\n  RELIC, 1 + relic_bonus,\n  BOSS, 0.5,\n\nNONRELIC * RELIC * WALL * BOSS))"
    notes: Wall thorns uses a percent of tower thorns; workshop +1% per level up to 99, relics multiply non-relic sources, bosses take 50% thorns damage (all per user-provided wiki excerpt in prompt, wall assumed always up).
  - id: plasma_cannon_card_frac_v1
    name: PLASMA_CANNON_CARD_FRAC
    type: excel_named_formula
    excel_definition: "LAMBDA(card_pct, bc_mult, IF(card_pct > 0, card_pct * bc_mult, 0))"
    notes: Plasma Cannon card applies a percent of boss max HP immediately; card percent is from tables/wiki_cache/cards_rare.csv and BC multiplier is from tier battle conditions (user-provided wiki excerpt in prompt).
ids_helpers:
  status: complete
stacking_rules:
  status: complete
