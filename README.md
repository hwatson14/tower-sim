# Archive Branch

This branch is non-authoritative.

Use `main` for active implementation.
This branch exists only for historical handoffs, legacy materials, bulky reference files, and recovery.

Rules:
- Do not treat content here as active implementation guidance.
- Do not import archive material into runtime code.
- Do not merge this branch into `main`.
- If something from archive is needed again, reintroduce it deliberately as fresh work.