# R19 Next Consumption Steps

1. Add gap logging so each optimizer records missing optional rows separately from missing required payloads.
2. Add optional domain-specific row weights inside each objective family.
3. Add a small validator script later if you decide the downstream optimizer should hard-validate the manifest before running.
