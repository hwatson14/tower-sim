# R62 TODO Tracker

## Queued work

### 1. Module runtime consumption audit
- Status: queued
- Trigger: after progression engine merge tranche
- Goal: classify all 24 module unique canonical outputs as (a) consumed by runtime engines, (b) consumed only by statbook/output surfaces, or (c) still awaiting downstream runtime use.
- Notes: This was explicitly deferred by the merge workstream and must stay tracked until completed.

- Module runtime consumption audit
  - status: queued
  - trigger: after next merge tranche
