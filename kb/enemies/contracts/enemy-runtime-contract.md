# Enemy runtime contract

This domain currently relies mainly on tables and notes plus shared combat contracts. A dedicated enemy runtime contract has not yet been formalized; use enemy tables together with `kb/combat/contracts/runtime-damage-pipeline.csv` and `kb/combat/contracts/runtime-ordering-canonical.csv` for behavior interpretation.
