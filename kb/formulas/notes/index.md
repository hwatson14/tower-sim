# Formula surfaces

This directory stores canonical exact generators for formula-first surfaces.

Use order:
1. `canonical-formula-registry.csv` for exact linear generators and compact canonical representation.
2. `formula-derived-surface-registry.csv` to locate retained materialized views.
3. Domain row tables only when a human-readable or row-by-row audited surface is needed.
