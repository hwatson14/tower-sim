# Runtime proof pass v49

## Purpose
This note records the v49 runtime-proof pass that separates **material simulator closure** from **frame-exact micro-precedence**.

## What changed
- Added `kb/combat/contracts/runtime-proof-scope.csv` to define the active simulator target scope.
- Added UW and bot runtime proof matrices that classify remaining unresolved items as de-scoped frame-exact micro-precedence rather than open material-runtime defects.
- Added lab externalized input surfaces for `LAB_WALL_FORTIFICATION` so simulation can proceed from explicit account-state input without inventing a research ladder.

## Consequence
The repository-hosted KB can now claim closure for **material simulator scope** while remaining honest that frame-exact tie-break behavior is not bundled as canonical truth.

## Non-negotiable
This is not permission to guess. It is an explicit split between:
- materially necessary simulator mechanics that are closed for active use
- frame-exact boundary behavior that remains outside current KB scope until new evidence appears
