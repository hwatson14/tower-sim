contract_id: module_drop_economy_r59
status: active_partial_corrected
purpose:
  - Publish wiki-backed boss-drop and shatter truths using canonical runtime tier and QE-routed module lab surfaces.
required_upstream_surfaces:
  - derived::module.runtime_profile.farming_tier
optional_upstream_surfaces:
  - derived::module.lab.reroll_shards_bonus
  - derived::module.lab.common_drop_chance_bonus_pct
  - derived::module.lab.rare_drop_chance_bonus_pct
  - derived::module.lab.daily_mission_shards_bonus
  - derived::module.lab.shatter_shards_bonus_pct
published_surfaces:
  - derived::module.drop_policy.reroll_shard_drop_chance_pct
  - derived::module.drop_policy.reroll_shards_per_boss
  - derived::module.drop_policy.common_module_drop_chance_pct
  - derived::module.drop_policy.rare_module_drop_chance_pct
  - derived::module.shatter_policy.common_module_shards
  - derived::module.shatter_policy.rare_module_shards
  - derived::module.mission_policy.daily_mission_shards_bonus
  - derived::module.drop_policy.expected_shatter_equivalent_shards_per_boss
forbidden_shortcuts:
  - manual_runtime_tier_for_drop_tables
  - manual_module_lab_values
unresolved_items:
  - base_daily_mission_shards_per_mission
  - gem_draw_expected_shards_per_draw
  - reroll_shard_auto_roll_spend_ladder
