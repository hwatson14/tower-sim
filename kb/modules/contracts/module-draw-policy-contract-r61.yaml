contract_id: module_draw_policy_r61
status: active_partial
purpose:
  - Publish wiki-backed module draw rarity rates and draw policy facts through QE.
published_surfaces:
  - derived::module.draw_policy.common_rate_pct
  - derived::module.draw_policy.rare_rate_pct
  - derived::module.draw_policy.epic_rate_pct
  - derived::module.draw_policy.epic_pity_draws
  - derived::module.draw_policy.ten_pull_minimum_rare
  - derived::module.draw_policy.gem_cost_per_draw
unresolved_items:
  - epic_draw_shard_equivalent_value
  - ten_pull_expected_value_model
rules:
  - rates are single-draw rarity rates
  - ten-pull rare guarantee must not be silently folded into single-draw EV without an explicit model
