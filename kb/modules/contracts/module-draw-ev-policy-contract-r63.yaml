contract_id: module_draw_ev_policy_r63
status: active_package_policy
purpose:
  - Encode the package policy for module draw expected shard value used by the shard income calculator.
package_policy:
  - epic draws are retained and contribute zero immediate shard value
  - a 10-pull is modeled as ten independent single draws for EV
published_surfaces_expected:
  - derived::module.draw_policy.epic_draw_immediate_shard_value
  - derived::module.draw_policy.ten_pull_ev_multiplier
rules:
  - this policy is package-scoped and must be explicit in KB
  - draw EV under this policy may be treated as exact for the package calculator
