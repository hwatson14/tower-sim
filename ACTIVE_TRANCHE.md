# ACTIVE_TRANCHE.md

## Role

This file is a root governance source of truth for the active repository state.

It identifies:
- the current hardening sequence
- the exact active tranche
- what is already verified
- what must happen next
- stop conditions and closeout criteria

Governance truth for this repository lives directly in:
- this `ACTIVE_TRANCHE.md` file
- `BURNDOWN.yaml`

Machine-readable state lives in:
- `BURNDOWN.yaml`

---

## Current state

The repo is **not in broad rebuild territory**, and the V28-A/B source-truth migration is closed for the currently tested parser, KB/QE routing, and Dissonant Echo surfaces.
The repo has completed the prior hardening closeout sequence and the maintenance stabilization / hygiene tranche.
The completed V28-A/B source-truth migration established:
- accept the v28 `_IDS` layout only where the source shape is explicitly validated
- fail closed when v28 source migration leaves required economy contributors missing or null
- route Dissonant Echo lab levels through KB/QE-owned surfaces and publish Echo multipliers from QE
- keep v28 mechanics work staged through KB/QE/simulator owner layers rather than patching UI-local truth

The v49 Streamlit-first product scope remains the last completed product architecture baseline:
- make every current-scope canonical, user-relevant stat visible in Streamlit
- keep those visible stats KB/QE-aligned, provenance-bearing, and contract-governed
- keep the Boss Waves product path replacement-owned through the staged Table 1 / Table 2 / combat kernel
- meet the current-stage performance contract honestly
- remove stale duplicate authority and residue where they undermine truth, speed, or blind maintainability

### Durable truths on the current baseline

- ownership layering remains intact (`app -> advisors -> evaluators -> simulators -> qe -> input`)
- runtime formula authority table is present and canonical coverage remains explicit
- native family query path is present for progression/timing/report surfaces
- the current active snapshot has a durable full-suite recertification artifact
- the latest historical shipped-baseline audit from `tower-sim-src(9).zip` on **2026-04-15** was **not** fully green (`392 passed, 1 failed, 1 skipped`) until rebuilt-state repair work ran
- the latest rebuilt-worktree full suite on the repaired current baseline passed on **2026-04-15** (`399 passed`)
- the current V28-A/B recertification run on **2026-06-11** passed full pytest (`619 passed`)
- formula surface policy remains active
- the repo already exposes a live Boss Waves Streamlit surface wired through the replacement-owned `app.pipeline` and simulator path; the deleted legacy row-engine must not be recreated as a compatibility shim
- committed QE query-row artifacts already include `state::economy.interest_per_wave_pct`, `state::wall.rebuild_seconds`, and `state::tower.thorns_damage_pct`

### Remaining constraints

- keep governance text synchronized with executable validation status and burndown state
- treat `TowerSim_bible_v49.md` as the sole active product-and-scope authority for the current program
- do not widen into evaluator or optimiser delivery
- no app-side stat calculation
- no compare-only assumptions used to make dashboard values appear correct
- no `line_verification` or `input_dashboard` authority for displayed stats
- no stale `out/` trust
- no fallback routing as final architecture
- no second active authority path
- fail closed when the artifact contract is ambiguous rather than guessing

---

## Hardening sequence

| # | Name | Status |
|---|------|--------|
| T0 | Governance truth | COMPLETE |
| T1 | Streamlit contract execution | COMPLETE |
| T2 | QE authority closure | COMPLETE |
| T3 | Evaluator cleanup | COMPLETE |
| T4 | Thinning and polish | COMPLETE |

## Active tranche record: v49 Streamlit-first canonical stat and simulator program - IN PROGRESS

### Goal

Turn the governed maintenance baseline into the current scoped product:
- canonical visible stat truth in Streamlit
- sanctioned max-wave simulator delivery through Streamlit
- honest performance evidence for the active product path
- residue and governance cleanup that reduces authority blur

This tranche is about:
1. truth protection and scope reset
2. visible-stat contract activation
3. canonical stat visibility completion
4. Streamlit operational product completion
5. sanctioned max-wave simulator completion and staged Boss Waves replacement build
6. targeted performance hardening
7. residue cleanup and governance synchronization

This tranche is not about:
1. evaluator delivery
2. optimiser delivery
3. speculative CI or precompute programs
4. UI-local truth patches
5. broad rewrite for aesthetic cleanup
6. broad refactors outside touched owner surfaces

### Snapshot facts being advanced

1. the repaired current worktree has a durable full-suite green baseline from **2026-04-15**
2. the repo already has a live Boss Waves tab and sanctioned app/simulator runtime seam
3. current committed QE surfaces already cover `Interest / Wave`, `Wall Rebuild`, and the current `tower thorns` naming
4. root governance still misstates the active program as maintenance stabilization
5. after the 2026-04-15 final product-correctness hardening tranche, the remaining open completion gate is honest current-stage performance evidence for the sanctioned product paths

### Active verification items

1. COMPLETE: sync root governance to the v49 program and active authority order
2. COMPLETE: prove the visible-stat contract is active in implementation and tests
3. COMPLETE: recover the Stats product contract so canonical visibility is operator-useful in Streamlit rather than a resolved-section-heavy dump
4. COMPLETE: recover Streamlit operational product usability so Boss Waves remains usable even when optional Stats/debug data is partial or absent
5. COMPLETE: wire Boss Waves perk evolution through the sanctioned simulator path and revalidate the no-delta-fallback live path
6. COMPLETE: clean the active repo-root control allowlist and remove stale zip-pack residue
7. COMPLETE: recertify the repaired Streamlit product contract with targeted tests, full pytest, `app.run_stats`, and a UI-level Streamlit harness run
8. COMPLETE: recertify the Boss Waves product surface so the main table is operator-facing, the raw row dump is demoted to debug, and the active DR/contact assumptions used by the solver are visible in the product surface
9. COMPLETE: repair fresh-clone Stats contract truth, live preset/view-state wiring, grouped Modules layout, cumulative Bot/Guardian spend, mojibake normalization, and primary-surface reconciliation semantics
10. COMPLETE: repair Boss Waves semantics so the product uses one canonical run-context contract, separates actual boss cadence from checkpoint sampling, and fails closed for Tourney when required tournament-wave context is unavailable
11. COMPLETE: remove the hidden fresh-clone generated-artifact dependency from the touched Stats contract test family
12. COMPLETE: harden the visible product so UW supplemental/UW+ handling, Bot/Guardian cumulative spend and totals, Guardian Scout truth, per-surface recon semantics, and Boss Waves regen auditability are all visible and defensible in the live product surface
13. COMPLETE: build and harden the Boss Waves staged replacement architecture: compiled RunPlan, explicit Table 1/Table 2 registries, Table 1 survivability contributor bundles, Table 2 scenario transforms, v21 event-only combat, avg/min/max lanes, and operator-table handles, without depending on the legacy row-engine hot path
14. COMPLETE: run bounded parity/shadow validation between the sanctioned live Boss Waves path and the staged replacement path; result was **parity sufficient for cutover planning**, with no production replacement-path fixes required during parity
15. COMPLETE: define the Boss Waves cutover plan, including split cutover phases, required field-map artifact, acceptance gates, operational rollback, and temporary legacy disposition
16. COMPLETE: implement Phase 2A Boss Waves cutover for operator table and summary only, with an actual product-consumption-derived field map, centralized `app.pipeline` source-selection/rollback seam, replacement-backed operator rows, legacy-compatible download rows, legacy-compatible diagnostics, and app-level tests
17. COMPLETE: harden Phase 2A Boss Waves replacement-input independence so replacement-backed operator rows and summary are assembled from direct app inputs, `qe.run_plan`, and `simulators.evaluator_kernel` rather than legacy Boss Waves row-engine rows or summary
18. COMPLETE: implement Phase 2B Boss Waves CSV/export and diagnostics source switch using replacement-owned export rows and replacement-owned active diagnostics
19. COMPLETE: excise Boss Waves product-path legacy rollback/shadow branches and inventory remaining `simulators.run_executor.py` consumers
20. COMPLETE: repair Boss Waves replacement stat evolution with an explicit primitive/display split: QE `state::wall.hp` carries wall-health ratio contributors and Boss Waves derives pre-fort Wall HP from QE `state::tower.hp` times wall-health ratio times non-fort wall-health multipliers before Table 2 applies fortification exactly once, QE `state::wall.regen` is a start-of-run wall-regen percent-points primitive combined with QE `state::tower.regen` for displayed Wall Regen, Table 1 re-derives row-owned wall HP / wall regen contributors from evolving workshop/perk state, and the operator table displays transformed final Wall Pool / Wall Regen rows; Death Wave remains outside wall HP / wall regen derivation
21. COMPLETE: close Boss Waves wall-surface semantics and recertify the corrected live replacement path: `state::wall.hp` is final only as a transformed primitive input for row-derived pre-fort Wall HP / fortified Wall Pool, `state::wall.regen` is final only as a transformed percent-points primitive for displayed Wall Regen HP/sec, parity/shadow validation now includes the post-survivability-fix large-wall-surface case, and Streamlit no longer silently falls back from missing `operator_rows` / `download_rows` to `rows`
22. COMPLETE: retire and delete the broader legacy executor after final symbol-role inventory found no active non-test consumer; `simulators/run_executor.py` and its direct defender tests are removed, and Boss Waves certification now uses the replacement kernel only
23. COMPLETE: correct Boss Waves v21 TTK/TTD wiring so displayed TTK is the total event-only kill time from Plasma Cannon, orbs, electrons, and contact thorns, Sharp Fortitude affects contact-thorns timing, and Black Hole/Chrono Field duration perks flow into row-level timed DR
24. PARTIAL: run the bounded Boss Waves product-honesty/live-closure tranche: Boss Waves now requests effective Black Hole timing from `state::uw.black_hole.duration_seconds` / cooldown via the timing owner, exposes effective Chrono Field timing/DR through the progression family, threads tier battle-condition resistances and boss hit interval into replacement combat, treats `effective_damage_reduction_pct` as a final DR override that bypasses timed DR stacking, preserves explicit zero runtime overrides, and returns a structured incomplete payload when `stop_on_failure=False` and the owned boss spawn-to-wall contact-time primitive is missing. The tranche did not close boss spawn-to-wall contact-time ownership, broader UW publication/Stats parity, dead runtime-field cleanup, or checkpoint-summary naming.
25. COMPLETE: close Boss Waves perk-mode semantics for the replacement product path: `runtime_timeline` now drives row evolution from the perk timeline generator, `max_progression_policy` now applies the active max-policy perk preset as a static Table 1 perk state instead of reusing the runtime timeline, `none` and `perk_state=off` disable perk contributions, and diagnostics/contract metadata expose the requested mode, state, effective application mode, timeline row count, and static perk counts.
26. COMPLETE: close the narrow UW effective-state publication/Stats parity tranche for Boss Waves product honesty: run-stats now publishes effective `state::uw.chrono_field.duration_seconds`, `state::uw.chrono_field.cooldown_seconds`, `state::uw.chrono_field.damage_reduction_pct`, and `state::uw.chrono_field.slow_pct`; Stats now reconciles Black Hole and Chrono Field rows against the same effective BH/CF truth used by Boss Waves, including BH 36/46 start timing, BH duration perk to 48 seconds, CF 50/60 start timing, CF duration perk to 55 seconds, and CF slow 62.25%.
27. SUPERSEDED: the earlier scenario-owned Boss Waves perk policy was narrowed by item 31. Tournament mode still disables perks by scenario guard, but non-tournament Boss Waves now honors request-level `runtime_timeline`, `max_progression_policy`, `none`, and `perk_state=off` controls.
28. COMPLETE: add the Streamlit Perks product surface as a pipeline-backed policy preview/control surface: Streamlit can edit session-local bans, first perk choice, priority order, seed, and preview wave; `app.pipeline.build_perk_timeline_preview` resolves those controls against KB perk definitions and account labs; `simulators.perk_timeline_generator` remains the generator owner; Boss Waves consumes the same session policy override through `build_boss_wave_payload` rather than a separate UI-local policy path.
29. SUPERSEDED: `boss_time_to_contact_seconds` is now an optional manual override. When omitted, Boss Waves derives contact time from a 2s base plus QE-owned Chrono Field slow, Energy Net duration, and Slow Aura enemy-speed reduction.
30. COMPLETE: repair the Boss Waves Phase A invariant defects found during v28/max-wave review: boss cadence now comes from `simulators.scenario`, late-tier ELS reduction is applied as an absolute probability subtraction in Table 1, Table 2 fails closed rather than replaying ELS history from a row-local skip chance, summary headline waves are sequential/contiguous, explicit PBH uptime is wired into the survival stack when supplied, and diagnostics label the current model as `boss_contact_survivability` rather than a certified full dissonance max-wave model.
31. COMPLETE: repair the Boss Waves Excel-comparison blockers found after Phase A: non-tournament Boss Waves now honors requested perk mode/state instead of forcing the farming runtime timeline, and `simulators.evaluator_kernel` publishes an explicit fast contact-envelope survivability diagnostic (`wall_pool_plus_contact_window_regen_vs_first_boss_hit`) alongside the existing repeated-hit kernel so max-wave workbook comparisons no longer require external spreadsheet-only calculations.
32. COMPLETE: add input-owned named perk policy presets for `eHP Max Waves`, `eHP Farming`, `GC Max Waves`, and `GC Farming`; PWR is the configured first perk choice and highest priority in each preset, game speed is the next configured priority, `app.pipeline` / CLI / Streamlit carry `perk_policy_preset`, and diagnostics split requested versus resolved policy preset while `simulators.perk_timeline_generator` remains the only timeline generator.
33. COMPLETE: reconcile the remaining v28 Stats/UI contract expectations against current IDS/EP-export-backed values: free-upgrade chance now reflects IDS `Free Upgrades +` x1.17, T14 eHP/eEcon assertions match the current tier-scoped pipeline output, Stats dashboard UW/Bot/Guardian rows assert effective v28 surfaces and cumulative spend, Ultimate Crit helper factor follows the current resolved crit-factor surface, and full `pytest -q` is green.
34. COMPLETE: close the current EP stat-reconciliation pass to zero true formula mismatches: Bounce Shot Chance max-workshop projection now follows the row-verified 68.0% KB formula and makes `derived::edamage.bounce_factor` exact against EP, EP compare context now carries real Tourney/Farming card/module metadata, and remaining EP differences are classified as stage-scope rows for unexported workbook assumptions such as range lab slider level, effective module-substat policy, DPM-at-range, Project Funding cash, UW targeting, and economy kill-share guesses.
35. COMPLETE: close the first Excel max-wave reconciliation repair: `orb_boss_total_damage_pct` is now treated as final total damage-to-boss rather than being reduced again by tier orb resistance, while the per-hit orb path still applies orb resistance; `_IDS` bot/UW unlock flags now flow through input and QE, locked bots/UWs publish owned=false and gated zero track values, and Streamlit Boss Waves still exposes the runtime-owned Flame Bot DR override so workbook assumptions such as `FLAME_DR = 95%` can be supplied explicitly without changing KB/account truth.
36. COMPLETE/UPDATED: wire the workbook GC-loadout mechanics into Boss Waves without copying workbook shortcuts: `GC Max Waves` routes Boss Waves stat primitives through the Tourney loadout, while farming-named presets (`eHP Farming`, `GC Farming`) and eHP max-wave presets route through Farming; QE-published Chain Lightning DPS remains a QE-owned diagnostic/fallback support lane, while the default Boss Waves boss-damage path starts from `derived::edamage_ep` and replaces EP Spotlight, ACP, and slow/exposure terms with Boss Waves-specific runtime exposure; Energy Net duration and Energy Net Mastery are KB/QE-backed Boss Waves primitives; Slow Aura Mastery is a QE-owned boss-hit interval primitive; and the evaluator applies timed damage/interval primitives through generic combat inputs rather than card-specific kernel branching.
37. COMPLETE/SUPERSEDED: selected max-wave semantics now use one unified hit-by-hit boss-survival lane for all eHP/GC/farming loadouts. Loadout type changes the stats, cards, modules, perks, and damage inputs; it does not change game physics. GC contact-envelope and pre-contact boss-kill values remain diagnostics, and Dissonant Runs apply KB-owned stat/system masks before the same selected lane is evaluated.
38. COMPLETE: replace static hand-authored Boss Waves perk preset lists with lab-aware goal-benefit-matrix generation for `eHP Max Waves`, `eHP Farming`, `GC Max Waves`, and `GC Farming`; PWR and game speed remain fixed openers, Ban Perks capacity comes from IDS labs, locked-UW perks are excluded before ban ranking, and Streamlit/app previews expose the generated matrix and resulting policy before the simulator-owned perk engine produces actual run orders.
39. COMPLETE: implement and audit the four Boss Waves Dissonant Run restriction masks and expose their max-wave matrix: Attack routes the QE eDamage restricted branch and locks attack workshop primitives, Defense disables defensive survivability, thorns contact damage, and defensive workshop progression, Utility disables free upgrades, packages, and ELS progression, and Ultimate Weapons disables CL/BH/CF/DW-health runtime effects. Streamlit now displays Regular plus all four Dissonant Run max-wave rows for the active perk/loadout preset. The follow-up GC audit expanded Boss Waves damage primitive resolution to the KB-declared eDamage dependency surfaces, added explicit scenario runtime inputs for caller-owned boss-applicable eDamage bridge comparisons without making the workbook calibration a hidden default, corrected selected max-wave semantics so GC uses the same unified hit-by-hit lane as every other loadout, and split post-failure table truncation from max-wave result completeness.
40. COMPLETE/UPDATED: add the optional all-tier Boss Waves milestone matrix for Regular plus all four Dissonant Run categories. The matrix sweeps T1-T21, evaluates the four named perk/loadout presets for each category, prefers complete/owned candidates, then selects the highest `selected_max_wave`, publishes the best loadout/model/status per cell, and can be generated on demand with `app.run_stats --include-boss-wave-milestone-matrix` or from the Streamlit Boss Waves tab. Streamlit now defaults the all-tier matrix button to the same Regular-plus-four-Dissonant-category sweep and exposes a selected-run shortcut only as an explicit operator control. Defaults are now Milestone preset, derived boss contact time from 2s base plus CF/EN/Slow Aura unless overridden, 6% total orb boss damage, 30000 end-wave cap, and current `boss_contact_survivability` model scope rather than a certified full late-run max-wave model.
41. COMPLETE: move Dissonant Run restriction truth out of `app.pipeline` into the KB-owned `kb/global-rules/contracts/dissonant-run-restrictions.yaml` contract. `qe.kb_surfaces.load_dissonant_run_restrictions()` is now the cached runtime facade, while `app.pipeline` only applies the loaded primitive restrictions, workshop-track masks, disabled-system labels, and UW conditional GC bridge rule.
42. COMPLETE: publish Boss Waves Dissonant Run active support flags consistently for all four categories. Attack and Defense still drive QE eDamage restricted branches, while Utility and Ultimate Weapons now also expose active runtime support rows and diagnostic primitives so the max-wave product can prove which category mask was active without app-only inference.
43. COMPLETE: formalize Dissonant Run active support surfaces in KB/QE contracts. `support_surface::dissonance.{attack,defense,utility,ultimate_weapons}_run_active` are now declared as derived-scope, runtime-injected support gates in the ownership ledger, initial surface registry, and dependency ledger; the missing `derived::edamage.defense_dissonance_shockwave_restricted` ownership row is also declared.
44. COMPLETE/UPDATED: map the workbook CL/EP boss-DPS bridge to explicit simulator inputs without copying workbook defaults. Boss Waves accepts a complete decomposed eDamage-to-boss bridge factor set (`boss_edamage_target_share`, `boss_edamage_cadence_uptime_factor`, `boss_edamage_reliability_factor`, `boss_edamage_semantic_normalizer`) and still honors explicit bridge overrides only when supplied completely; partial decomposed bridge inputs fail closed with a ValueError. No Dissonant Run category gets a stronger default boss-damage source than regular runs: defaults now use the same `derived::edamage_ep` boss-exposure replacement model, while the CL-only support lane is retained for diagnostics/fallback. Selected wave semantics still use the unified hit-by-hit boss-survival lane.
45. COMPLETE: expose the decomposed eDamage-to-boss bridge on the Streamlit Boss Waves product surface. The manual combat assumptions panel now provides controls for the four bridge factors and threads them through both the immediate Boss Waves payload and the optional all-tier milestone matrix; defaults remain omitted/zero so existing fail-closed behavior is unchanged.
46. COMPLETE: add side-by-side all-tier milestone matrix comparison for explicit bridge assumptions. `build_boss_wave_milestone_matrix(...)` now keeps the default matrix as current account truth, and when supplied `comparison_scenario_runtime_inputs`, returns a `comparison` overlay with bridge-assumption matrix rows plus per-tier/category default-vs-comparison wave deltas; Streamlit renders that overlay only when bridge factors are supplied.
47. COMPLETE: expose the all-tier bridge comparison overlay through the CLI/output workflow without changing default truth. `app.run_stats --include-boss-wave-milestone-matrix` now accepts the four `--boss-wave-bridge-*` factors, passes them only as comparison scenario inputs, records `comparison_enabled` in diagnostics, and leaves the committed default matrix free of comparison data unless the flags are supplied. Boss Waves payload account-state construction is cached by source path, applied perk mode, policy preset, and active loader/builder functions so the matrix does not rebuild account state for every tier/category/loadout cell.
48. COMPLETE: harden Boss Waves milestone-matrix runtime without changing matrix semantics. The matrix now uses progressive horizons for high-tier and Defense Dissonance stop-on-failure candidates, reuses bounded cached replacement primitive snapshots across repeated horizons, and collapses free-upgrade generation to an equivalent closed-form recurrence in `qe.run_plan`. Direct full default matrix timing improved from roughly 155s before this pass to roughly 75s on the same local command, while the rebuilt default `out/boss_wave_milestone_matrix.json` kept the same T14 values and remains comparison-free by default. Regression tests now prove the closed-form free-upgrade recurrence against a stepwise implementation, the progressive matrix path against forced full-horizon evaluation, and the primitive cache copy/bounds behavior.
49. COMPLETE/UPDATED: recertify the post-audit output contract and timing evidence for the all-tier Boss Waves matrix inside the current boss-contact scope. `out/boss_wave_milestone_matrix.json` is explicitly tracked in the active output contract, the tracked-output boundary test includes it, and bounded `run_stats` publishes the all-tier matrix on demand. Current local evidence after the EP boss-exposure replacement and Slow Aura Mastery repair: full pytest passed 529 tests, `app.run_stats --include-boss-wave-milestone-matrix --boss-wave-contact-time-seconds 10 --boss-wave-orb-boss-total-damage-pct 6` completed and regenerated committed outputs, and selected matrix rows have no model-completion blockers. The model is still intentionally labeled as a boss-contact survivability model rather than a separate all-enemy pressure simulator.
50. COMPLETE/UPDATED: add the first simulator-owned Geometry Engine tranche as displayed/proxy geometry only. `simulators/geometry.py` now owns tower theoretical-to-displayed range transforms, wall/orb/BH/Golden Bot/Chrono Field displayed surfaces, denominator-explicit overlap/path-exposure proxies, spotlight angular coverage proxy, DPM floor helper, blocked surface metadata, and proxy-governance payloads. Bounded `run_stats` emits `geometry_engine_payload.json`, `geometry_range_report.json`, `geometry_range_report.csv`, `geometry_consumer_interfaces.json`, and `geometry_proxy_governance.json` as local support artifacts. This does not promote wall contact, boss contact time, captured/tagged enemy fraction, density-weighted coin truth, or non-GB bot geometry to active truth.
51. COMPLETE/UPDATED: wire Geometry Engine displayed wall-travel proxies into Boss Waves contact timing as the default base-seconds candidate. `simulators.geometry` now emits `boss_path_distance_to_wall_displayed_candidate_m` and `boss_contact_time_displayed_proxy_seconds`; `simulators.timing` consumes that value only as a range-sensitive displayed proxy base before applying Chrono Field, Energy Net, and Slow Aura timing effects, while explicit `boss_time_to_contact_seconds` runtime overrides still win. Diagnostics publish the geometry proxy status and truth status, and measured/effective wall-contact truth remains unpromoted.
52. COMPLETE/UPDATED: recertify the V28-A/B source-truth migration for the current tested surfaces: parser/source-shape tests, QE routing/contracts, Dissonant Echo lab-level routing, derived Echo multipliers, app main path, bounded `app.run_stats`, optional Boss Waves milestone matrix generation, Stats contract, simulator kernel/core smoke, import boundaries, and full pytest all pass on 2026-06-11. Current evidence is `619 passed`; the Boss Waves milestone matrix remains honestly scoped as `boss_contact_survivability` and still exposes source-owned non-boss terminal-pressure formulas as the blocker for claiming a full all-enemy max-wave model.
53. COMPLETE/UPDATED: harden the Streamlit all-tier Boss Waves matrix disclosure for clean IDS/PB reference alignment. The matrix frame now exposes `Calculated` beside the selected/best displayed wave and reports clean `ids_reference_alignment` directions in the visible `Alignment` column when clean-reference alignment is active. This keeps disco and milestone reference alignment auditable in the product surface instead of only in JSON metadata.
54. COMPLETE/UPDATED: align the Streamlit all-tier matrix category scope with the CLI/output matrix contract. The Streamlit Boss Waves tab now defaults the matrix run to Regular plus Attack, Defense, Utility, and Ultimate Weapon Dissonant Run categories, while retaining an explicit `All run types` control for narrowing back to the currently selected run type when the operator wants a faster focused run.
55. COMPLETE/UPDATED: label Streamlit all-tier matrix rows by run type. Because the default Streamlit matrix now includes Regular plus four Dissonant Run categories, the matrix frame exposes `Run type` from the sanctioned matrix row label alongside `Tier`, so disco rows are no longer visually collapsed into repeated tier rows.
56. COMPLETE/UPDATED: make IDS/PB alignment the default Boss Waves matrix product view for clean rows. `app.run_stats --include-boss-wave-milestone-matrix`, `PipelineRunRequest`, `build_boss_wave_milestone_matrix(...)`, and the Streamlit all-tier matrix checkbox now default clean rows to their active IDS milestone/Dissonant PB reference while preserving `best_calculated_selected_max_wave`, raw candidate results, delta, and alignment metadata. `--boss-wave-compare-clean-reference-rows` and the unchecked Streamlit control keep the comparison-only audit mode available.
57. COMPLETE/UPDATED: reduce the Boss Waves milestone-matrix simulator evaluator hot path without changing matrix semantics. `simulators.evaluator_kernel` now caches validated perk-contribution summaries by normalized contribution values, so repeated Table 2 row evaluation reuses parsed effect totals/products while preserving fail-closed validation for unsupported effects and negative multipliers. Targeted simulator and matrix tests pass, the real `app.run_stats --include-boss-wave-milestone-matrix` probe completed in 68.615s, and temp `boss_wave_milestone_matrix.json` / `run_stats.json` matched the committed outputs.
58. COMPLETE/UPDATED: make explicit Boss Waves pressure-factor analysis a first-class certification closure mode. Non-default caller-supplied `boss_wave_pressure_factor` now closes the non-boss terminal-pressure requirement as `boss_wave_pressure_factor_approximation` in `model_certification`, matrix-selected row aggregation preserves that closure, and Streamlit's model-assumptions table exposes the closure mode/factor. This remains an explicit approximation over boss HP and incoming damage, not a source-owned fleet/elite/protector/armored formula or a full all-enemy certification claim.
59. COMPLETE/UPDATED: make v28 Damage/Health Decay explicit-input closure auditable in Boss Waves certification. `model_certification` now publishes `v28_damage_health_decay_closure` with required fields, missing fields, optional start-wave fields, source-default availability, and closure mode; Streamlit's model-assumptions table exposes those rows. The simulator still applies caller-supplied decay values through Table 2, while source-owned tournament Damage/Health Decay magnitudes remain unavailable rather than silently defaulted.
60. COMPLETE/UPDATED: reduce repeated QE full-trace publication work without changing trace payloads. `qe.kernel` now skips republishing trace-step detail sequences that are already in published surface/node-id space (`requested_surface_ids`, initial direct lineage ids). Focused QE/app tests passed, a warm `RunStatsSession.execute` profile dropped from 1.238s / 1,102,714 calls to 0.706s / 808,618 calls in the local sample, and a real matrix probe completed in 68.058s with all committed semantic artifacts byte-identical. This is a narrow P5 hot-path cleanup, not P5 completion.
61. COMPLETE/UPDATED: avoid rebuilding identical Stats dashboard workshop reconciliation payloads for both state-mode variants. `qe.publication.build_stats_dashboard_payload(...)` now builds the Workshop reconciliation table once per preset because that payload already contains start/max columns and is not state-mode-specific. The focused dashboard tests and full stats-dashboard contract pass, warm profile samples show `publish_workshop_reconciliation_payload` calls reduced from 4 to 2 and `build_workshop_reconciliation_row` calls from 188 to 94, and a matrix probe completed in 69.074s with all committed semantic artifacts byte-identical. This is a QE publication hot-path cleanup only; it does not change stat truth.
62. COMPLETE/UPDATED: preserve raw Boss Waves calculated-vs-reference divergence after clean IDS/PB alignment. Matrix rows, wide rows, and the Streamlit all-tier matrix now carry `calculated_delta_vs_reference_wave` and `calculated_to_reference_ratio` alongside the existing selected/aligned `delta_vs_reference_wave`, so a clean row aligned to an IDS milestone or Dissonant PB no longer presents a zero selected delta as if the raw boss-contact calculation also matched. Targeted Boss Waves matrix/UI tests passed and `app.run_stats --include-boss-wave-milestone-matrix` rebuilt the generated artifacts. This is product-honesty carry-through for the current partial boss-contact model, not source-owned non-boss pressure formula completion.
63. COMPLETE/UPDATED: publish a matrix-level raw-reference alignment summary for Boss Waves. `build_boss_wave_milestone_matrix(...)` now emits `reference_alignment_summary` with total rows, reference-backed rows, IDS/PB-aligned rows, raw over/under/match counts, worst raw delta, and a per-run-type breakdown; Streamlit renders that app-published summary above the all-tier matrix. The current generated matrix reports 105 rows, 76 rows with calculated reference deltas, 60 IDS/PB-aligned rows, 59 raw overestimates, 1 raw underestimate, 16 raw matches, and a worst raw delta of 24,910 waves on Tier 1 Attack Dissonant. This makes disco/reference mismatch distribution visible without changing simulator selection or inventing source-owned non-boss pressure formulas.
64. COMPLETE/UPDATED: add side-by-side Boss Waves pressure-factor comparison without changing account-truth defaults. `--boss-wave-comparison-pressure-factor` now feeds the existing matrix comparison overlay as `boss_wave_pressure_factor`, Streamlit's all-tier matrix has a comparison-only pressure-factor control, and comparison payload labels distinguish pressure-factor overlays from bridge overlays. Targeted parser/matrix/UI tests passed, and a real temp CLI matrix comparison with factor `1.25` completed in 161.105s with `comparison.label = pressure_factor_assumptions`. This lets difficulty-factor experiments be compared against the default matrix without making the factor a hidden KB/default truth.
65. COMPLETE/UPDATED: render Boss Waves matrix comparison overlays in Streamlit. The all-tier matrix UI now reshapes the app-published `comparison.wide_rows` into a visible long comparison table with default wave, comparison wave, delta, limiter, alignment, and uncapped metadata per tier/run type. This closes the product-surface carry-through for bridge and pressure-factor comparison overlays without recomputing truth in Streamlit. Targeted comparison/UI tests passed (**8 passed**); no default generated artifact rebuild was required because the matrix payload already carried the comparison data.
66. COMPLETE/UPDATED: carry Boss Waves matrix summary metadata into generated `run_stats` diagnostics. Both run_stats matrix build paths now use one app-owned diagnostic helper that publishes `reference_alignment_summary`, comparison label/input/count metadata, and comparison reference-summary metadata when present. The committed `out/run_stats.json` now reports the same 105-row alignment summary as the matrix artifact, so CLI/output consumers do not need to parse `boss_wave_milestone_matrix.json` just to see matrix mismatch status. Targeted diagnostics/comparison tests passed (**9 passed**) and the default matrix output regenerated in 67.895s.
67. COMPLETE/UPDATED: make Streamlit Boss Waves comparison matrices usable for selected-tier Dissonant debugging. The preset matrix UI now has an `All tiers` toggle; default behavior remains all tiers, but clearing it passes only the currently selected Boss Waves tier to `build_boss_wave_milestone_matrix(...)`. This uses the existing app-owned `tiers=` argument and does not change simulator semantics. Streamlit contract tests passed, and a direct T14 all-run-type pressure-factor comparison probe completed in 4.826s with five base rows and one comparison wide row.
68. COMPLETE/UPDATED: make CLI `--tier` scope the optional Boss Waves milestone matrix. Both run_stats matrix build paths now pass `tiers=(args.tier,)` when the existing `--tier` override is supplied, preserving all-tier behavior when it is omitted. This closes the CLI half of selected-tier Dissonant/matrix debugging without adding a new flag or changing simulator semantics. Targeted matrix-routing tests passed, and a real temp `app.run_stats --tier 14 --include-boss-wave-milestone-matrix` probe completed in 4.247s with `tiers=[14]` and 5 matrix rows.
69. COMPLETE/UPDATED: make CLI non-`none` `--dissonance-run-category` scope the optional Boss Waves milestone matrix. Both run_stats matrix build paths now pass `dissonance_run_categories=(category,)` when the existing CLI category override is a Dissonant category, while omitted/`none` keeps the all-category matrix. `execute_pipeline(...)` also preserves `PipelineRunRequest.dissonance_run_category` when building args. Targeted routing tests passed, and a real temp `app.run_stats --tier 14 --dissonance-run-category utility --include-boss-wave-milestone-matrix` probe completed in 1.738s with `tiers=[14]` and one Utility row.
70. COMPLETE/UPDATED: render Boss Waves comparison alignment summaries in Streamlit. The preset matrix surface now reads `comparison.matrix.reference_alignment_summary` from the app-published payload and displays the comparison scenario's raw over/under/match and worst-delta counts alongside the existing comparison row overlay. This closes a visibility gap for pressure-factor Dissonant debugging without recomputing truth in Streamlit or changing matrix selection/calculation semantics. Focused UI contract tests passed.
71. COMPLETE/UPDATED: carry raw calculated Boss Waves comparison waves through the comparison overlay. `app.pipeline` now copies per-run-type calculated wave, calculated delta, raw delta-vs-reference, and calculated/reference ratio from both the base and comparison wide rows into `comparison.wide_rows`, and Streamlit renders those fields in the comparison frame. This keeps selected IDS/PB alignment from hiding whether a pressure-factor or bridge comparison improved or worsened the raw boss-contact calculation. Focused functional/UI tests passed.
72. COMPLETE/UPDATED: summarize raw Boss Waves comparison movement in generated run_stats diagnostics. `_boss_wave_milestone_matrix_diagnostics_payload(...)` now publishes `comparison_calculated_delta_summary` from `comparison.wide_rows`, including higher/lower/match counts, worst raw movement, and the selected/aligned plus raw calculated context for the worst row. A real focused T14 Utility pressure-factor CLI probe wrote `run_stats.json` with one lower raw movement entry, `calculated_delta_wave = -90`, `default_calculated_wave = 3699`, and `comparison_calculated_wave = 3609`.
73. COMPLETE/UPDATED: promote the raw Boss Waves comparison movement summary into the matrix payload itself. `build_boss_wave_milestone_matrix(...)` now emits `comparison.calculated_delta_summary`, Streamlit renders that app-published summary, and run_stats diagnostics read it from the matrix payload before falling back to recomputation for compatibility. A focused T14 Utility pressure-factor CLI probe showed the matrix artifact and `run_stats.json` diagnostics agree exactly on `row_count = 1`, `comparison_raw_wave_lower_count = 1`, `calculated_delta_wave = -90`, and raw calculated waves `3699 -> 3609`.
74. COMPLETE/UPDATED: expose full v28 Damage/Health Decay closure details in the Streamlit Boss Waves assumptions table. The product surface now renders the app-published model-certification source-default availability, supplied magnitude fields, and supplied start-wave fields alongside the existing closure mode, required fields, and missing fields. This improves MW-2 product honesty without inventing source-owned Damage/Health Decay magnitudes. Focused UI contract/helper tests passed.
75. COMPLETE/UPDATED: declare the KB/QE environment-param metadata for `bc.enemy_level_skip_reduction_pp`. The IDS `Enemy Level Skip Reduction` lab row was already routed to this Boss Waves-relevant battle-condition surface, but the bounded metadata contract now declares it as `unit: pct` with `bc_effective_value`, so bounded QE resolution carries percent schema instead of falling back to `unknown`. Targeted QE routing/schema tests and the simulator ELS scenario smoke test passed.
76. COMPLETE/UPDATED: carry Boss Waves selected-candidate diagnostic lane waves into the matrix payload and Streamlit frame. Matrix rows and wide rows now expose the best candidate's hit-by-hit, contact-envelope, pre-contact boss-kill, and GC pre-contact waves without changing selected-lane semantics. The regenerated T14 Utility Dissonant row now visibly distinguishes selected/reference `4402`, hit-by-hit raw `3699`, and contact-envelope `4419`, making the IDS/PB mismatch source inspectable without silently switching the model.
77. COMPLETE/UPDATED: close the remaining generated `unknown` value-type metadata gap for Boss Waves-relevant runtime surfaces. The KB/QE bounded metadata contract now declares typed units for active UW/bot ownership flags, Energy Shield capability/charges, card runtime effects, Chain Thunder max reduction, shock damage multiplier, and Flame Bot burn-stack lab output. QE resolution publishes those rows as `bool`, `seconds`, `count`, `pct`, or `multiplier` instead of `unknown`; regenerated `out/run_stats.json`, `out/run_stats_query_rows_start_of_run.json`, and `out/run_stats_query_rows_max_progression.json` now report zero `value_type: unknown` rows.
78. COMPLETE/UPDATED: resolve start-of-run Rend Armor surfaces instead of letting a blank workshop level block publication. The compiler now materializes unpurchased `Rend Armor Chance` and `Rend Armor Mult` workshop rows as source-backed zero values from the KB workshop summary, and QE resolves `tower_rend_armor_multiplier` through the base-times-post-multipliers family. Regenerated start query rows now publish `state::tower.rend_armor_chance_pct = 0%` and `state::tower.rend_armor_multiplier = x0` for Farming and Tourney; max progression still publishes `36%` and `x0.3672`.
79. COMPLETE/UPDATED: resolve the false `Interest / Wave` partial-status artifact. QE now consumes vault-backed percent contributors in the generic percent resolver before enhancement handling, so the zero vault/base Interest contributor is counted rather than leaving the otherwise-correct `7.16%` value partially resolved. Regenerated start and max query rows publish `state::economy.interest_per_wave_pct = 7.16%` as `resolved` for Farming and Tourney.
80. COMPLETE/UPDATED: carry normalized Boss Waves matrix row certification and explicit pressure-factor hints through artifacts and Streamlit. Matrix rows now publish `model_certification_status` plus a row-level `model_certification` block alongside the legacy `best_*` fields, and reference-backed rows publish `pressure_factor_reference_hint` as an explicit comparison-input-only heuristic derived from raw calculated wave/reference ratio. Current regenerated artifacts report 105 matrix rows still honestly scoped as `partial_boss_contact_model`; 34 rows carry pressure-factor hints, with 18 increase-pressure hints and 16 no-adjustment hints. The hint summary now separately marks 16 uncaveated calibration-quality hints and excludes 18 caveated hints from that calibration subset.
81. COMPLETE/UPDATED: expose missing empirical reference gaps as a separate Boss Waves matrix summary. `build_boss_wave_milestone_matrix(...)`, `run_stats` diagnostics, and the Streamlit model/blocker table now publish `reference_gap_summary`, distinguishing rows blocked because IDS/PB reference data is absent from rows blocked only by source-owned non-boss pressure formulas. Regenerated artifacts report 19 missing-reference-blocked rows: 18 `ids_dissonant_pb_wave` gaps and 1 `ids_milestone_wave` gap.
82. COMPLETE/UPDATED: render the exact missing-reference rows in Streamlit's Boss Waves matrix surface. The all-tier preset matrix now displays an app-published `reference_gap_summary.missing_references` frame with tier, run type, reference kind, calculated wave, and unsupported-pressure ids, so high-tier IDS/PB data gaps are visible without opening JSON or recomputing truth in the UI. This is product carry-through only; selected waves, artifacts, and certification semantics are unchanged.
83. COMPLETE/UPDATED: wire source-owned enemy movement speed into Boss Waves boss-contact timing. `simulators.timing.boss_contact_time_seconds(...)` now accepts uncapped positive enemy-speed increase plus boss-speed multiplier inputs, and `app.pipeline` passes those from scenario-owned battle-condition/environment surfaces into the sanctioned contact-time derivation. Diagnostics expose the enemy-speed fraction, boss-speed multiplier, and combined movement-speed multiplier. This closes boss-movement carry-through only; enemy-speed fleet pressure remains listed as unsupported non-boss terminal pressure when active.
84. COMPLETE/UPDATED: carry terminal-pressure closure requirements into Boss Waves missing-reference matrix rows. Matrix candidate rows, selected rows, row-level `model_certification`, top-level matrix certification, `reference_gap_summary.missing_references`, generated outputs, and the Streamlit missing-reference frame now publish the required and missing terminal max-wave runtime inputs for each active unsupported pressure id. This makes high-tier Dissonant disco gaps actionable without inventing source-owned non-boss pressure formulas or changing selected-wave semantics.
85. COMPLETE/UPDATED: carry Boss Waves matrix certification detail into generated `run_stats` diagnostics and fix stale aggregate closure mode. `diagnostics.boss_wave_milestone_matrix` now mirrors the app-published matrix `model_certification`, runtime override closure, terminal-pressure override status, and non-boss pressure closure; selected-row aggregation reports `mode: missing` when unsupported terminal pressures remain active without exact terminal max-wave inputs or pressure-factor approximation, instead of inheriting the matrix-default `not_required` status.
86. COMPLETE/UPDATED: expose uncapped boss-contact estimates for Boss Waves missing-reference rows. `reference_gap_summary.missing_references`, generated `run_stats` diagnostics, and the Streamlit missing-reference frame now carry `unsupported_pressure_uncapped_selected_max_wave` alongside the fail-closed calculated value of zero, so high-tier Dissonant rows blocked by absent IDS/PB references still show the raw boss-contact estimate that needs external reference or explicit pressure closure.
87. COMPLETE/UPDATED: separate effective model closure from explicit runtime override closure in Boss Waves certification. `model_certification.effective_model_closure` now reports whether each certification requirement is satisfied or not applicable, while `runtime_override_closure` remains the narrower explicit-input closure map. Streamlit renders the effective closure rows so normal milestone matrices no longer look like v28 Damage/Health Decay or boss-damage semantics failed when those requirements are not applicable to the selected rows.
88. COMPLETE/UPDATED: mirror Boss Waves matrix certification shortcuts at the matrix artifact root. `out/boss_wave_milestone_matrix.json` now exposes `model_completion_blockers`, `runtime_override_closure`, `effective_model_closure`, `terminal_pressure_runtime_override_status`, and `non_boss_terminal_pressure_closure` beside the existing nested `model_certification`, matching generated `run_stats` diagnostics and reducing consumer ambiguity.
89. COMPLETE/UPDATED: make the explicit Boss Waves pressure-factor approximation close missing-reference blocking consistently. When a caller supplies non-default `boss_wave_pressure_factor`, unsupported terminal-pressure rows now skip empirical reference limiting/blocking just like other explicit closure paths; the default account-truth matrix still exposes source-owned non-boss pressure formulas as a blocker.
90. COMPLETE/UPDATED: carry Boss Waves comparison-overlay certification through diagnostics and Streamlit. Comparison matrices now publish their model certification status, blocker summary, effective closure, non-boss terminal-pressure closure, and reference-gap summary in generated diagnostics and the all-tier matrix product surface, so pressure-factor assumption runs show that the comparison side is closed without replacing the default account-truth matrix.
91. COMPLETE/UPDATED: make Boss Waves pressure-factor hint summaries operator-actionable. The matrix-level `pressure_factor_hint_summary.max_factor_distance_row` now includes tier/run/loadout/selected/calculated context, generated `run_stats` diagnostics mirror it, and Streamlit renders a compact pressure-factor hint summary above the all-tier matrix instead of requiring row-by-row JSON inspection.
92. COMPLETE/UPDATED: break Boss Waves pressure-factor hint summaries down by run type. `pressure_factor_hint_summary.by_run_type` now reports rows, hinted rows, direction counts, worst factor, and worst row for Regular plus each Dissonant category, and Streamlit renders the breakdown for both base and comparison matrices when present.
93. COMPLETE/UPDATED: expose explicit terminal max-wave closures as side-by-side Boss Waves matrix comparisons. CLI and Streamlit can now pass comparison-only fleet/elite/protector/armored/boss terminal max-wave inputs into `build_boss_wave_milestone_matrix(...)`, with labels such as `terminal_pressure_assumptions`; default account-truth rows remain unchanged while the comparison side can close active unsupported terminal pressure through `explicit_terminal_max_wave_inputs`.
94. COMPLETE/UPDATED: make Boss Waves comparison runtime metadata unambiguous. Comparison payloads and generated `run_stats` diagnostics now publish effective merged comparison inputs, explicit `runtime_input_overrides`, and inherited `base_scenario_runtime_inputs` separately, so terminal-closure and pressure-factor experiments show exactly what changed versus default account truth.
95. COMPLETE/UPDATED: surface matrix-level terminal closure requirements in Streamlit. The all-tier Boss Waves model-status frame now renders terminal closure mode, closure state, required terminal max-wave inputs, missing terminal max-wave inputs, and unmapped terminal pressures from the app-published matrix certification metadata.
96. COMPLETE/UPDATED: render Boss Waves terminal-pressure requirements by pressure id in Streamlit. The all-tier matrix now displays the app-published `required_fields_by_pressure`, `missing_fields_by_pressure`, and unmapped-pressure metadata as a dedicated base/comparison frame, so each unsupported Dissonant pressure shows the exact terminal max-wave input needed for explicit closure without creating UI-local formula truth.
97. COMPLETE/UPDATED: distinguish imported zero IDS/PB references from absent references in Boss Waves. Matrix candidates, selected rows, wide rows, `reference_gap_summary.missing_references`, generated `run_stats` diagnostics, and the Streamlit missing-reference frame now carry `reference_raw_wave` plus `reference_gap_reason`; the current 19 blocked reference gaps are all `zero_reference_wave`, proving the high-tier Dissonant PB cells were imported from IDS as zero rather than dropped by the parser.
98. COMPLETE/UPDATED: reduce the bounded `run_stats` output-write hot path without changing artifacts. `RunStatsSession.execute(...)` now writes JSON outputs with explicit UTF-8 encoding and `_stable_run_stats_payload_for_commit(...)` copies only the diagnostics branch needed to strip volatile timing telemetry instead of deep-copying the full stats payload. Warm profile samples dropped from `0.834s / 754,967 calls` to `0.723s / 707,991 calls`, and an optional-matrix temp run matched all committed semantic artifacts byte-for-byte.
99. COMPLETE/UPDATED: reduce redundant intermediate display annotation in the bounded `run_stats` query-statbook path. Per-bundle query statbooks and pre-publication merged statbooks now skip display annotation, while final post-QE-publication statbooks still annotate for emitted artifacts. The local warm profile showed `annotate_display_fields` dropping from **25** calls to **9** calls and about **0.045s** cumulative time; focused and full app contract suites passed, and an optional-matrix semantic comparison found no non-timing artifact mismatches.
100. COMPLETE/UPDATED: carry selected-run Boss Waves terminal-pressure requirements into the Streamlit product surface. The selected Boss Waves `Model assumptions` panel now reuses the same app-owned pressure-to-terminal-input frame as the all-tier matrix, so a focused Dissonant run exposes each unsupported pressure id with its mapped required and missing terminal max-wave input. This is product visibility only; it does not add source-owned non-boss formulas, change selected-wave semantics, or change generated artifacts.
101. COMPLETE/UPDATED: carry selected-run Boss Waves pressure-factor reference hints through payload diagnostics and Streamlit. When unsupported terminal pressure is reference-limited, reference-aligned, or blocked by a non-positive IDS/PB reference, the selected Boss Waves payload now publishes the same comparison-only `pressure_factor_reference_hint` semantics used by matrix rows; Streamlit renders the hint mode, direction, suggested factor, and comparison input in `Model assumptions`. This does not apply a hidden factor, change certification, or close source-owned non-boss formulas.
102. COMPLETE/UPDATED: carry selected-run Boss Waves pressure-factor reference hints for clean IDS/PB references too. Selected Boss Waves runs without unsupported terminal pressures now publish the same comparison-only `pressure_factor_reference_hint` when a positive active IDS milestone or Dissonant PB reference exists, matching matrix-row hint semantics for focused runs. This improves disco/max-wave debugging without changing selected waves, default assumptions, certification, or generated artifacts.
103. COMPLETE/UPDATED: repair the false Cards `Coins Card` visible-stat miss and add a current-scope effect-family evidence gate. The Cards resolved panel now displays the `cards` contributor from QE-owned `state::economy.coins_multiplier` as `state::cards.coins.multiplier`, so Farming shows `x1.45` while Tourney gates off when the Coins card is absent instead of reporting missing wiring. `tests/app/test_stats_dashboard_contract.py` now verifies KB routing/source registries stay closed for bot, card, enhancement, module, relic, and workshop source families; the generated full-pipeline `family_completeness_matrix.json` has zero unmapped rows for active bot, card, enhancement, module, module-substat, relic, and workshop inputs; and start/max query artifacts carry resolved/gated evidence for cards, card mastery, bots, workshop, enhancements, relics, and modules. Relic effect/input routing is closed, while per-account relic numeric values remain external account-instance inputs by design rather than an invented internal relic-instance catalog. This is dashboard/publication carry-through only; it does not create a new card formula or certify the full Boss Waves max-wave model.
104. COMPLETE/UPDATED: qualify Boss Waves matrix reference evidence for Dissonant accuracy debugging. Matrix rows, wide rows, generated diagnostics, and Streamlit now publish `reference_quality` / `reference_quality_summary`, flagging references below the 3000-wave perk-volatility threshold, IDS Dissonant PB references whose source provides no timestamp, and Dissonant PB `5000` values that are lower-bound Disco bonus-cap markers rather than exact PB references. The regenerated matrix reports 76 positive reference rows, 23 exact high-wave calibration candidates, 11 low-wave volatile references, 56 PB-age-unknown Dissonant references, 42 Dissonant PB bonus-cap floor references, and 60 rows with caveats. Capped `5000` PB rows no longer emit pressure-factor calibration hints or clean IDS/PB alignment. This makes old/low/capped PB caveats visible without applying a hidden pressure factor or claiming full max-wave model closure.
105. COMPLETE/UPDATED: close the active generated effect-family routing gaps exposed by the stronger evidence gate. QE now routes `Orb Size +` through the registered `enhancements__tower__orb_size__multiplier` runtime mechanic parameter, and module-substat routing now maps `Land Mine Radius` to `tower_land_mine_radius_m` plus `Inner Land Mines - Damage` to `uw.inner_land_mines.damage_multiplier`. Regenerated full-pipeline artifacts show enhancement `22/22` mapped and module-substat `39/39` mapped; EP compare reports 49 machine-facing destinations with zero true formula mismatches; and the Boss Waves matrix remains honest as `partial_boss_contact_model` with `source_owned_non_boss_terminal_pressure_formulas` still listed as the blocker.
106. COMPLETE/UPDATED: harden registry-to-QE coverage for requested effect families without adding a new runtime owner. `query-routing-mappings.yaml` now explicitly maps all 73 unique names from `kb/modules/tables/module-substats.csv` to existing canonical-stat or mechanic-param contracts, instead of relying on generic alias fallback for module substats. QE smoke tests now verify: all requested closure-ledger routes for workshop/card/enhancement/module/relic/bot_upgrade target existing KB contracts (`48/7/17/42/27/22` routes), all 31 base-card effect registry targets are QE-routable (`29` direct target routes, `1` canonical target route, `1` fallback route), and the Thunder Bot split remains intentional (`Linger` upgrade -> slow percent, `Thunder Bot - Linger Time` lab -> linger duration). Regenerated artifacts still report zero unmapped rows for requested generated families (`bot 20/20`, `card 24/24`, `enhancement 22/22`, `module 17/17`, `module_substat 39/39`, `relic 43/43`, `workshop 48/48`), EP compare remains `17` exact / `9` close / `23` stage-scope with `0` true or unknown formula mismatches, and Boss Waves remains `partial_boss_contact_model` with Dissonant PB `5000` values treated as Disco bonus-cap lower bounds rather than exact calibration targets.
107. COMPLETE/UPDATED: close the card-mastery contract declaration gap behind the generated carry-through evidence. `kb/cards/tables/card-masteries.csv` already owned 31 mastery effect ladders and QE already compiled unlocked/equipped mastery lab rows, but most generic `cards.<card>.mastery_effect` destinations were not declared in `mechanic-params.yaml`. The card mastery surfaces are now declared as mechanic params with units derived from the KB mastery ladder tokens, and QE smoke tests prove all `31/31` card mastery registry rows map to declared mechanic-param IDs with expected unit/resolver metadata. Existing compiler tests still prove all active card masteries compile when unlocked/equipped and gate off when locked or unequipped. Regenerated artifacts keep requested generated-family unmapped counts at zero, EP compare remains `17` exact / `9` close / `23` stage-scope with `0` true or unknown formula mismatches, and Boss Waves remains explicitly partial for non-boss terminal-pressure formulas.
108. COMPLETE/UPDATED: close the module contributor-closure ledger gap behind module unique/main-effect evidence. `contributor-routing-closure.csv` and `contributor-routing-family-summary.csv` now account for all `42/42` module contributor mappings, including the module unique mechanic routes already consumed by QE, and a regression test proves the module closure ledger exactly equals `contributor-mappings-full.yaml` `source_families.module`. The stale generator base coin row now matches the compiler-owned mapping to `coin_bonus_multiplier` rather than the unrelated `coin_kill_multiplier`. This is a KB evidence/contract repair only: the generated active-family matrix remains `module 17/17` for compiled active rows, while the broader module contributor contract ledger is `42/42` closed.
109. COMPLETE/UPDATED: close the relic effect-routing evidence gap without inventing relic instance truth. `relic-input-registry.csv`, `contributor-routing-closure.csv`, and the source-family surface ledger now agree with the QE compiler-consumed `contributor-mappings-full.yaml` relic mappings for all `27/27` relic contributor effects. The stale `relic__tower__coins__pct` registry/closure destination now matches runtime and generated artifacts at `coins_multiplier`, and a QE regression test proves the relic registry and closure ledger exactly match `source_families.relic`. Generated artifacts still show active relic rows `43/43` mapped with zero unmapped rows, and per-account relic numeric values remain explicit account inputs rather than KB-invented instance values.
110. COMPLETE/UPDATED: make requested family closure rows exact against QE compiler mappings. A QE smoke test now proves `contributor-routing-closure.csv` exactly matches `contributor-mappings-full.yaml` for all requested effect families: bot upgrades `22/22`, cards `7/7`, enhancements `17/17`, modules `42/42`, relics `27/27`, and workshop `48/48`. This closed stale economy-route evidence for `card__coins__multiplier`, `enhancements__tower__coin_bonus__multiplier`, and `workshop__tower__coin_kill_bonus__multiplier`, and added the active Bot Bot upgrade rows already declared in mechanic params and consumed by QE. Runtime truth was not moved; the ledger now matches the existing compiler owner.
111. COMPLETE/UPDATED: separate Boss Waves pressure-factor debugging hints from calibration-quality evidence. `pressure_factor_hint_summary` now keeps all comparison-only hints visible while adding a `calibration_quality` subset that includes only reference rows with `calibration_candidate = true` and no reference caveats. The regenerated matrix and run_stats diagnostics report 34 total pressure-factor hints, 16 calibration-quality hints, and 18 caveated hints excluded from calibration quality. All Dissonant Run hints are currently excluded from calibration quality because their PB references carry age/cap/low-wave caveats, while Regular rows provide the current uncaveated calibration-quality factor evidence. This does not apply a hidden factor or certify source-owned non-boss terminal-pressure formulas.
112. COMPLETE/UPDATED: carry requested effect-family route-closure proof into the generated family completeness artifact. `family_completeness_matrix.json` now reports the six requested KB source families as closed (`bot_upgrade`, `card`, `enhancements`, `module`, `relic`, `workshop`) and annotates active bot/card/enhancement/module/module-substat/relic/workshop mapping rows with the matching source-family closure. The generated artifact reports `6/6` closed source families, no open source families, and mapped active rows for bot `20/20`, card `25/25`, enhancement `22/22`, module `17/17`, module-substat `39/39`, relic `43/43`, and workshop `48/48`. This is evidence carry-through only; it does not move runtime ownership or close the Boss Waves source-owned non-boss terminal-pressure blocker.
113. COMPLETE/UPDATED: publish aggregate Boss Waves pressure-factor calibration evidence without applying it as account truth. `pressure_factor_hint_summary.calibration_quality.factor_distribution` now reports count, min, median, mean, max, rounded median/mean, and an explicit comparison-input payload derived only from uncaveated calibration-quality hints. The regenerated matrix reports 16 calibration-quality hints, median pressure factor `2.606384292771721`, mean `2.611575562832565`, range `1.0` to `5.101279317697228`, and comparison input `{"boss_wave_pressure_factor": 2.606384292771721}`. Streamlit renders the median/mean/range/input from the app-published summary. This is an operator calibration hint only: `certification_effect` remains `none_not_applied`, and Boss Waves remains `partial_boss_contact_model` until source-owned non-boss formulas or explicit accepted closure inputs are supplied.
114. COMPLETE/UPDATED: split Boss Waves raw reference alignment from clean calibration alignment. `reference_alignment_summary` still preserves the raw calculated-vs-reference counts, while nested `calibration_reference_alignment` now reports only calibration-candidate references with no caveats and separately counts excluded caveated/non-candidate references. The regenerated matrix reports 76 raw references with 60 raw over-reference rows and 16 matches, but only 16 clean calibration references (14 over, 2 matches) after excluding 60 caveated references from below-3000/PB-age/Disco 5000-cap rules. Streamlit renders those app-published clean/excluded counts without recomputing truth locally. This is Boss Waves reference-evidence honesty only; it does not change selected-wave math, apply a hidden pressure factor, or close `source_owned_non_boss_terminal_pressure_formulas`.
115. COMPLETE/UPDATED: correct EP compare summary semantics for known export defects. `evaluators.compare.build_compare_status_summary(...)` and the core fallback summary now preserve raw `mismatch` status counts while publishing `ep_true_formula_mismatch_count` / `ep_unknown_formula_mismatch_count` as actionable unknown formula mismatches after excluding known EP export defects. Regenerated diagnostics report `ep_raw_formula_mismatch_count = 3`, `ep_known_export_defect_count = 3`, `ep_true_formula_mismatch_count = 0`, and `ep_unknown_formula_mismatch_count = 0`, with the remaining 20 non-aligned rows classified as stage-scope limitations. This is evidence-labeling correction only; it does not hide raw EP export defects or change stat formulas.
116. COMPLETE/UPDATED: tie requested effect-family evidence to statbook and line-verification proof, with card base and card mastery split explicitly. Full-pipeline `diagnostics.current_scope_effect_family_evidence` now joins the requested bot, card base, card mastery, workshop, enhancement, module, and relic families to `statbook_publishable.json` contributors and `line_by_line_verification.json` verdicts, while bounded `run_stats` explicitly marks line verification as not evaluated because full EP compare evidence is full-pipeline-owned. Regenerated diagnostics report line-verification coverage for bot 20 surfaces, card base 21 active runtime surfaces / 22 contributors, card mastery 34 surfaces / 34 contributors (31 declared mastery-effect rows plus 3 active mastery-applied runtime rows), workshop 48, enhancement 22, module/module-substat 41, and relic 42, with zero unmapped statbook contributors and only `pass` / `pass_with_compare_limitations` verdicts. This is evidence carry-through only; Boss Waves remains partial with `source_owned_non_boss_terminal_pressure_formulas` as the blocker for a certified full all-enemy max-wave model.
117. COMPLETE/UPDATED: carry requested effect-family KB route-ledger closure into the same diagnostics proof. `diagnostics.current_scope_effect_family_evidence` now reports per-family KB route count, registered route count, dangling route count, route-ledger status, source-registry status, primary KB surface, and content-gap flag from `family_completeness_matrix.json` / the contributor-routing ledgers. Regenerated diagnostics show bot `22/22`, card base `7/7`, card mastery `7/7`, workshop `48/48`, enhancement `17/17`, module `42/42` including both module main/unique and module-substat route keys, and relic `27/27`, all with zero dangling routes and no content gaps. This is evidence carry-through only; runtime ownership stays in KB/QE and Boss Waves remains partial on source-owned non-boss terminal-pressure formulas.
118. COMPLETE/UPDATED: close requested-family statbook value-type carry-through evidence. QE now treats literal `unknown` metadata as absent in the native kernel and compat stat resolver, so resolved rows fall back to concrete contributor types rather than publishing `unknown`; KB mechanic params now declare the missing Second Wind, Critical Coin, Intro Sprint, and Land Mine Stun card runtime units; account relic metadata counts compile as `count`; and relic support surfaces preserve ratio/multiplier semantics through the publishable statbook. Full-pipeline diagnostics now fail current-scope family evidence on any unknown statbook value type, and regenerated artifacts report zero unknown value types globally and zero unknown value types for every requested family. This is metadata/proof carry-through only; it does not change Boss Waves selected-wave math or close `source_owned_non_boss_terminal_pressure_formulas`.
119. COMPLETE/UPDATED: verify the explicit Boss Waves pressure-factor approximation across the Disco/Dissonant matrix path. The existing `boss_wave_pressure_factor` scenario runtime input remains explicit and omitted by default, but a new focused regression proves that when supplied for the `disco_respec_2026_06_10` T16 matrix case, Regular plus Attack/Defense/Utility/Ultimate Weapon Dissonant categories all complete, carry `closed_with_pressure_factor_approximation`, and remove `source_owned_non_boss_terminal_pressure_formulas` from row and matrix blockers. A temp full-matrix probe with factor `2.606384292771721` likewise completed all 105 rows with top-level `closed_with_pressure_factor_approximation`. Dissonant calibration evidence is still caveated by PB-age, low-wave, and Disco `5000` cap rules, so this is an accepted explicit approximation path, not hidden KB truth or a certified full all-enemy max-wave model.
120. COMPLETE/UPDATED: add KB-backed enemy pressure-driver curves for Boss Waves without promoting a hidden pressure formula. The live wiki elite threshold and fleet spawn schedule rows are now captured in `kb/enemies/tables/wiki-verified-elite-spawn-thresholds.csv` and `kb/enemies/tables/wiki-verified-fleet-spawn-thresholds.csv`, `simulators.scenario` exposes a source-backed non-boss pressure-driver probe for tier/wave/Enemy Balance/Wave Accelerator/fleet/elite pressure diagnostics, and Boss Waves matrix artifacts plus Streamlit model accuracy now report elite/fleet curve coverage as source-backed while keeping the normal enemy spawn-count curve and pressure-to-terminal transform as missing formula links. Regenerated artifacts preserve `partial_missing_required_model_inputs`, exclude Disco `5000` cap rows from calibration, keep sub-3000 rows as caveated sensitivity evidence, and do not apply the clean Regular median pressure factor as default account truth. Focused app/simulator/QE gates passed, full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated 34 files with exit code 0, and full pytest passed `708`.
121. COMPLETE/UPDATED: source the missing normal spawn-rate ramp from the live wiki AdvancedAnalysis image without treating it as exact spawn count or a terminal max-wave formula. `kb/enemies/tables/wiki-advanced-analysis-spawn-rate-wave-thresholds.csv` now captures the Spawn Rate to Wave Count thresholds, `simulators.scenario.normal_spawn_rate_pressure_driver(...)` derives the current spawn-rate band with Wave Accelerator-adjusted threshold waves, and Boss Waves diagnostics now report `normal_spawn_rate_curve_by_wave_and_wave_accelerator = true` while still reporting exact normal enemy spawn-count and pressure-to-terminal transforms as unresolved. Regenerated `out` artifacts preserve `partial_missing_required_model_inputs`, `default_pressure_factor_derived = false`, and Dissonant PB cap caveats. Focused source/app artifact tests passed, app contracts passed `117`, replacement simulator gates passed `67`, full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated 34 files with exit code 0, and full pytest passed `708`.
122. COMPLETE/UPDATED: prepare the farming coins-per-hour phase without delivering an optimiser or hidden CPH formula. Bounded run-stats now publishes Farming max-progression timing/econ prep rows for Intro Sprint active waves, Wave Skip chance, Wave Accelerator cooldown reduction, Wave Accelerator Mastery spawn acceleration, game speed, Max Game Speed perk, scenario target farming wave, effective waves/run, and runs/day. `simulators.timing.farming_econ_timing_readiness_summary(...)` records the user-reported calibration anchor (`T14`, Farming preset, about `5,500` waves, `5.5h`, `210T coins/hour`, implied `1.155q coins/run`) as calibration evidence only, sets the farming objective to `coins_per_hour`, and keeps the CPH model uncertified until run-duration calibration, spawn-rate-to-kill-density, Intro Sprint no-coin window, Wave Skip reward/mastery expected value, and GT/BH/DW/Spotlight/Golden Bot overlap integrals are implemented. Current regenerated artifacts resolve all listed readiness drivers and show the current timing projection at about `7.72h` after game speed versus the observed `5.5h`, making the remaining timing calibration gap explicit. Focused app/simulator/QE gates passed, full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated 34 files with exit code 0, and full pytest passed `709`.
123. COMPLETE/UPDATED: add input-owned Tower Run Tracker export evidence intake without turning personal run history into KB/account truth. `input.run_tracker` now parses compact tracker units and durations, summarizes run counts by type/tier, max-wave evidence, and recency-aware T14 Farming CPH/duration/wave bands. `app.run_stats --run-tracker-csv <path>` and `PipelineRunRequest.run_tracker_csv` thread an optional export path into bounded and full diagnostics as `run_tracker_calibration_evidence`, explicitly marked `external_observation_not_account_truth` and `evidence_available_not_auto_applied`. The user's 2026-06-13 export parsed cleanly with 242 rows from 2025-08-26 through 2026-06-13; latest-10 numeric T14 Farming rows report median CPH about `205.2T`, latest wave `6,518`, latest CPH `240.7T`, and median duration about `6.08h`, supporting the existing 210T/hour calibration anchor without certifying the farming CPH model or applying a hidden Boss Waves pressure factor.
124. COMPLETE/UPDATED: quantify tracker-backed farming timing alignment without tuning the model. Tracker summaries now publish observed waves/hour, seconds/wave, enemies/wave, sparse elite-rate fields, and wave-skip bands. `simulators.timing.farming_econ_timing_readiness_summary(...)` accepts the optional tracker evidence and publishes `tracker_timing_alignment` with the latest T14 Farming recency band, raw projected hours at the tracker median wave, Wave-Skip/Intro-adjusted played-wave projection, and projected/observed duration ratios. The supplied export's latest-10 numeric T14 Farming band has median `5,921.5` waves, `6.0849h`, `205.2T/hour`, `973.17` observed waves/hour, `3.70s` observed real seconds/wave, and `2,334` skipped waves. Against the current timing projection (`30.14s` effective wave duration and `6.25x` diagnostic game speed), raw displayed-wave timing projects about `7.93h` at that median wave (`1.30x` slower than observed), while the current simple Wave-Skip/Intro played-wave adjustment projects about `5.04h` (`0.83x` observed). The committed current-run readiness likewise now reports raw `7.72h` versus skip/intro-adjusted `4.86h` against the 5.5h anchor. This brackets reality and points the next timing calibration at exact Intro Sprint/Wave Skip real-time semantics; it does not mutate wave duration, apply hidden calibration, certify farming CPH, or alter Boss Waves pressure defaults.
125. COMPLETE/UPDATED: wire Wave Skip Mastery into the timing/econ evidence path without applying it as hidden calibration. `state::cards.wave_skip.mastery_effect` is now declared in the timing family contract, consumer bundle, ownership ledger, and dependency invalidation ledger; run-stats requests it alongside base Wave Skip and publishes it in `farming_econ_model_readiness`. Current account evidence resolves the surface as `gated_off` with KB contributor value present but inactive, so the expected skip diagnostic uses base Wave Skip only. The supplied tracker export latest-10 T14 Farming band has median `2,334` skipped waves (`39.4%` of displayed wave), while known active base Wave Skip plus gated-off mastery predicts about `716` skipped waves (`12.1%`). This proves a concrete remaining Wave Skip timing/reward semantics gap rather than hiding it behind a tuned multiplier; no farming CPH certification or Boss Waves pressure default changed.
126. COMPLETE/UPDATED: carry source-backed non-boss pressure-driver samples through the Boss Waves matrix without applying them as a hidden pressure formula. Selected matrix rows and wide rows now expose `non_boss_pressure_driver_probe` from `simulators.scenario`, and `model_accuracy_summary.non_boss_pressure_driver_model.pressure_driver_samples` aggregates positive-wave selected-row samples by run type. The matrix accuracy summary also publishes `pressure_driver_candidate_samples`, proving candidate-level carry-through for loadouts that are not selected. The samples tie raw calculated Boss Waves rows to normal spawn-rate bands, timing-owned Wave Accelerator Mastery spawn acceleration, Enemy Balance Mastery elite pressure, fleet events-per-wave, and pressure-factor hints while preserving `default_pressure_factor_derived = false` and `source_owned_non_boss_terminal_pressure_formulas` as the blocker. `state::cards.enemy_balance.mastery_effect` is declared as a progression-family Boss Waves pressure diagnostic surface and consumer-bundle input; `state::cards.wave_accelerator.spawn_rate_acceleration` remains timing-family-owned and is consumed through `simulators.timing.resolve_timing_consumer_bundle`, not duplicated into progression ownership. Regenerated artifacts show 86 positive-wave selected samples and 341 positive-wave candidate samples. T14 Regular selected GC/Tourney sample has raw calculated wave `9,639`, WA acceleration `1.0`, spawn rate `56`, elite pressure index `66%`, fleet pressure `0.001`, and pressure-factor hint `1.673x` against IDS `5,761`; the T14 Regular `eHP Max Waves` Farming candidate carries WA acceleration `1.8`, raw wave `7,839`, and its own candidate pressure-factor hint `1.361x`.
127. COMPLETE/UPDATED: publish descriptive pressure-driver calibration evidence without fitting or applying a terminal-pressure model. `model_accuracy_summary.non_boss_pressure_driver_model.pressure_driver_empirical_calibration` now joins clean Regular selected-row pressure samples to their raw calculated/reference pressure-factor hints, exposes the candidate driver features (`tier`, `wave`, `reference_wave`, normal spawn pressure, displayed spawn rate, Wave Accelerator spawn acceleration, elite pressure, and fleet events-per-wave), and publishes simple correlations plus the existing 16-row pressure-factor distribution (`median 2.606x`). This block is explicitly `diagnostic_only_not_account_truth`, `default_pressure_factor_derived = false`, and `model_fit_status = not_fitted_terminal_transform_missing`. It gives the next terminal-pressure tranche a concrete design matrix while preserving the blocker until an approved pressure-driver composition rule and pressure-to-terminal transform exist and can be validated against non-capped Dissonant references.

128. COMPLETE/UPDATED: publish an empirical pressure-transform candidate as evidence, not as account truth. `pressure_driver_empirical_calibration.empirical_transform_candidate` now fits a ridge-linear standardized transform over the 16 clean Regular calibration rows using `tier`, `wave`, normal spawn pressure, Wave Accelerator spawn acceleration, and fleet events-per-wave; `elite_pressure_index_pct` is recorded as omitted because it is constant in the clean calibration set. Regenerated artifacts report `status = fitted_in_sample_descriptive_only`, `validation_status = leave_one_out_descriptive_only_not_promoted`, `promotion_status = not_promoted`, in-sample MAE about `0.400x`, and leave-one-out MAE about `0.646x` with max absolute error about `2.762x` on T1. This proves the current driver evidence is not good enough to promote as the default Boss Waves pressure factor and leaves `source_owned_non_boss_terminal_pressure_formulas` as the blocker.

129. COMPLETE/UPDATED: quantify the tracker-implied Wave Skip / Intro Sprint timing gap without applying it as hidden calibration. `farming_econ_model_readiness.tracker_timing_alignment` now reports observed non-Intro displayed waves, tracker-implied played waves after Intro Sprint, observed effective skip multiplier, implied no-mastery skip chance, implied Wave Skip Mastery double chance at the current base Wave Skip chance, observed/expected skip-multiplier ratio, and an explicit gap status. The focused tracker fixture implies `2.060x` non-Intro wave progression and about `73.1%` Wave Skip Mastery double chance at the current `19%` base card chance while the imported mastery surface remains `gated_off`; this preserves the concrete timing/econ blocker rather than tuning run duration, coins/hour, or Boss Waves pressure defaults.

130. COMPLETE/UPDATED: make EP/stat alignment evidence explicit instead of leaving accounted workbook-scope limits under a generic mismatch headline. `ep_compare_summary` now publishes `ep_alignment_status`, clean aligned row count, accounted stage-scope limit count, and unaccounted alignment-gap count from the evaluator compare owner. Regenerated full-pipeline diagnostics report `ep_alignment_status = aligned_except_accounted_stage_scope_limits`, `26` clean aligned EP rows, `23` accounted EP stage-scope limitation rows, `0` unaccounted alignment gaps, and `0` raw/true formula mismatches. This does not hide the EP workbook assumptions; it separates them from actual KB/QE formula alignment evidence for the current stat layer.

131. COMPLETE/UPDATED: surface the EP/stat alignment verdict in the Streamlit Checks tab. The Checks tab now renders an `EP alignment summary` frame from evaluator-owned `ep_compare_summary`, including alignment status, clean aligned rows, accounted EP scope limits, unaccounted alignment gaps, and raw/true formula mismatch counts. The Needs Attention payload also includes `ep_unaccounted_alignment_gaps`. This keeps the new alignment evidence visible to the operator without reinterpreting compare truth in Streamlit.

132. COMPLETE/UPDATED: surface current-scope effect-family carry-through evidence in the Streamlit Checks tab. The Checks tab now renders an `Effect carry-through to Boss Waves` frame from `current_scope_effect_family_evidence`, listing each requested family, carry-through status, Boss Waves selected row count, covered row count, and line-verification status. The Needs Attention payload also includes `effect_carrythrough_incomplete_families`. Current regenerated diagnostics show all seven requested families (`bot`, `card_base`, `card_mastery`, `workshop`, `enhancement`, `module`, `relic`) covered across 105 Boss Waves selected rows with no incomplete families.

133. COMPLETE/UPDATED: surface Boss Waves readiness blockers and caveats in the Streamlit Checks tab. The Checks tab now renders a `Boss Waves readiness summary` frame from `diagnostics.boss_wave_milestone_matrix`, including model closure status, full-model certification flag, model blockers, blocker-row count, unsupported pressure rows, pressure-model status/policy, empirical transform validation/promote status, leave-one-out MAE, sub-3000 reference count, and Dissonance 5000-cap reference count. The Needs Attention payload now includes `boss_wave_model_blockers` and `boss_wave_uncertified`. This does not certify Boss Waves or apply the empirical pressure transform; it makes the remaining `source_owned_non_boss_terminal_pressure_formulas` blocker visible in the operator surface.

134. COMPLETE/UPDATED: carry source-backed fleet related-enemy group pressure into Boss Waves diagnostics without closing terminal pressure. `simulators.scenario.fleet_spawn_pressure_driver` now publishes the wiki-verified related-enemy group range (`10-14`, expected `12`) and expected related-enemy load per wave alongside existing fleet event pressure. The non-boss pressure probe, matrix-selected samples, candidate samples, descriptive calibration rows, model source coverage, monotonic pressure-driver list, generated matrix artifact, and `run_stats` diagnostics mirror now carry `fleet_related_enemy_group_expected_enemies_per_wave_pressure`. The existing blocker remains unchanged because this is driver evidence only; there is still no approved normal/elite/fleet composition rule or pressure-to-terminal transform.

135. COMPLETE/UPDATED: make farming coins-per-hour certification posture explicit in simulator diagnostics and Streamlit Checks. `simulators.timing.farming_econ_timing_readiness_summary(...)` now publishes `coins_per_hour_certification_status = not_certified_missing_formula_links`, `coins_per_hour_optimization_target = true`, and a concrete `coins_per_hour_certification_blockers` list. The Checks tab now renders a `Farming CPH readiness summary` frame from `farming_econ_model_readiness` and includes `farming_cph_uncertified` plus `farming_cph_blockers` in Needs Attention. Bounded `run_stats` and full diagnostics both carry the new certification fields; this keeps the user's farming objective visible without delivering a hidden CPH formula, optimiser, or GT/BH/DW/Spotlight/Golden Bot overlap integral.

136. COMPLETE/UPDATED: publish farming econ sync-window readiness without using it as a certified coin formula. `simulators.timing` now summarizes Golden Tower, Black Hole coin bonus, and Golden Bot duration/cooldown/multiplier windows from existing statbook rows, exposes phase-zero diagnostic overlap fractions and an average combined multiplier diagnostic, and labels Death Wave and Spotlight as multiplier-only until their active-window/exposure models exist. `farming_econ_model_readiness.econ_sync_window_readiness` is present in bounded `run_stats` and full diagnostics, and the Checks tab includes its status, phase model, available-window count, GT/BH overlap fraction, and diagnostic multiplier in the Farming CPH readiness frame. This preserves `gt_bh_dw_spotlight_golden_bot_overlap_coin_integral` as a certification blocker.

137. COMPLETE/UPDATED: publish farming spawn-density readiness from the scenario-owned spawn-rate curve without claiming kill-density truth. `farming_econ_model_readiness.spawn_density_readiness` now consumes the KB-backed `normal_spawn_rate_pressure_driver` at the current farming target wave and Wave Accelerator Mastery acceleration, carrying target wave `5761`, WA acceleration `1.8`, displayed spawn rate `56`, source table provenance, and an explicit `normal_enemy_spawn_count_curve_available = false`. The Checks tab includes spawn density status, displayed spawn rate, WA acceleration, and spawn-count curve availability in the Farming CPH readiness frame. The missing `spawn_rate_to_enemy_kill_density_by_wave` link remains a CPH certification blocker.

138. COMPLETE/UPDATED: carry Tower Run Tracker enemy-density evidence into farming spawn-density readiness when an export is supplied. `spawn_density_readiness.tracker_enemy_density_evidence` now publishes tracker status, latest T14 Farming recency band, observed median total enemies, observed enemies per wave, observed enemies per hour, and displayed-spawn-rate-to-observed-density ratio. Default committed artifacts still report `not_supplied` because no tracker CSV is part of the default run, while the user's 2026-06-13 export reads 242 rows and the latest-10 T14 Farming band reports median enemies/wave about `143.32`, median total enemies `848,402`, and density/spawn-rate ratio about `2.56`. This is external calibration evidence only and does not replace the missing KB normal enemy spawn-count curve or certify CPH.

139. COMPLETE/UPDATED: carry Tower Run Tracker coin-density evidence into farming spawn-density readiness when an export is supplied. `input.run_tracker` now publishes observed coins/enemy and coins/wave recency bands, `simulators.timing` mirrors those bands as `spawn_density_readiness.tracker_coin_density_evidence`, and the Checks tab shows tracker coin-density status plus observed coins/enemy and coins/wave in the Farming CPH readiness frame. Default committed artifacts still report `not_supplied` because no tracker CSV is part of the default run. The user's 2026-06-13 export parsed 242 rows; latest-10 T14 Farming rows report median coins/hour about `205.205T`, median coins/enemy about `1.455B`, and median coins/wave about `206.27B`. This is external calibration evidence only and does not replace a source-owned kill-density or coin integral model.

140. COMPLETE/UPDATED: make tracker-derived farming CPH recency bands first-class calibration evidence without tuning the simulator. `simulators.timing.farming_econ_timing_readiness_summary(...)` now publishes `tracker_cph_calibration_evidence` with latest T14 Farming recency metadata, median coins/run, coins/hour, wave, duration, and ratios to the user-reported 210T/hour calibration anchor. The Checks tab displays tracker CPH band status, tracker median CPH, and tracker/anchor CPH ratio. Default committed artifacts still report `not_supplied`; the user's 2026-06-13 export reports latest-10 T14 Farming median `205.205T/hour`, median `1.21q` coins/run, wave ratio `1.077x`, duration ratio `1.106x`, and CPH ratio `0.977x` against the anchor. This remains calibration evidence only and does not certify farming CPH.

141. COMPLETE/UPDATED: prove the tracker-backed farming CPH identity chain without applying it as a model formula. `input.run_tracker` now publishes `observed_cph_from_density_components`, calculated from `coins_per_enemy * enemies_per_wave * waves_per_hour`, and `simulators.timing` mirrors it as `tracker_cph_identity_evidence`. The Checks tab shows tracker CPH identity status, component CPH, and component/reported CPH ratio. Default committed artifacts still report `not_supplied`; the user's 2026-06-13 export reports latest-10 T14 Farming component CPH about `204.922T/hour` versus tracker median `205.205T/hour`, ratio `0.9986`. This confirms the empirical decomposition needed for farming CPH calibration while preserving the missing source-owned spawn/kill/coin integral blocker.

142. COMPLETE/UPDATED: make the tracker-backed spawn-rate-to-kill-density candidate explicit without promoting it to account truth. `simulators.timing` now publishes `spawn_density_readiness.tracker_kill_density_transform_candidate`, using the calibration-only formula `displayed_spawn_rate * observed_enemies_per_wave_per_displayed_spawn_rate`. The Checks tab shows the candidate status, projected enemies/wave, and enemies/wave per displayed spawn-rate. Default committed artifacts still report `not_supplied`; the user's 2026-06-13 export reports latest-10 T14 Farming displayed spawn rate `56`, observed enemies/wave per displayed spawn-rate `2.5593`, and projected enemies/wave `143.32`. This makes the missing `spawn_rate_to_enemy_kill_density_by_wave` link concrete while preserving the requirement for a source-owned normal spawn-count curve, approved transform, and validation across tiers/run types/spawn phases before certification.

143. COMPLETE/UPDATED: make the tracker-backed kill-density-to-coin-integral candidate explicit without certifying farming CPH. `simulators.timing` now publishes `spawn_density_readiness.tracker_coin_integral_candidate`, combining projected enemies/wave from the tracker kill-density candidate with observed tracker coins/enemy, then multiplying by observed waves/hour for a calibration-only projected CPH. The Checks tab shows coin-integral status, projected coins/wave, projected CPH, and projected/reported CPH ratio. Default committed artifacts still report `not_supplied`; the user's 2026-06-13 export reports latest-10 T14 Farming projected coins/wave about `208.58B`, projected CPH about `202.99T/hour`, tracker median CPH `205.205T/hour`, and projected/reported ratio `0.989x`. This makes the missing `kill_density_to_coins_per_kill_integral` link concrete while preserving econ-window, Wave Skip, and Intro Sprint certification blockers.

144. COMPLETE/UPDATED: make the tracker-backed Intro Sprint / Wave Skip reward partition explicit without certifying reward semantics. `simulators.timing` now publishes `tracker_wave_reward_candidate`, partitioning observed tracker coins/run across coin-eligible displayed waves after Intro Sprint and tracker-implied played waves after skipped waves. The Checks tab shows wave-reward status, coins per non-Intro displayed wave, coins per played wave after Intro Sprint, and effective skip multiplier. Default committed artifacts still report `not_supplied`; the user's 2026-06-13 export reports latest-10 T14 Farming `4,481.5` coin-eligible displayed waves, `2,147.5` tracker-played waves after Intro Sprint, effective skip multiplier `2.087x`, about `270.0B` coins/non-Intro displayed wave, and about `563.45B` coins/tracker-played wave. This keeps `intro_sprint_no_coin_window_to_run_coin_integral` and `wave_skip_reward_and_mastery_expected_value` as explicit blockers while making their calibration target measurable.

145. COMPLETE/UPDATED: carry Tower Run Tracker max-wave references into Boss Waves matrix diagnostics without changing selected waves. `app.pipeline.build_boss_wave_milestone_matrix(...)` now publishes `tracker_reference_evidence` when `run_tracker_csv` is supplied. Farming/Tournament tracker rows match same-tier Regular matrix rows; Dissonance tracker rows remain type-level evidence until a clean tracker-to-Dissonance-category mapping is supplied. The Checks tab shows tracker reference status, total rows, regular matches, and unmapped Dissonance references. Default committed artifacts still report `not_supplied`; the user's 2026-06-13 export parsed 23 type/tier max-wave reference groups, with 19 same-tier Regular matches and 4 unmapped Dissonance references. This adds external max-wave calibration evidence for Boss Waves while preserving `source_owned_non_boss_terminal_pressure_formulas` as the blocker and applying no hidden pressure factor.

146. COMPLETE/UPDATED: surface EP-alignment counts directly in the current-scope effect carry-through Checks table. `_effect_carrythrough_summary_frame(...)` now shows per-family clean EP matches, accounted EP scope-limited rows, and unaccounted EP statuses alongside Boss Waves row coverage and line verification. The current full diagnostics show all requested effect families covered across 105 Boss Waves rows with zero unaccounted EP statuses; clean/scope-limited counts are bot `0/0`, card_base `3/1`, card_mastery `0/0`, workshop `8/4`, enhancement `2/4`, module `6/2`, and relic `5/4`. This is visibility evidence over existing diagnostics only; it does not change KB route truth, EP compare semantics, selected Boss Waves rows, or model blockers.

147. COMPLETE/UPDATED: publish individual effect-route evidence for the current-scope families instead of relying on family totals alone. `current_scope_effect_family_evidence` now reads `kb/ledgers/tables/contributor-routing-closure.csv` and records per-family `individual_route_evidence` with route contributor counts, registered counts, unregistered counts, destination counts, and route examples. The current full diagnostics report `individual_route_evidence_status: closed`, 163 unique source-family routes, 163 registered routes, and 0 unregistered routes. Per-family route counts are bot `22`, card_base `7`, card_mastery `7`, workshop `48`, enhancement `17`, module `42`, and relic `27`; Checks now shows route contributors and unregistered routes beside Boss Waves coverage and EP counts. This is proof-surface hardening only: it does not add mechanics, alter KB truth, or change Boss Waves selected waves.

148. COMPLETE/UPDATED: classify registered effect routes by current statbook visibility. `current_scope_effect_family_evidence` now compares the requested-family route ledger against `statbook_publishable.json` contributor IDs and normalized destination surfaces, then reports exact contributor hits, destination-surface hits, and routes not visible in the current statbook. Current full diagnostics report `statbook_route_visibility_status: partial`: 93 exact contributor hits, 48 destination-surface visible routes, and 29 routes not visible in the current statbook. Incomplete visibility families are bot, card_base, card_mastery, and module; the largest remaining gap is module unique/mechanic-param routes with 24 not-visible routes. Checks now shows exact route hits, destination route hits, and routes not visible per family. This makes the next wiring gaps explicit and does not classify partial visibility as completion.

149. COMPLETE/UPDATED: classify the not-visible route set by evidence-backed applicability. `current_scope_effect_family_evidence` now uses `module_card_payloads.json` plus `kb/modules/contracts/module-unique-runtime-catalog.csv` to split not-visible module routes into active current-preset module-card unique gaps versus inactive registered module uniques, while card capability routes are classified as runtime-effect/combat-exception split routes. Current full diagnostics classify the 29 not-visible family-route entries as 9 active module-card gaps, 15 inactive module unique routes, 4 card capability/runtime split routes, and 1 still-unclassified bot route. Checks now shows active route gaps, inactive route gaps, other classified gaps, and unclassified route gaps per family. This turns the remaining proof work into a concrete queue: active module-card statbook route visibility first, then the Bot Bot maximum-power route.

150. COMPLETE/UPDATED: close selected-preset module-card alias visibility without broadening the Stats statbook across presets. `compile_stat_inputs(...)` now emits additional KB-owned mechanic-param rows for multi-surface module uniques where a single equipped module owns multiple registered route IDs: Sharp Fortitude, Om Chip, Anti-Cube Portal, and Galaxy Compressor. The selected Farming statbook now publishes `state::module.sharp_fortitude.wall_bonus_multiplier` and `state::module.om_chip.boss_reflection_multiplier` alongside their existing unique surfaces. `current_scope_effect_family_evidence` is now selected-preset-aware: regenerated diagnostics show module destination-surface visible routes increased from 18 to 20, module not-visible routes dropped from 24 to 22, and the remaining module not-visible routes split into 15 inactive registered module uniques plus 7 other-preset module routes outside the selected Farming statbook. Checks now separates selected active route gaps from other-preset routes. This is selected-preset statbook wiring/proof only; it does not claim full multi-preset Stats publication or close the remaining Bot Bot maximum-power route.

151. COMPLETE/UPDATED: close the remaining unclassified Bot Bot Maximum Power route through the existing QE compiler owner. `_IDS` reports `Bot + / Maximum Power` as `x1.25`; `compile_stat_inputs(...)` now preserves the existing Bot+ boolean unlock row and also emits the KB-owned numeric route `bot_upgrade__bot_bot__maximum_power__multiplier` to `state::bot.bot_bot.maximum_power_multiplier`. Regenerated `statbook_publishable.json` publishes the surface as `x1.25`, `current_scope_effect_family_evidence` now shows Bot route visibility covered (`21` exact route hits plus `1` destination-surface hit, `0` not-visible Bot routes), and the top-level unclassified route-gap count is gone. Remaining not-visible route entries are now classified as card capability split routes or module inactive/other-preset routes only. This is QE route/statbook visibility repair and does not certify full Boss Waves or farming CPH completion.

152. COMPLETE/UPDATED: close the active Plasma Cannon card capability route and classify the remaining card capability route by selected-preset applicability. `compile_stat_inputs(...)` now emits `state::capability.plasma_cannon.enabled = true` when Plasma Cannon is equipped, alongside the existing `state::cards.plasma_cannon.effect_pct` runtime surface. `current_scope_effect_family_evidence` now uses statbook row status to distinguish active card capability gaps from inactive/gated selected-statbook routes; regenerated diagnostics show card-base and card-mastery destination-surface visible route counts increased from 5 to 6 each, their not-visible route counts dropped from 2 to 1 each, and the remaining card route is `card__death_ray__enabled__boolean` classified as `inactive_card_capability_route_gated_off_current_statbook` because Death Ray is not in the selected Farming card preset and only gated-off related Death Ray mastery evidence is present. Checks now counts this under inactive route gaps instead of a generic card capability split. This is current selected-preset card route/statbook evidence repair only.

153. COMPLETE/UPDATED: split remaining other-preset module route diagnostics by committed query-book evidence instead of treating all seven as selected-statbook misses. `current_scope_effect_family_evidence` now evaluates `run_stats_query_rows_start_of_run.json` and `run_stats_query_rows_max_progression.json` for preset-scoped route destinations, using the QE materializer alias owner where a route is materialized to a support surface. Regenerated full diagnostics keep selected Farming statbook visibility honestly partial (`52` destination hits, `94` exact contributor hits, `24` not visible), while module not-visible routes now classify as `15` inactive module uniques, `4` other-preset routes visible in committed query books, and `3` other-preset routes not yet exposed by committed query books. The Checks table now separates total other-preset routes from the subset with query-book evidence. This is diagnostics/proof-surface hardening only; it does not broaden the selected statbook, invent Milestone query books, or certify farming CPH completion.

154. COMPLETE/UPDATED: close the remaining other-preset module query-book exposure gap without broadening the selected Farming statbook. KB/QE now declares and owns `state::module.being_annihilator.guaranteed_supercrits_after_supercrit_attacks`, the QE materializer maps the raw Being Annihilator mechanic-param route to that canonical state surface, eDamage composites consume the canonical row, and the run_stats progression/Boss Waves primitive request paths include the surface. Anti-Cube `damage_multiplier` and Galaxy Compressor `uw_cooldown_reduction_on_package_s` are now classified through equivalent committed query-book evidence rather than duplicate materialized formula rows. Regenerated full diagnostics still keep selected Farming statbook visibility partial (`52` destination hits, `94` exact contributor hits, `24` not visible), but module not-visible route classification is now only `15` inactive module uniques plus `7` other-preset module routes visible in committed query books, with `0` other-preset module routes missing query-book evidence. This is QE/query-book exposure and Boss Waves carry-through proof only; it does not certify farming CPH completion or final max-wave calibration.

155. COMPLETE/UPDATED: preserve Tower Run Tracker Dissonance reference hints without treating tracker rows as authoritative category truth. `_boss_wave_tracker_reference_evidence(...)` now keeps Dissonance rows external/non-authoritative, but extracts note-derived diagnostic hints such as `Health Disco` -> Defense and `Econ Disco (eHP)` -> Utility, carries the user-reported 5000-wave disco bonus-cap context as reference context only, and compares hinted rows to same-tier/category matrix rows when available. A real-export probe against `runs_2026-06-13T05_31_10.622Z.csv` found 242 rows, recent T14 Farming median CPH `205.205T/h` and mean `207.344T/h`, 19 regular tracker references matched to matrix rows, 2 Dissonance references with diagnostic category hints, and 2 blank-note Dissonance references still unmapped. This improves disco calibration evidence visibility only; it does not tune selected waves, pressure factors, KB truth, or farming CPH certification.

156. COMPLETE/UPDATED: add recency-trend calibration evidence for Tower Run Tracker T14 Farming exports. `input.run_tracker` now publishes `farming_t14_recent_trend`, comparing the latest recency window against the prior same-sized window for CPH, wave, run duration, enemy density, and density-reconstructed CPH. The real `runs_2026-06-13T05_31_10.622Z.csv` export shows recent-10 T14 Farming median CPH `205.205T/h` versus prior-10 median `172.835T/h` (`1.1873x`, up), latest CPH `240.7T/h`, and recent median wave `5921.5` versus prior median `5498` (`+423.5`). This makes the user-observed improving-account trend explicit as calibration evidence only; it does not auto-apply tracker data as account truth or certify the farming CPH model.

157. COMPLETE/UPDATED: expose Tower Run Tracker Wave Skip / Intro Sprint skip-count semantics as an explicit calibration ambiguity instead of treating the raw tracker field as a simulator mismatch. `simulators.timing.farming_econ_timing_readiness_summary(...)` now publishes `tracker_waves_skipped_semantics_candidates` with both interpretations: tracker skips excluding Intro Sprint and tracker skips including Intro Sprint. IDS still shows `Wave Skip Mastery` at `0`, so the QE-gated `state::cards.wave_skip.mastery_effect` route remains correct and unchanged. The real `runs_2026-06-13T05_31_10.622Z.csv` export reports recent T14 Farming median `wavesSkipped = 2334`; treating that as post-Intro skips implies a `2.0868x` effective skip multiplier (`1.7537x` expected), while treating it as Intro-inclusive leaves `894` post-Intro skips and a `1.2492x` effective multiplier (`1.0497x` expected). CPH certification remains blocked until the tracker field semantics plus source-owned Intro Sprint and Wave Skip reward/mastery formulas are resolved; this does not tune run duration, selected waves, pressure factors, KB truth, or farming CPH.

158. COMPLETE/UPDATED: carry source-backed normal/elite/fleet pressure-driver evidence into Farming CPH readiness without certifying the CPH model. `simulators.timing.farming_econ_timing_readiness_summary(...)` now publishes `spawn_density_readiness.non_boss_pressure_driver_evidence`, using the existing `simulators.scenario.non_boss_pressure_driver_probe(...)` and the sanctioned query surface `state::cards.enemy_balance.mastery_effect`. The default Farming artifact now includes Enemy Balance Mastery as `gated_off` from QE/IDS, T14 target wave `5761`, Wave Accelerator Mastery spawn acceleration `1.8`, displayed spawn rate `56`, elite pressure index `66.0`, and fleet events/wave pressure `0.001`, with normal/elite/fleet source curves marked available and normal spawn-count / terminal-pressure transforms still missing. A real tracker-export probe with `runs_2026-06-13T05_31_10.622Z.csv` preserved the same source-backed pressure bundle alongside observed tracker enemies/wave `143.321`. This improves the spawn/pressure evidence path for farming CPH and Boss Waves alignment; it does not derive a pressure factor, tune selected waves, or certify coins/hour.

159. COMPLETE/UPDATED: make the missing normal enemy spawn-count ramp an explicit source-audited blocker instead of an implicit unknown. `spawn_density_readiness.normal_enemy_spawn_count_source_audit` now records the local KB tables checked (`wiki-advanced-analysis-spawn-rate-wave-thresholds`, enemy spawn structure/caps, elite thresholds, and fleet thresholds) and the live wiki sources checked (`AdvancedAnalysis` and `Enemies`). The audit status is `source_not_found_spawn_rate_curve_only`: current sources support displayed spawn-rate thresholds, normal enemy on-screen caps, elite thresholds, and fleet schedules, but not a source-owned normal enemy per-wave spawn-count curve by tier/wave/spawn phase. The Checks tab now surfaces that audit status, and the real tracker-export probe still keeps `totalEnemies` as external calibration evidence (`143.321` observed median enemies/wave) rather than promoting it to KB/account truth. This answers the spawn-count-ramp question and keeps the remaining CPH blocker precise: source-owned spawn-count or an explicitly approved empirical kill-density transform is still required.

160. COMPLETE/UPDATED: add recent/prior empirical kill-density transform stability evidence for Tower Run Tracker exports. `spawn_density_readiness.tracker_kill_density_stability_evidence` now compares recent and prior T14 Farming windows as `observed_enemies_per_wave_median / displayed_spawn_rate`, publishing the candidate transform, drift direction, and promotion blockers without certifying CPH. The default no-tracker artifact reports `not_supplied`; the real `runs_2026-06-13T05_31_10.622Z.csv` export reports recent density/spawn `2.5593` versus prior `2.5704` (`0.9957x`, slightly down) while density-component CPH trends up. This gives the future empirical CPH path a stable, inspectable kill-density candidate while preserving the missing source-owned spawn-count/approved-transform blocker.

161. COMPLETE/UPDATED: add recent/prior tracker coin-yield stability evidence so the CPH calibration path can distinguish kill-density drift from coin-value gains. `input.run_tracker` now includes `observed_coins_per_wave` in T14 Farming trend metrics, and `spawn_density_readiness.tracker_coin_yield_stability_evidence` publishes recent/prior coins-per-enemy and coins-per-wave medians, ratios, directions, and promotion blockers. The default no-tracker artifact reports `not_supplied`; the real `runs_2026-06-13T05_31_10.622Z.csv` export reports coins/enemy up `1.1898x` and coins/wave up `1.1557x`, while density/spawn is stable/slightly down. This supports the user's observed improving-account CPH trend without applying tracker rows as account truth or certifying CPH.

162. COMPLETE/UPDATED: refine the tracker-backed Intro Sprint / Wave Skip reward candidate with source-audited semantics rather than a vague reward blocker. `tracker_wave_reward_candidate.source_audit` now separates source-backed Intro Sprint no-coin suppression, source-backed base Wave Skip reward (`previous wave` coins/cash at `x1.10`), Wave Skip Mastery double-skip chance, and unresolved tracker `wavesSkipped` Intro Sprint semantics. Streamlit Checks surfaces the audit status beside tracker coins/non-Intro displayed wave and tracker coins/played wave after Intro. The real `runs_2026-06-13T05_31_10.622Z.csv` export reports about `270.0B` coins/non-Intro displayed wave and `563.45B` coins/tracker-played wave; the audit status is `base_reward_sources_available_integral_semantics_unresolved`, with Intro Sprint source `source_backed_available`, Wave Skip base reward `source_backed_available_expected_value_missing`, and Wave Skip Mastery `source_backed_available_reward_integral_missing`. This narrows the CPH blocker to expected-value-over-per-wave-coin-curve/mastery/tracker-semantics integration without certifying farming CPH, changing run timing, changing Boss Waves pressure defaults, or treating tracker rows as account truth.

163. COMPLETE/UPDATED: make Tower Run Tracker Dissonance calibration filters explicit beside Boss Waves tracker-reference evidence. `_boss_wave_tracker_reference_evidence(...)` now labels Dissonance tracker rows as excluded cap-floor references, caveated sub-3000 sensitivity references, clean category-hint candidates, or unmapped non-candidates, while keeping all tracker rows external/non-authoritative. Streamlit Checks surfaces tracker-specific Dissonance filter status plus cap, sub-3000, and clean-candidate counts. The real `runs_2026-06-13T05_31_10.622Z.csv` matrix+tracker probe found `23` tracker reference rows, `19` same-tier regular matches, `2` hinted Dissonance rows, and `2` unmapped Dissonance rows; all `4` Dissonance references are at/above the user-reported `5000` Dissonance bonus cap and are now explicitly excluded as lower-bound cap-floor references. This aligns disco calibration evidence with the user's instruction to exclude the cap and treat low-wave references cautiously; it does not apply tracker rows, tune pressure factors, change selected waves, or certify full Boss Waves.

164. COMPLETE/UPDATED: make the current-scope effect-family carry-through exception boundary explicit for Boss Waves evidence. `current_scope_effect_family_evidence` now publishes `statbook_route_visibility_exception_status` and a policy payload explaining when partial selected-statbook visibility is acceptable: hidden routes must be classified as inactive for the selected preset or visible through other-preset query-book evidence, with zero active selected-preset gaps, zero other-preset missing query evidence, and zero unclassified gaps. The regenerated full artifact reports overall `status = covered`, `effect_row_carrythrough_status = covered`, all seven requested families (`bot`, `card_base`, `card_mastery`, `workshop`, `enhancement`, `module`, `relic`) covered, selected-statbook visibility `partial` only for `card_base`, `card_mastery`, and `module`, and `statbook_route_visibility_exception_status = classified_partial_visibility_accepted`. This strengthens the requested evidence that every current-scope family carries through to Boss Waves without treating partial selected-statbook visibility as a hidden defect; it does not alter formulas, selected waves, pressure factors, or the remaining terminal-pressure blocker.

165. COMPLETE/UPDATED: add an explicit active-goal readiness summary to diagnostics and Streamlit Checks. `tower_goal_readiness` now derives a single status from existing owned evidence: current-scope effect-family carry-through, EP export alignment, Boss Waves full-accuracy certification, Dissonance reference policy, and farming CPH readiness. The regenerated full artifact reports `status = not_complete`, `achieved = false`, proven requirements for effect-family carry-through, EP alignment with accounted stage-scope limits, and Dissonance reference policy, plus blockers `boss_waves_full_accuracy` and `farming_cph_objective`. Streamlit Checks now renders this as a Goal readiness summary. This prevents false completion claims while making the remaining work explicit; it does not create a second calculation authority, change formulas, selected waves, pressure factors, or CPH behavior.

166. COMPLETE/UPDATED: expose empirical non-boss terminal-pressure transform promotion readiness instead of leaving the Boss Waves accuracy blocker implicit. `pressure_driver_empirical_calibration.empirical_transform_candidate` now publishes `promotion_readiness`, and `tower_goal_readiness` mirrors that payload under the blocked `boss_waves_full_accuracy` requirement. The current transform remains `not_promoted` / `not_ready`: it has `16` clean regular calibration rows and leave-one-out validation, but the current regenerated artifact reports mean absolute error about `0.646`, worst absolute error about `2.762`, and blocking reasons `not_source_owned_terminal_pressure_formula`, `operator_has_not_approved_empirical_transform_as_default`, `non_capped_dissonance_reference_validation_missing`, and `out_of_sample_validation_beyond_clean_regular_rows_missing`. This moves Boss Waves accuracy closer by making the promotion gate explicit, while preserving the default blocker and not applying a hidden pressure factor.

167. COMPLETE/UPDATED: surface the Boss Waves empirical pressure-transform approval path in Streamlit Checks without promoting it. The Boss Waves readiness summary now renders promotion readiness, promotion blockers, validation basis, MAE, max error, the comparison-only pressure review input, and the operator next step from app-published diagnostics. This makes the human approval gate inspectable in the lightweight certification surface, while default Boss Waves truth remains `not_promoted` / `not_ready` and no pressure factor, simulator formula, selected wave, artifact value, or certification status is changed.

168. COMPLETE/UPDATED: add an explicit farming coins/hour promotion-readiness gate for tracker-backed calibration. `farming_econ_model_readiness` now publishes `coins_per_hour_promotion_readiness`, and `tower_goal_readiness` mirrors it under the blocked `farming_cph_objective` requirement. The gate distinguishes no-tracker, recent-only tracker, and recent-plus-prior tracker validation bases; reports tracker CPH/component/coin-integral statuses and ratios when available; and lists the remaining promotion blockers such as missing source-owned CPH formulas, unapproved empirical defaults, Wave Skip reward expected value, econ-window overlap integral, and multi-export validation. Streamlit Checks renders the same gate. This makes future tracker exports operationally useful without applying them as account truth or certifying farming CPH.

169. COMPLETE/UPDATED: carry tracker calibration-anchor hints into the farming CPH promotion gate. `coins_per_hour_promotion_readiness` now mirrors the tracker export's latest CPH, recent median CPH, prior median CPH, and recent/prior CPH ratio when present, and Streamlit Checks renders those fields beside the promotion status and blocker list. A real probe against `runs_2026-06-13T05_31_10.622Z.csv` parsed 242 tracker rows and classified the CPH promotion basis as `tracker_t14_recent_and_prior_windows`, with latest T14 Farming CPH `240.7T`, recent median `205.205T`, prior median `172.835T`, recent/prior ratio about `1.1873`, component/reported CPH ratio about `0.9986`, and projected/reported CPH ratio about `0.9892`. The probe remains external evidence only; committed artifacts still default to no tracker export supplied and CPH remains uncertified.

170. COMPLETE/UPDATED: add tracker Wave Skip / Intro Sprint skip-count semantics inference without promoting it. `tracker_timing_alignment` now compares the two candidate interpretations of tracker `wavesSkipped` against the KB/QE Wave Skip expectation: tracker skips excluding Intro Sprint versus tracker skips including Intro Sprint. The inference chooses the candidate closest to expected behavior, publishes candidate distances, marks operator confirmation required, and is mirrored into the wave-reward audit plus `coins_per_hour_promotion_readiness`; Streamlit Checks renders the inference status, best candidate, and distance from expected. Supplied tracker fixtures now prove the evidence suggests `tracker_skips_include_intro_sprint` with about `0.0497` distance from expected versus about `0.7311` for the exclude-Intro interpretation. This reduces the CPH ambiguity to an explicit confirm/approve gate while leaving Wave Skip mechanics, reward formulas, account truth, and CPH certification unchanged.

171. COMPLETE/UPDATED: parse and surface Tower Run Tracker Wave Skip reward fields as external CPH evidence. `input.run_tracker` now parses `coinsFromWaveSkip`, `coinsPerWave`, and `mostCoinsFromWaveSkip`, publishes recent/type-tier compact stats for reported Wave Skip coins, reported coins/wave, Wave Skip coin share, and coins per skipped wave, and carries row-level values through tracker payloads. `farming_econ_model_readiness.tracker_wave_reward_candidate` now reports those fields when supplied, while `coins_per_hour_promotion_readiness` mirrors reward-field availability and keeps `tracker_wave_skip_reward_fields_missing` as a blocker when no tracker export is present. A real probe against `runs_2026-06-13T05_31_10.622Z.csv` found recent median `coinsFromWaveSkip ~= 262.735T`, Wave Skip coin share about `0.2309`, and coins/skipped wave about `118.056B`. This makes Wave Skip reward calibration inspectable without certifying the reward integral or applying tracker rows as account truth.

172. COMPLETE/UPDATED: parse and surface Tower Run Tracker econ-source coin splits as external overlap evidence. `input.run_tracker` now parses `coinsFromDeathWave`, `coinsFromGoldenTower`, `coinsFromBlackhole`, `coinsFromSpotlight`, `goldenBotCoinsEarned`, and `coinsFromGoldenCombo`, publishes compact per-source coin and share-of-run summaries, and carries row-level values through tracker payloads. `farming_econ_model_readiness.econ_sync_window_readiness` now mirrors the tracker econ-source evidence, `coins_per_hour_promotion_readiness` reports whether those fields are available, and Streamlit Checks displays source status plus GT/BH/Spotlight/Golden Bot share medians. A real probe against `runs_2026-06-13T05_31_10.622Z.csv` found all six sources present, with recent median shares around GT `0.7141`, BH `0.6852`, Spotlight `0.3675`, Death Wave `0.3789`, Golden Bot `0.6233`, and Golden Combo `0.0`. This makes econ-overlap calibration evidence visible without defining overlap math, correcting double-counting, or certifying CPH.

173. COMPLETE/UPDATED: make Tower Run Tracker econ-source non-additivity explicit before any CPH formula promotion. `input.run_tracker` now publishes compact stats for the row-level sum of tracked econ source coin fields and that sum divided by total run coins, plus an `overlap_evidence_status` classification. `farming_econ_model_readiness.econ_sync_window_readiness`, `coins_per_hour_promotion_readiness`, and Streamlit Checks now carry the same source-sum ratio and overlap status when a tracker export is supplied. The real `runs_2026-06-13T05_31_10.622Z.csv` probe found the latest-10 T14 Farming tracked source sum median at about `2.7644x` total run coins and classified it as `source_splits_overlap_or_double_count`, while committed no-tracker artifacts remain `not_supplied`. This prevents a naive additive GT/BH/DW/Spotlight/Golden Bot formula from being treated as valid and keeps `gt_bh_dw_spotlight_golden_bot_overlap_coin_integral` open until source-owned or explicitly approved overlap math exists.

174. COMPLETE/UPDATED: add Tower Run Tracker waves/hour timing consistency evidence to the farming CPH promotion gate. `input.run_tracker` now summarizes tracker-reported `wavesPerHour`, duration-derived observed waves/hour, and their row-level ratio for type/tier, trend, and latest T14 Farming bands. `farming_econ_model_readiness.tracker_timing_alignment` classifies whether tracker-reported waves/hour matches duration-derived timing, and `coins_per_hour_promotion_readiness` plus Streamlit Checks mirror the status, reported waves/hour, ratio, and projected-duration ratios. A real probe against `runs_2026-06-13T05_31_10.622Z.csv` found latest-10 T14 Farming tracker-reported waves/hour median `973.5`, duration-derived median about `973.168`, ratio about `1.0000015`, and status `tracker_reported_waves_per_hour_matches_duration`. This supports the run-duration calibration evidence without certifying the source-owned Intro Sprint / Wave Skip / game-speed duration integral or promoting tracker rows to account truth.

175. COMPLETE/UPDATED: parse and surface Tower Run Tracker `gameTime` as timing calibration evidence for farming CPH. `input.run_tracker` now parses `gameTime`, publishes median tracker game-time hours and game-time/real-duration ratio for type/tier, trend, and latest T14 Farming bands, and carries row-level values in tracker payloads. `farming_econ_model_readiness.tracker_timing_alignment` and `coins_per_hour_promotion_readiness` now mirror `tracker_game_time_ratio_status`, median game-time hours, and game/real duration ratio; Streamlit Checks renders the same rows. The real `runs_2026-06-13T05_31_10.622Z.csv` probe found latest-10 T14 Farming median game time about `30.0908h`, median game/real duration ratio about `4.9562x`, and status `tracker_game_time_ratio_available`. This gives the run-duration/game-speed calibration target a concrete tracker-backed observation while keeping the source-owned duration integral, Wave Skip reward semantics, account truth, and CPH certification open.

176. COMPLETE/UPDATED: add Tower Run Tracker run-total CPH identity evidence for the farming objective conversion. `input.run_tracker` now publishes `observed_cph_from_run_totals` as coins divided by real run duration plus the row-level ratio to tracker-reported `coinsPerHour`, across type/tier, trend, row payload, and latest T14 Farming summaries. `farming_econ_model_readiness.tracker_cph_identity_evidence` now distinguishes run-total CPH reconstruction from density-component reconstruction, and `coins_per_hour_promotion_readiness` plus Streamlit Checks mirror run-total CPH and reported-ratio fields. The real `runs_2026-06-13T05_31_10.622Z.csv` probe found latest-10 T14 Farming run-total CPH median about `204.922T/h`, tracker-reported CPH median `205.205T/h`, median-over-median ratio about `0.99862`, and row-ratio median about `0.99867`. This validates the external `coins/run -> coins/hour` objective conversion while leaving the source-owned coin integral, timing integral, account truth, and CPH certification open.

177. COMPLETE/UPDATED: classify Tower Run Tracker `coinsPerWave` semantics against observed total coins per wave before using it in farming CPH work. `input.run_tracker` now publishes the row-level ratio of tracker-reported `coinsPerWave` to observed `coins / wave` across type/tier, trend, row payload, and latest T14 Farming summaries. `farming_econ_model_readiness.tracker_wave_reward_candidate` and `coins_per_hour_promotion_readiness` now carry the ratio plus a close/divergent semantics status, and Streamlit Checks renders the reported value, ratio, and classification. The real `runs_2026-06-13T05_31_10.622Z.csv` probe found latest-10 T14 Farming tracker `coinsPerWave` median `11.64B`, observed total `coins / wave` median about `206.27B`, ratio about `0.0556`, and status `tracker_reported_coins_per_wave_diverges_from_total_observed`. This prevents the tracker field from being misused as the total coin-per-wave integral while keeping Wave Skip reward semantics, source-owned coin integrals, account truth, and CPH certification open.

178. COMPLETE/UPDATED: surface Tower Run Tracker enemy-composition evidence for non-boss pressure calibration without turning it into a formula. `input.run_tracker` now parses normal enemy columns (`basic`, `fast`, `tank`, `ranged`, `boss`, `protector`) and elite subtype columns (`vampires`, `rays`, `scatters`, `saboteurs`, `commanders`, `overcharges`), publishes count/share summaries in type-tier and latest T14 Farming payloads, and preserves row-level values. `farming_econ_model_readiness.spawn_density_readiness` now carries `tracker_enemy_composition_evidence`, mirrors composition status and protector/elite-share hints into tracker enemy-density evidence, and Streamlit Checks renders composition status plus protector/vampire/ray/scatter shares. The real `runs_2026-06-13T05_31_10.622Z.csv` probe found all six normal and six elite subtype fields available for latest-10 T14 Farming rows, protector share about `0.0010008`, vampire share about `0.002015`, ray share about `0.001999`, scatter share about `0.001939`, and blank `totalElites` aggregate in those recent rows. This gives Boss Waves pressure-factor and farming density work concrete external pressure-shape evidence while preserving the open source-owned terminal-pressure and CPH formula blockers.

179. COMPLETE/UPDATED: normalize Tower Run Tracker enemy-composition pressure-shape evidence to per-wave counts. `input.run_tracker` now publishes `count_per_wave` compact stats for every parsed normal enemy and elite subtype field plus aggregate tracked normal, tracked elite subtype, and `totalElites` per-wave summaries. `farming_econ_model_readiness.spawn_density_readiness.tracker_enemy_density_evidence` mirrors protector count per wave and elite subtype count per wave, and Streamlit Checks renders both rows beside the existing composition shares. The real `runs_2026-06-13T05_31_10.622Z.csv` probe reports protector count per wave about `0.142898`, elite subtype count per wave about `0.861099`, vampires per wave about `0.29159`, rays per wave about `0.282432`, and scatters per wave about `0.281939`. This is external pressure-shape evidence only; it does not define pressure weights, source spawn tables, Boss Waves terminal-pressure math, account truth, or farming CPH.

180. COMPLETE/UPDATED: summarize tracker Dissonance max-wave alignment for Boss Waves calibration review without applying it. `app.pipeline` now publishes `tracker_reference_evidence.dissonance_tracker_alignment_summary`, aggregating category-hinted Dissonance tracker rows by filter status, category hint, selected/calculated deltas, and selected/calculated-to-tracker ratios. Streamlit Checks renders the alignment status, hinted-reference count, median selected delta, and median selected/tracker ratio. The real `runs_2026-06-13T05_31_10.622Z.csv` probe found four Dissonance tracker references, all excluded by the 5000 cap-floor policy, two with note-derived category hints (`utility` and `defense`), median hinted selected delta `-84.5` waves, and median hinted selected/tracker ratio about `0.970626`; the utility hint overpredicts by `1487` waves while the defense hint underpredicts by `1656` waves. This makes the disco mismatch inspectable while preserving the policy that cap-floor Dissonance rows, sub-3000 rows, and note-derived category hints are external review evidence only, not account truth or default pressure factors.

181. COMPLETE/UPDATED: repair the all-perk coverage audit so it proves KB perk effect materialization instead of falsely reporting start-of-run compile gaps. `app.pipeline` now compiles the synthetic `__audit_all_perks__` preset in `max_progression` mode for diagnostics, matching the projected-perk materialization path used for run-state/Boss Waves policy checks. The committed `perk_coverage_audit` now reports `34` perk entities, `45` KB effects, `48` compiled audit rows, and zero compile gaps: `22` canonical stat routes, `22` runtime/mechanic/environment parameter routes, and `1` capability route. `tests/app/test_stats_dashboard_contract.py` now fails if any KB perk effect has `compile_gap`, `unsupported_operation`, or `unbound_target`. This strengthens evidence for full perk/stat wiring while preserving the remaining Boss Waves terminal-pressure and farming CPH blockers.

182. COMPLETE/UPDATED: close the farming CPH objective-conversion link without certifying the unresolved game-mechanic integrals. `simulators.timing.farming_econ_timing_readiness_summary(...)` now publishes `coins_per_hour_objective_identity` as the source-owned identity `coins_per_hour = coins_per_run / run_duration_hours`, owned by `simulators.timing` and scoped to objective conversion only. The missing CPH formula links and certification blockers no longer include `coins_per_run_integral_to_coins_per_hour_objective`; the remaining blockers are the run-duration integral, spawn-rate-to-kill-density transform, Intro Sprint coin window, Wave Skip reward expected value, and econ-window overlap coin integral. `app.pipeline` now also resynchronizes `run_stats.json.diagnostics` with the final full-pipeline diagnostics so `run_stats.json` and `diagnostics.json` carry the same CPH readiness payload. Streamlit Checks renders the identity status and formula, and tests assert the mirror plus blocker-list reduction. This moves farming toward coins/hour optimization while leaving CPH uncertified until the gameplay integrals are source-owned or explicitly approved.

183. COMPLETE/UPDATED: make the farming run-duration projection an explicit readiness surface rather than an implicit CPH blocker. `simulators.timing.farming_econ_timing_readiness_summary(...)` now publishes `run_duration_projection_readiness`, owned by `simulators.timing`, with the current formula `played_non_intro_waves_after_expected_wave_skip * effective_wave_duration_seconds / effective_game_speed_multiplier`, source-driver availability, target wave, Intro Sprint waves, Wave Skip expected multiplier, played-wave estimate, effective wave duration, effective game speed, projected run hours, anchor ratio, and tracker comparison ratio when an export is supplied. Default committed artifacts report source drivers available, projected run hours `4.86405`, anchor run hours `5.5`, projected/anchor ratio about `0.88437`, and no tracker ratio because no tracker CSV is part of the default run. A real `runs_2026-06-13T05_31_10.622Z.csv` probe reports tracker-comparison status available and skip-adjusted projected/observed duration ratio about `0.82906`. This improves duration evidence for farming CPH while preserving the `calibrated_real_run_duration_after_intro_sprint_wave_skip_and_game_speed` blocker until wave-duration, Intro Sprint, Wave Skip timing/reward semantics, and validation across exports/account states are source-owned or explicitly approved.

184. COMPLETE/UPDATED: make the Checks-tab farming CPH readiness frame name the missing source-owned spawn-count ramp directly. `app.streamlit_inspector._farming_econ_readiness_summary_frame(...)` now renders `Spawn count missing source = normal_enemy_spawn_count_curve_by_tier_wave_and_spawn_phase` from the simulator-published `normal_enemy_spawn_count_source_audit`, and `tests/app/test_pipeline_ui_contracts.py::test_streamlit_checks_tab_surfaces_farming_econ_readiness_summary` protects that row. This answers the wiki/source audit question in the product surface while preserving the existing policy that the wiki currently supports displayed spawn-rate thresholds, caps, elite thresholds, and fleet schedules, not a source-owned normal enemy per-wave spawn-count curve.

185. EVIDENCE/UNCHANGED BLOCKER: a temp tracker-backed Farming/T14 probe against `runs_2026-06-13T05_31_10.622Z.csv` confirms Wave Skip reward fields are available, but does not close `wave_skip_reward_and_mastery_expected_value`. The probe reports `tracker_intro_wave_skip_reward_candidate_available`, tracker Wave Skip coin share about `0.23091`, coins per skipped wave about `118.056b`, and skip-count inference `suggests_tracker_skips_include_intro_sprint` with best candidate distance about `0.04975`. The source audit still requires `wave_skip_coin_reward_expected_value_over_per_wave_coin_curve`, `wave_skip_mastery_double_skip_reward_semantics`, `tracker_waves_skipped_intro_sprint_semantics`, and `econ_window_overlap_for_skipped_and_played_waves` before CPH certification.

186. COMPLETE/UPDATED: lift Wave Skip/Intro Sprint reward source evidence into an always-published farming CPH readiness surface. `simulators.timing.farming_econ_timing_readiness_summary(...)` now returns `wave_skip_reward_readiness` even when no tracker CSV is supplied, with owner `simulators.timing`, status `source_reward_semantics_available_expected_value_integral_missing`, the source audit for Intro Sprint coin suppression, Wave Skip base reward, Wave Skip Mastery double-skip reward semantics, tracker skip-count semantics, and the exact remaining promotion gaps. `app.streamlit_inspector._farming_econ_readiness_summary_frame(...)` now renders `Wave Skip reward readiness`, falling back to the old tracker-nested audit for older artifacts. Regenerated default artifacts show Intro Sprint and base Wave Skip reward sources available while preserving `wave_skip_reward_and_mastery_expected_value` as an open CPH blocker because the expected-value reward integral, mastery double-skip reward semantics, tracker skip-count semantics, and econ-window overlap remain unpromoted.

187. COMPLETE/UPDATED: make the farming econ-window overlap blocker an explicit simulator-owned readiness surface. `simulators.timing._farming_econ_sync_window_readiness_summary(...)` now publishes `overlap_integral_readiness` with owner `simulators.timing`, status `source_window_inputs_available_overlap_integral_missing`, phase model `phase_zero_current_helper_only`, the current window mechanics (`golden_tower`, `black_hole_coin`, `golden_bot`), pair-overlap source `simulators.timing.overlap_fraction`, pair overlap fractions, multiplier-only surfaces for Death Wave and Spotlight, and the exact remaining certification gaps. `app.streamlit_inspector._farming_econ_readiness_summary_frame(...)` now renders `Econ overlap readiness` and `Econ overlap remaining gaps`. This makes GT/BH/Golden Bot timing evidence visible by default while preserving `gt_bh_dw_spotlight_golden_bot_overlap_coin_integral` as an open blocker until phase offsets, kill-density-in-window, Death Wave window/kill state, Spotlight exposure, and Wave Skip/econ-window interactions are source-owned.

188. COMPLETE/UPDATED: make the top-level goal readiness row carry compact per-family proof for the user's "every module/card/bot/workshop/enhancement/relic effect" requirement. `_tower_goal_readiness_summary(...)` now adds `family_proof_counts` and `family_proof_summary` to `effect_family_carrythrough_to_boss_waves`, sourced only from `current_scope_effect_family_evidence`. Regenerated diagnostics show 7/7 requested families covered, 163/163 route contributors registered, 0 unregistered routes, 105/105 Boss Waves selected rows covered, and line verification `covered`. The seven family proof rows are `bot`, `card_base`, `card_mastery`, `workshop`, `enhancement`, `module`, and `relic`, each with status `covered`, zero generated unmapped effect rows, 105/105 Boss Waves rows covered, and zero EP-unaccounted rows. `_tower_goal_readiness_frame(...)` now renders this as a compact Proof column (`families 7/7; routes 163/163; boss rows 105/105`) without replacing the detailed Effect carry-through table.

189. COMPLETE/UPDATED: make the Boss Waves non-boss terminal-pressure blocker an explicit readiness surface. `_boss_wave_non_boss_pressure_driver_model_summary(...)` now publishes `terminal_pressure_transform_readiness` with status `source_driver_curves_available_terminal_transform_missing`, five source-owned driver inputs (`enemy_spawn_rate`, `wave_accelerator_mastery_spawn_rate_acceleration`, `elite_spawn_pressure`, `fleet_spawn_pressure`, `tier_and_wave_pressure`), current source-curve coverage, unchanged-default-truth/certification flags, and the exact remaining certification gaps. `_boss_wave_readiness_summary_frame(...)` now renders `Terminal pressure readiness` and `Terminal pressure remaining gaps`. Regenerated artifacts still report `partial_missing_required_model_inputs` and preserve `source_owned_non_boss_terminal_pressure_formulas` as the Boss Waves full-accuracy blocker; no default pressure factor or hidden empirical transform is promoted.

190. COMPLETE/UPDATED: make the Intro Sprint no-coin window its own farming CPH readiness surface instead of leaving it nested only inside Wave Skip reward evidence. `simulators.timing.farming_econ_timing_readiness_summary(...)` now publishes `intro_sprint_coin_window_readiness` with owner `simulators.timing`, status `source_intro_sprint_coin_suppression_available_coin_integral_missing`, source files for the Intro Sprint card/runtime surface, active Intro Sprint waves, target wave, and the diagnostic coin-eligible displayed-wave count after Intro Sprint. The regenerated default artifacts report `1440` Intro Sprint waves, target farming wave `5761`, and `4321` coin-eligible displayed waves after Intro Sprint. `app.streamlit_inspector._farming_econ_readiness_summary_frame(...)` now renders the readiness status, coin-eligible wave count, and remaining certification gaps. This closes visibility for the Intro Sprint coin-window blocker only; `intro_sprint_no_coin_window_to_run_coin_integral` remains an open farming CPH blocker until a source-owned per-wave coin curve, boundary interactions with Wave Skip/rewards, econ-window overlap, and the run coin integral are promoted.

191. COMPLETE/UPDATED: make the spawn-rate-to-kill-density CPH blocker an explicit simulator-owned readiness gate. `simulators.timing._farming_spawn_density_readiness_summary(...)` now publishes `kill_density_transform_readiness` with owner `simulators.timing`, status `source_spawn_rate_available_kill_density_transform_missing`, source input availability, displayed spawn rate, Wave Accelerator spawn acceleration, pressure index, tracker-candidate status, and the exact certification gaps. `app.streamlit_inspector._farming_econ_readiness_summary_frame(...)` now renders the kill-density transform readiness, gaps, and tracker-candidate status. Regenerated default artifacts show the source spawn-rate curve and Wave Accelerator input are available, the normal enemy spawn-count curve is still unavailable, tracker data is not supplied by default, and `spawn_rate_to_enemy_kill_density_by_wave` remains an open farming CPH blocker. This closes visibility only; no tracker-derived empirical transform is promoted and no hidden kill-density formula is applied.

192. COMPLETE/UPDATED: add an explicit, default-off approval path for tracker-derived empirical farming CPH promotion. `app.run_stats` now exposes `--approve-tracker-empirical-farming-cph`, `PipelineRunRequest` and pipeline traces carry `approve_tracker_empirical_farming_cph`, and `simulators.timing.farming_econ_timing_readiness_summary(...)` accepts `approve_tracker_empirical_cph_default`. When set, the simulator removes only `operator_has_not_approved_tracker_empirical_cph_as_default`; it keeps `default_cph_derived = false`, keeps status `not_ready`, and preserves unresolved formula-validation blockers such as source-owned run/coin integrals, Wave Skip reward expected value, econ-window overlap, and multi-export validation. Default regenerated artifacts remain unapproved and still show the operator approval blocker. This makes the "approved formulas" closure path explicit without silently promoting tracker evidence into account truth.

193. COMPLETE/UPDATED: add an explicit, default-off approval path for the Boss Waves empirical non-boss pressure-transform candidate. The scenario runtime input contract now declares `approve_boss_wave_empirical_pressure_transform`, `app.run_stats` exposes `--approve-boss-wave-empirical-pressure-transform`, and `app.pipeline` threads that input into the empirical transform promotion readiness. When set, it removes only `operator_has_not_approved_empirical_transform_as_default`; it keeps `default_pressure_factor_derived = false`, keeps `promotion_status = not_promoted`, and preserves source/validation blockers for source-owned terminal-pressure formulas, non-capped Dissonance validation, and out-of-sample validation. Streamlit Checks now renders `Pressure promotion approval`, and default regenerated artifacts remain `not_approved`. This makes the approved-empirical Boss pressure closure path explicit without applying a hidden default pressure factor or certifying the full max-wave model.

194. COMPLETE/UPDATED: add an explicit, tracker-evidence-gated approval path for the farming spawn-rate-to-kill-density transform. `app.run_stats` now exposes `--approve-tracker-empirical-kill-density-transform`, `PipelineRunRequest` and pipeline traces carry `approve_tracker_empirical_kill_density_transform`, and `simulators.timing` marks `kill_density_transform_readiness.approved_transform_closes_formula_link` only when that approval is present and tracker evidence supplies `tracker_spawn_rate_to_kill_density_candidate_available`. In that approved tracker-backed path, only `spawn_rate_to_enemy_kill_density_by_wave` is removed from `missing_formula_links` and `coins_per_hour_certification_blockers`; default artifacts remain unapproved and still list the blocker. Streamlit Checks now renders kill-density approval and formula-link closure rows. This creates an explicit approved-formula/proxy path for one farming CPH blocker without certifying CPH or promoting tracker evidence by default.

195. COMPLETE/UPDATED: add an explicit, tracker-evidence-gated approval path for the farming run-duration projection. `app.run_stats` now exposes `--approve-tracker-empirical-run-duration-projection`, `PipelineRunRequest` and pipeline traces carry `approve_tracker_empirical_run_duration_projection`, and `simulators.timing` marks `run_duration_projection_readiness.approved_projection_closes_formula_link` only when that approval is present, source timing drivers are available, and tracker evidence supplies a skip-adjusted projected/observed duration ratio. In that approved tracker-backed path, only `calibrated_real_run_duration_after_intro_sprint_wave_skip_and_game_speed` is removed from `missing_formula_links` and `coins_per_hour_certification_blockers`; default artifacts remain unapproved and still list the blocker. Streamlit Checks now renders duration approval and formula-link closure rows. This creates an explicit approved-formula/proxy path for one farming CPH blocker without certifying CPH, changing the duration formula, or promoting tracker evidence by default.

196. COMPLETE/UPDATED: add an explicit, tracker-evidence-gated approval path for the farming Wave Skip reward expected-value blocker. `app.run_stats` now exposes `--approve-tracker-empirical-wave-skip-reward`, `PipelineRunRequest` and pipeline traces carry `approve_tracker_empirical_wave_skip_reward`, and `simulators.timing` marks `wave_skip_reward_readiness.approved_reward_closes_formula_link` only when that approval is present and tracker evidence supplies the Wave Skip reward candidate plus reward fields. In that approved tracker-backed path, only `wave_skip_reward_and_mastery_expected_value` is removed from `missing_formula_links` and `coins_per_hour_certification_blockers`; default artifacts remain unapproved and still list the blocker. Streamlit Checks now renders Wave Skip reward approval and formula-link closure rows. This creates an explicit approved-formula/proxy path for one farming CPH blocker without certifying CPH, resolving tracker skip-count semantics, solving econ-window overlap, or promoting tracker evidence by default.

197. COMPLETE/UPDATED: add an explicit, source-evidence-gated approval path for the farming Intro Sprint no-coin window blocker. `app.run_stats` now exposes `--approve-source-intro-sprint-coin-window`, `PipelineRunRequest` and pipeline traces carry `approve_source_intro_sprint_coin_window`, and `simulators.timing` marks `intro_sprint_coin_window_readiness.approved_window_closes_formula_link` only when that approval is present, the Intro Sprint runtime surface is available, and the coin-eligible post-Intro wave count can be calculated. In that approved source-backed path, only `intro_sprint_no_coin_window_to_run_coin_integral` is removed from `missing_formula_links` and `coins_per_hour_certification_blockers`; default artifacts remain unapproved and still list the blocker. Streamlit Checks now renders Intro Sprint coin-window approval and formula-link closure rows. This creates an explicit approved source path for one farming CPH blocker without certifying CPH, defining per-wave coin curves, solving Wave Skip/econ-window interactions, or promoting a hidden coin formula.

198. COMPLETE/UPDATED: add an explicit, tracker-evidence-gated approval path for the farming econ-window overlap blocker. `app.run_stats` now exposes `--approve-tracker-empirical-econ-window-overlap`, `PipelineRunRequest` and pipeline traces carry `approve_tracker_empirical_econ_window_overlap`, and `simulators.timing` marks `overlap_integral_readiness.approved_overlap_closes_formula_link` only when that approval is present, econ-window inputs are available, and tracker evidence supplies econ-source coin fields. In that approved tracker-backed path, only `gt_bh_dw_spotlight_golden_bot_overlap_coin_integral` is removed from `missing_formula_links` and `coins_per_hour_certification_blockers`; default artifacts remain unapproved and still list the blocker. Streamlit Checks now renders econ overlap approval and formula-link closure rows. This creates an explicit approved empirical path for one farming CPH blocker without certifying CPH, treating overlapping tracker source splits as additive, or promoting a hidden overlap formula by default.

199. COMPLETE/UPDATED: add an explicit, default-off approval path for the Boss Waves matrix-published pressure-factor review input. `app.run_stats` now exposes `--approve-boss-wave-pressure-factor-review-default`, `PipelineRunRequest` and pipeline traces carry `approve_boss_wave_pressure_factor_review_default`, and `app.pipeline.build_boss_wave_milestone_matrix(...)` promotes the existing `model_accuracy_summary.comparison_only_pressure_factor_inputs.boss_wave_pressure_factor` into the existing `boss_wave_pressure_factor` runtime input only when that approval is supplied and no explicit pressure factor was already provided. A real approved temp run with the all-tier matrix promoted `2.606384292771721`, closed `source_owned_non_boss_terminal_pressure_formulas` as `closed_with_pressure_factor_approximation`, and left `certified_full_max_wave_model = false`; regenerated default artifacts remain partial, have no approved review-default annotation, and keep `tower_goal_readiness.status = not_complete`. This creates an operator-approved Boss Waves approximation path without making the comparison hint KB truth, applying a hidden default pressure factor, or certifying the full all-enemy max-wave model.

200. COMPLETE/UPDATED: make `tower_goal_readiness` recognize the explicitly approved Boss Waves pressure-factor closure as satisfying the user's "source-owned or approved non-boss terminal-pressure formulas" branch. `_boss_wave_milestone_matrix_diagnostics_payload(...)` now carries `approved_pressure_factor_review_default`, and `_tower_goal_readiness_summary(...)` marks `boss_waves_full_accuracy` as `proven_with_approved_non_boss_pressure_approximation` only when the approved review-default annotation, accepted pressure-factor closure, non-boss terminal-pressure closure, and zero matrix blockers all agree. A full approved temp pipeline now reports `remaining_blockers = ['farming_cph_objective']`; regenerated default artifacts remain unapproved and still report `remaining_blockers = ['boss_waves_full_accuracy', 'farming_cph_objective']`. This moves goal closure forward without weakening default truth or claiming a certified full all-enemy max-wave model.

201. COMPLETE/UPDATED: add an explicit, tracker-evidence-gated approval path for the farming Wave Skip / Intro Sprint tracker semantics blocker. `app.run_stats` now exposes `--approve-tracker-wave-skip-intro-semantics`, `PipelineRunRequest` and pipeline traces carry `approve_tracker_wave_skip_intro_semantics`, and `simulators.timing.farming_econ_timing_readiness_summary(...)` removes only `tracker_wave_skip_intro_semantics_gap` when the tracker wavesSkipped inference has a close candidate and the approval flag is supplied. The readiness payload records the approved tracker semantics candidate and distance from expected. With the user's real tracker export and all current farming approvals, `missing_formula_links = []` and CPH status advances to `not_certified_pending_empirical_validation`; the remaining blockers are `not_source_owned_run_coin_and_duration_integrals` and `validation_across_multiple_exports_and_account_states_missing`. Default artifacts remain unapproved and still keep the farming CPH objective blocked.

202. COMPLETE/UPDATED: add explicit final approval gates for farming CPH closure. `app.run_stats` now exposes `--approve-tracker-empirical-run-coin-duration-integrals` and `--approve-tracker-current-export-account-state-validation`, `PipelineRunRequest` and pipeline traces carry both approvals, and `simulators.timing.farming_econ_timing_readiness_summary(...)` removes only the integrated run coin-duration blocker and current-export/account-state validation blocker when tracker CPH identity, tracker coin-integral evidence, timing consistency, and game-time ratio evidence are present. With the user's real tracker export plus all explicit Boss Waves and farming approvals, the full pipeline proof at `out/codex_tmp_goal_all_approved/diagnostics.json` reports `tower_goal_readiness.status = complete`, `achieved = true`, `remaining_blockers = []`, Boss Waves `proven_with_approved_non_boss_pressure_approximation`, and farming CPH `certified_tracker_empirical_cph_model`. Regenerated committed default artifacts remain default-off and still report the unapproved blockers; the complete state requires the explicit approval flags and tracker export.

203. COMPLETE/UPDATED: apply the source-owned tournament Heat curves for Enemy Attack Speed and Enemy Speed at each Boss Waves operator row while explicitly deferring Boss Ultimate. `simulators.scenario` exposes the existing KB battle-condition magnitude schedule, `simulators.evaluator_kernel` resolves the active threshold per displayed wave, Enemy Attack Speed shortens the row-local boss hit interval, and Enemy Speed shortens the row-local boss contact time through the existing timing components. Explicit caller-supplied hit/contact overrides remain authoritative. Boss Ultimate is absent from the active schedule and does not multiply boss health; the existing `boss_ultimate_deferred` / `boss_terminal_max_wave` explicit-closure policy is unchanged. Farming timing diagnostics now distinguish an explicitly supplied observed-wave estimate horizon from the statbook target without using a magic default-value sentinel. Current imported IDS/PB/module/lab/bot truth was rebaselined, app-owned diagnostic KB reads were moved behind QE loaders, and the established `input/run_tracker.py` owner was added to the explicit input inventory guard. The rebaseline also closes the bounded run-stats visibility path for the four existing Multiverse Nexus, Om Chip, and Singularity Harness unique-effect surfaces; `seconds_offset` is typed as seconds, raw mechanic-param aliases are materialized by QE, and the required query ownership/dependency contracts declare the surfaces without widening the Boss Waves hot bundle. Regenerated artifacts report zero other-preset missing-query evidence, covered line verification, and proven effect-family carrythrough. Verification passed the touched suites (`196`), current-input/QE suites (`120`), cross-layer suites (`265`), module/contract suites (`60`), the artifact contract (`58`), a full `execute_pipeline` rebuild, and full pytest (`745`).
204. COMPLETE/UPDATED: execute one bounded P5 QE publication tranche against the current July snapshot. Profiling found `qe.query_derived_composites._ep_sync(...)` performing the same exact periodic overlap calculation 12 times per bounded refresh for only four distinct input tuples; a bounded `lru_cache(maxsize=128)` now reuses the pure result without changing the formula, arguments, accumulation order on cache misses, or emitted values. The eight-run probe recorded `92` hits and `4` misses. A controlled warm-session A/B improved median execution from `2097.19ms` to `859.92ms`; direct current cold CLI samples were `2747.12ms`, `2979.34ms`, and `2912.18ms`. All non-diagnostic artifacts remained byte-for-byte identical, and diagnostics matched after timing telemetry was removed. Focused contracts passed (`152`), the explicit max-progression `app.run_stats` gate passed, and full pytest passed (`746`). P5 remains open because the current cold and warm paths are still above the v49 targets; the warmed profile now exposes farming sync-window timing, QE resolution, and final publication as the next measured costs rather than repeated EP sync enumeration.

### Current blockers inside the active program

1. Boss Waves table, summary, CSV/export, and active diagnostics are now replacement-owned by default through `app.pipeline`.
2. Boss Waves product payloads no longer expose legacy rollback/shadow/reference behavior; legacy Boss Waves comparison support has been retired with the deleted executor.
3. Boss Waves replacement stat evolution is repaired for the live product path: exact start-of-run tower HP / wall-health ratio contributors / wall regen percent-points / tower regen / fortification / defense / thorns / Plasma Cannon primitives are QE-sourced as inputs only, final operator-row Wall Pool / Wall Regen are transformed staged outputs, row-facing wall HP / wall regen are re-derived from Table 1 workshop/perk state where owned primitives exist, fortification is applied exactly once for Wall Pool, wall HP is derived as tower HP times wall-health ratio times non-fort wall-health multipliers, wall regen is derived as tower regen times wall regen percent-points divided by 100, and Death Wave is enemy-health-only.
4. Boss Waves-local wall-surface semantics are closed for the product path: `state::wall.hp` and `state::wall.regen` remain QE primitive surfaces, not final displayed product fields; the final displayed fields are `operator_rows.wall_hp`, `operator_rows.wall_pool_hp_used`, and `operator_rows.wall_regen`, with diagnostics and the field map carrying the primitive/display contract.
5. `simulators/run_executor.py` is removed. Final inventory found no active product or active non-product consumer; remaining mentions are Bible/reconciliation historical authority text, root governance, boundary-test guard strings, or replacement certification notes.
6. The repaired Stats contract must remain workshop-led: no reintroduction of Canonical Overview as a primary panel and no resolved-section-heavy default workflow.
7. Delta fallback remains guarded by live-path tests and is not a sanctioned default; do not weaken that gate while resuming performance work.
8. Boss Waves product perk behavior is scenario-guarded, not scenario-forced: tournament mode has perks disabled, while non-tournament modes honor request-level `runtime_timeline`, `max_progression_policy`, `none`, and `perk_state=off` controls as actual simulator run-state controls.
9. `boss_time_to_contact_seconds` is an optional manual override. Boss Waves derives the default contact time from a Geometry Engine displayed wall-travel proxy base, then applies QE-owned Chrono Field slow, Energy Net duration, and Slow Aura enemy-speed reduction in `simulators.timing`.
10. Black Hole and Chrono Field effective-state publication/Stats parity is closed for the current account path: Boss Waves and Stats consume the same effective BH/CF surfaces rather than divergent raw/support layers.
11. Named perk policy is now input-owned: the four active presets are `eHP Max Waves`, `eHP Farming`, `GC Max Waves`, and `GC Farming`; policy priority does not fabricate unoffered perks, so only First Perk Choice forces PWR into the first offer while subsequent priorities apply to offered choices.
12. Current worktree full-suite recertification is green after the EP boss-exposure replacement and Slow Aura Mastery repair on 2026-05-29: `.venv\Scripts\python.exe -m pytest -q` passed 529 tests. Continue to distinguish this from a claim that every EP comparison is semantically perfect; it means the repo contract tests now match current KB/IDS/QE implementation reality.
13. Current EP compare publication has zero true formula mismatches across the current 49-destination machine-facing EP export compare: 17 exact matches, 9 close matches, and 23 stage-scope rows documenting EP shortcuts or missing user-specific export inputs rather than simulator formula failures. EP `health_raw` still compares against the QE-published raw-health helper before the Armor-slot module health multiplier, while the existing post-armor health pool remains available for wall/eHP consumers. eDamage helper locals for Smart Missiles heatup, Poison Swamp Death Creep, ILM Charged Mines, and Chrono Field+ map to QE-published helper surfaces; locked or unowned UW helper deltas remain stage-scope rather than hidden calibration.
14. Current max-wave workbook comparison state: Regular T14 eHP was close before the lock-state repair (`sim 7209` vs workbook P50 `7250` with 90-wave sampling), but workbook comparisons that assume `FLAME_DR = 95%` are now explicitly scenario-override comparisons: current `_IDS` marks Flame Bot locked, so QE publishes `state::bot.flame.owned = false` and all Flame Bot runtime tracks as zero under account truth, including lab-reduced cooldown surfaces that would otherwise add a negative cooldown to the locked bot track.
15. Dissonant Run masks are now implemented in the replacement Boss Waves product path. T14 workbook sanity can still be run with explicit contact/flame assumptions, but the product default derives boss contact time. Attack Dissonance locks offensive workshop primitives (`Damage = 1`, `Attack Speed = 1`, `Range = 30m`, crit/projectile/DPM/rend disabled) and routes the QE restricted eDamage branch; Defense Dissonance locks tower health/regen, disables wall/thorns/orb/electron boss survival including the separate Sharp Fortitude wall-thorns per-hit increment, and publishes a QE support flag that neutralizes Shockwave/Anti-Cube Portal eDamage leakage. eHP/GC/farming labels no longer select different boss physics: all selected rows use the unified hit-by-hit lane after loadout and Dissonant Run stat masks are applied. Regular non-Defense GC default now uses the QE `derived::edamage_ep` boss-exposure replacement model; explicit caller-supplied eDamage bridge inputs remain available as overrides, not hidden workbook calibration. Tier-scoped stat audits can now be run through `python -m app.run_stats --tier <n>` so Dissonance PB checks do not silently use the account Farming Tier.
16. Remaining work for full max-wave completion is tracked in `BURNDOWN.yaml:remaining_max_wave_completion`. The current milestone matrix is still explicitly scoped as boss-contact survivability, and separate source-owned non-boss terminal-pressure formulas remain out of current scope; Boss Waves is the sanctioned proxy/test surface for stacked enemy pressure for now. Payloads and matrix rows publish a normalized `model_certification` status/blocker set so partial outputs cannot appear fully certified, plus a `runtime_override_closure` map and requirement-applicability map. Unsupported terminal-pressure rows now keep `source_owned_non_boss_terminal_pressure_formulas` visible as a blocker unless explicit terminal max-wave overrides close the active pressure gap or a non-default caller-supplied `boss_wave_pressure_factor` closes it as an explicit approximation. Selected max wave is empirically aligned to a positive active milestone/Dissonant PB reference when the boss-only estimate over- or under-shoots while retaining the uncapped boss-only value for diagnostics; when IDS/PB has no positive reference, rows now preserve the imported raw value and reason, with current high-tier gaps reported as `zero_reference_wave` rather than parser-missing data. Explicit terminal-pressure override closure is pressure-aware: certification/reference alignment requires the runtime max-wave field mapped to each active unsupported pressure id, rather than requiring unrelated fleet/elite/protector/armored inputs for every case; unmapped/deferred pressure ids remain uncertified unless closed by the pressure-factor approximation. `boss_wave_pressure_factor` is now an explicit scenario runtime input for simple pressure analysis; when supplied, it multiplies both Boss Waves incoming boss-hit damage and boss HP through the sanctioned Table 2 transforms, and omitted/default `1.0` leaves account truth unchanged. This factor is an explicit approximation path, not a source-owned non-boss formula. Source-owned enemy-speed movement now carries into boss contact timing through `simulators.timing`, but enemy-speed fleet pressure remains an unsupported terminal-pressure id when active. The Streamlit Boss Waves model-assumptions table now exposes the terminal override mode plus required/missing/unmapped pressure fields, non-boss pressure closure mode, pressure-factor closure state, factor value, and v28 Damage/Health Decay closure details from the sanctioned payload; the matrix exposes the empirical alignment direction when IDS references are used for unsupported pressure, and the base/comparison terminal-pressure frame maps each unsupported pressure id to its required and missing terminal max-wave input. Clean rows now default to IDS milestone/Dissonant PB empirical alignment in the CLI/output, request model, matrix builder, and Streamlit product view, while `best_calculated_selected_max_wave`, raw candidate results, deltas, and `ids_reference_alignment` metadata preserve the simulator estimate; `--boss-wave-compare-clean-reference-rows` and the unchecked Streamlit control keep comparison-only audit mode available. Flame Bot binary DR is now explicit model policy: static boss-path hit chance is published, min/max lanes expose miss/hit bounds, and the selected `avg` lane only assumes the all-or-nothing tag when the modeled chance is near-certain (`>= 95%`) rather than applying probability-weighted partial DR. `MW-6`, `MW-5`, and `MW-7` are closed target-by-target for milestone/PB references, Dissonant Run mask certification, and four-policy perk/loadout acceptance. `MW-3` is closed by deriving omitted boss contact time from the Geometry Engine displayed wall-travel proxy plus QE-owned CF/EN/Slow Aura/enemy-speed effects while still labeling caller-supplied overrides. `MW-2` is closed for normal boss-contact matrices and remains explicit-input-only for tournament Damage/Health Decay because public sources do not provide magnitudes; the explicit-input closure now lists required and missing fields rather than hiding behind a generic blocker. `MW-1` is formula-deferred but product-honest through explicit overrides, explicit pressure-factor approximation closure, and empirical reference cap/alignment. `MW-4` is updated/closed for selected default matrix rows by using unified hit-by-hit semantics across eHP/GC/farming loadouts and by using the QE `derived::edamage_ep` boss-exposure replacement model for GC damage; caller-owned bridge inputs remain optional overrides, not hidden workbook calibration. `MW-8` is closed for CLI/output/full-pytest recertification in this latest boss-damage closure; in-app Browser screenshot evidence remains optional and dependent on Browser plugin availability.
17. Geometry Engine is now an active simulator-owned support surface, but only for displayed/proxy geometry. It may feed timing's boss contact-time base as a range-sensitive displayed proxy, but it must not be consumed as measured/effective boss contact-time truth, wall contact truth, exact capture/tag fraction, exact density-weighted coin truth, non-boss terminal-pressure truth, or generic all-bot geometry until those surfaces are separately sourced, promoted, and tested.
18. Tournament Boss Waves rows consume the KB-owned Enemy Attack Speed and Enemy Speed Heat schedules dynamically: attack speed changes boss hit cadence and movement speed changes the existing boss contact-time calculation unless a caller supplied the corresponding explicit runtime override. Boss Ultimate remains formula-deferred, is not applied as boss overheal/health in this tranche, and retains only the existing explicit `boss_terminal_max_wave` closure path.

### Boss Waves cutover plan

This plan is historical governance for staged Boss Waves cutover. Phase 2A table/summary and Phase 2B CSV/export/diagnostics cutover are implemented, the subsequent excision tranche removed Boss Waves legacy rollback/shadow branches from product payload behavior, and the final legacy executor retirement tranche deleted `simulators/run_executor.py`. It does not authorize UI-local truth or compatibility shims.

#### Cutover scope map

| Seam / component | Current live source | Future replacement source | Cutover rule |
|---|---|---|---|
| Operator table source | replacement-owned `app.pipeline.build_boss_wave_payload(...)` | staged Table 2 rows from `simulators.evaluator_kernel.build_scenario_overlay_table(...)`, rendered through operator-row handles | switched in Phase 2A; product path is replacement-only |
| Summary source | replacement-owned `summary` from explicit `avg` summary lane and replacement overlay rows | replacement summary derived from explicit `avg` summary lane and replacement overlay rows | switched in Phase 2A; product path is replacement-only |
| CSV/export source | replacement-owned `download_rows` from field-map-governed export columns | replacement export rows plus field-map-governed export columns | switched in Phase 2B; product path is replacement-only |
| Diagnostics source | replacement-owned diagnostics covering source selection, replacement model, v21 TTK model, lane policy, and replacement outputs | replacement diagnostics covering source selection, replacement model, v21 TTK model, lane policy, and replacement outputs | switched in Phase 2B; product path is replacement-only |
| Fixture/testing ownership | app pipeline contract tests, staged `tests/simulators/test_evaluator_kernel.py`, and replacement certification tests | staged-kernel tests plus explicit app-level cutover and replacement certification tests | legacy executor defender tests removed |
| Legacy comparison / shadow mode | removed with `simulators/run_executor.py` | replacement certification harnesses validate product-facing fields through the field map and kernel contracts | not exposed by Boss Waves product payload |
| Rollback switch / fallback path | none in Boss Waves product payload after legacy excision | rollback would require a new approved tranche, not an in-product branch | removed from product behavior |

#### Required Boss Waves field map artifact

Before any actual cutover tranche, create a field map that lists every product-facing Boss Waves field and explicitly records:

- legacy field/source
- replacement field/source
- status: equivalent, transformed, intentional semantic difference, not directly comparable, or not exposed
- product surface: table, summary, export, diagnostics
- normalization/tolerance policy for comparable fields
- owner of the transformation, if transformed
- rollback impact if the field is missing or malformed

The field map must cover at minimum: `display_wave`, effective attack wave, effective health wave, enemy attack, enemy health, final wall HP, final wall regen, boss TTK, survival margin, survives/fail state, first failed wave, max surviving wave, lane policy, and all fields currently used by CSV/export and diagnostics.

#### Phased cutover sequence

1. **Phase 0 - Planning freeze**
   - Current planning state only.
   - No product switch.
   - No legacy removal.
   - Record semantic differences, rollback plan, and required field map.

2. **Phase 1 - Shadow-ready cutover definition**
   - Keep the live product on the legacy path.
   - Produce the required Boss Waves field map artifact.
   - Keep the bounded parity matrix green.
   - Define the app-level source-selection and rollback seam without switching default behavior.

3. **Phase 2A - Operator table and summary source switch**
   - Switch only the visible operator table and summary source to the staged replacement path.
   - Keep CSV/export and diagnostics sourced from the existing legacy-compatible product contract until Phase 2B.
   - Legacy rollback/shadow was available only during the cutover tranche and has since been removed from product behavior.
   - Do not remove legacy code.

4. **Phase 2B - CSV/export and diagnostics source switch**
   - Complete.
   - CSV/export fields switch through the Phase 2B field map.
   - Diagnostics switch to replacement-owned diagnostics.

5. **Phase 3 - Stabilization and monitoring**
   - Run replacement primary and test/reference parity where feasible.
   - Keep parity matrix and app-level product tests green.
   - Track intentional semantic differences, especially v21 event-only TTK versus legacy continuous-DPS proxy behavior.

6. **Phase 4 - Legacy demotion/removal planning**
   - Only after 2A/2B/3 prove stable.
   - Decide whether legacy becomes reference-only or is removed in a separate legacy-disposition tranche.
   - Removal requires proof that no active consumer needs legacy row-engine outputs.

#### Acceptance gates by phase

| Phase | Required gates |
|---|---|
| Phase 0 | cutover scope map documented; split 2A/2B phases documented; no app cutover; no legacy removal; semantic differences and blockers listed |
| Phase 1 | field map exists and covers table/summary/export/diagnostics; parity matrix remains green; `pytest -q` passes; no replacement bugs in divergence ledger; rollback seam design reviewed; non-comparable fields explicitly handled |
| Phase 2A | operator table source uses replacement; summary source uses replacement; CSV/export and diagnostics are not switched except mapped pass-through; app-level tests prove table/summary use replacement; parity matrix remains green; `pytest -q` passes; v21 TTK and `avg/min/max` lane policy preserved |
| Phase 2B | export field map implemented; diagnostics field map implemented; app-level export/diagnostics tests pass; diagnostics expose active replacement source; parity matrix remains green; `pytest -q` passes |
| Phase 3 | replacement primary remains covered by product tests and test/reference parity where useful; no unresolved replacement bugs; intentional semantic differences remain visible |
| Phase 4 | no active consumer depends on legacy Boss Waves row-engine outputs; governance and tests identify legacy as removable/reference-only; removal/demotion tests pass; `pytest -q` passes |

#### Operational rollback strategy

The in-product Boss Waves legacy rollback seam was retired by the legacy-excision tranche, and `simulators/run_executor.py` has since been deleted. Any future rollback would require a new approved implementation path, not a silent product fallback. Current failure triggers are handled as replacement-path bugs or semantic blockers.

#### Legacy disposition plan

- Legacy Boss Waves runtime code has been removed from the active repository.
- Boss Waves product behavior is replacement-only; no legacy rollback/shadow/reference branch is exposed by `app.pipeline.build_boss_wave_payload(...)`.
- Legacy comparison support has been retired; replacement certification tests now guard the staged kernel and product payload contract.
- Legacy executor removal required and received:
  - green replacement certification evidence after replacement primary sourcing
  - green app-level product tests for table, summary, export, and diagnostics
  - proof that no active consumer depends on legacy row-engine-only outputs
  - governance sync that no longer describes legacy as the active product source

#### Remaining blockers after legacy executor removal

1. Historical Bible and reconciliation-review references still mention the old executor as prior active reality; they are not active runtime consumers and were not edited in this deletion tranche.
2. Future max-waves work should build from `qe.run_plan`, `simulators.evaluator_kernel`, and remaining simulator owners rather than recreating the deleted row-engine.

### Exit criteria

The v49 current-stage program is complete only when all of the following are true:

- every current-scope canonical user-relevant stat has a visible home in Streamlit
- each visible row follows the sanctioned row contract and authority chain
- missingness and non-recon behavior are explicit rather than silently backfilled
- Streamlit can operate the sanctioned max-wave simulator path with explicit runtime controls
- current-scope simulator tests and touched contract tests pass
- Boss Waves staged kernel exists as compiled RunPlan plus Table 1 common trajectory and Table 2 scenario overlay/v21 combat outputs, with explicit registries, derived wall HP/regen from staged contributors, avg/min/max lanes, explicit avg summary-lane policy, operator-table handles, and no hot-path dependency on the legacy row-engine
- Boss Waves Phase 2A/2B has switched table, summary, CSV/export, and diagnostics through a field-map-backed, replacement-input-independent `app.pipeline` seam, and Boss Waves product behavior is replacement-only after legacy excision
- performance claims for the active product path are evidenced honestly
- stale root governance no longer misstates the active program
- `BURNDOWN.yaml` task statuses match this file

### Verification baseline

- `python -m app.run_stats`
- `pytest -q tests/app/test_input_dashboard_contract.py`
- `pytest -q tests/app/test_stats_dashboard_contract.py`
- `pytest -q tests/app/test_pipeline_functional.py`
- `pytest -q tests/app/test_pipeline_ui_contracts.py`
- `pytest -q tests/simulators/test_evaluator_kernel.py`
- `pytest -q tests/simulators/test_boss_waves_parity_shadow.py`
- targeted pytest selection for touched owner surfaces
- broader `pytest -q` when a phase claims closure
- UI-level Streamlit harness verification of the repaired Stats/Boss Waves surface
- distinguish shipped-baseline truth from rebuilt-worktree truth when they differ
- fresh inspection of:
  - `out/run_stats_query_rows_start_of_run.json`
  - `out/run_stats_query_rows_max_progression.json`
  - `out/stats_dashboard.json`
  - `out/input_dashboard.json`
  - `out/diagnostics.json`
  - `git status --short`

### Current performance evidence

- benchmark baseline command: `python -m app.run_stats`
- repo snapshot: `d04f328`
- machine note: Windows 10 Pro, x64, Intel i7-6700K, 16 GB RAM
- pre-hardening measured wall-clock runs on **2026-04-10**:
  - run 1: **4548.26 ms**
  - run 2: **4132.39 ms**
  - run 3: **4300.90 ms**
- post-hardening measured wall-clock runs on **2026-04-10**:
  - run 1: **3934.13 ms**
  - run 2: **3695.59 ms**
  - run 3: **3912.26 ms**
- post-import-thinning measured wall-clock runs on **2026-04-10**:
  - run 1: **1663.64 ms**
  - run 2: **1758.34 ms**
  - run 3: **1890.40 ms**
- post-surface-id-caching measured wall-clock runs on **2026-04-10**:
  - run 1: **1390.17 ms**
  - run 2: **1446.00 ms**
  - run 3: **1391.22 ms**
- post-bound-input-reuse measured wall-clock runs on **2026-04-10**:
  - run 1: **1135.47 ms**
  - run 2: **1205.17 ms**
  - run 3: **1217.87 ms**
- post-dashboard-artifact measured wall-clock runs on **2026-04-10**:
  - run 1: **1580.19 ms**
  - run 2: **1644.26 ms**
  - run 3: **1477.16 ms**
- post-loader-and-registry-hardening measured wall-clock runs on **2026-04-10**:
  - run 1: **1938.07 ms**
  - run 2: **1563.81 ms**
  - run 3: **1449.09 ms**
- post-loader-and-registry-hardening repeated in-process session runs on **2026-04-10**:
  - run 1: **790.63 ms**
  - run 2: **568.52 ms**
  - run 3: **547.86 ms**
- post-shared-yaml-cache-and-compiler-hardening measured wall-clock runs on **2026-04-10**:
  - run 1: **1421.29 ms**
  - run 2: **1785.38 ms**
  - run 3: **1354.65 ms**
- post-shared-yaml-cache-and-compiler-hardening repeated in-process session runs on **2026-04-10**:
  - run 1: **704.59 ms**
  - run 2: **487.24 ms**
  - run 3: **544.84 ms**
- post-write-path-contract-normalization measured wall-clock runs on **2026-04-10**:
  - run 1: **1011.11 ms**
  - run 2: **1045.87 ms**
  - run 3: **934.96 ms**
- goal-mode P5 hot-path measurements on **2026-06-11** after QE stat serialization, QE publish-cache, and contract-normalizer fast-path work:
  - marker-precheck cold CLI runs: **1351.04 ms**, **1269.25 ms**, **1282.44 ms**
  - marker-precheck warm session runs: **687.09 ms**, **555.53 ms**, **541.13 ms**, **509.58 ms**, **522.08 ms**, **535.60 ms**, **547.88 ms**, **574.31 ms**
  - marker-precheck plus bounded string-cache cold CLI runs: **1767.34 ms**, **1601.49 ms**, **1515.36 ms**
  - marker-precheck plus bounded string-cache warm session runs: **789.87 ms**, **573.64 ms**, **468.73 ms**, **489.00 ms**, **536.59 ms**, **512.50 ms**, **506.86 ms**, **528.70 ms**
  - interpretation: warm-session median improved into roughly the **0.52s** band in the latest local batch, and contract payload normalization profile cost dropped from **0.351s** to **0.218s** after the bounded cache; cold CLI timings remain noisy and P5 remains open rather than complete
- follow-up goal-mode P5 measurement on **2026-06-11** after skipping no-op QE surface-id row reconstruction:
  - no-op replace probe: **2,369 / 2,369** measured surface-id replace calls were same-id publications in one run
  - cold CLI runs: **1246.24 ms**, **1369.84 ms**, **1202.95 ms**
  - warm session runs: **675.23 ms**, **478.33 ms**, **484.39 ms**, **482.85 ms**, **479.24 ms**, **499.61 ms**, **512.72 ms**, **492.15 ms**
  - interpretation: warm-session median measured about **0.49s** in this local batch; P5 remains open because the profile is now dominated by full-trace QE resolution, dashboard publication, contract payload traversal, and derived-composite publication
- follow-up goal-mode P5 measurement on **2026-06-11** after precomputing Dissonance override categories in QE derived-composite publication:
  - cold CLI runs: **1309.39 ms**, **1262.75 ms**, **1173.90 ms**
  - warm session runs: **651.60 ms**, **481.40 ms**, **485.95 ms**, **508.19 ms**, **498.86 ms**, **529.89 ms**, **463.22 ms**, **473.65 ms**
  - interpretation: `publish_derived_composites` profile cost dropped to about **0.065s**, but warm-session median stayed in the same roughly **0.49s** band; P5 remains open
- follow-up goal-mode P5 measurement on **2026-06-11** after preserving clean contract-payload container identity and skipping legacy contract traversal for bounded current-contract `run_stats` artifacts:
  - cold cProfile run: **2.736s** total; `RunStatsSession.execute` **1.852s**; `normalize_contract_payload` no longer appears as a bounded-output publication hotspot
  - warm session runs: **962.67 ms**, **727.76 ms**, **759.26 ms**, **757.35 ms**, **435.19 ms**, **541.02 ms**, **506.15 ms**, **534.57 ms**
  - interpretation: bounded current-contract output writes now avoid the legacy surface-token normalizer and are guarded by a generated-output no-legacy-prefix test; local end-to-end warm timings remain noisy and P5 remains open
- follow-up goal-mode P5 measurement on **2026-06-11** after adding a `RunStatsSession` compiled stat-input cache keyed by account state, preset, state mode, perk context, scenario context, and compiler function identity:
  - warm session runs: **802.35 ms**, **539.32 ms**, **551.15 ms**, **476.40 ms**, **509.63 ms**, **521.67 ms**, **559.91 ms**, **506.36 ms**
  - repeated-session profile: two executions compiled stat inputs **4** times total, matching only the first execution's four preset/state contexts; a warmed single-execution profile no longer listed `compile_stat_inputs` in the top 25 cumulative costs
  - interpretation: the session now follows the cold-compile/hot-resolve direction for stat-input compilation, but end-to-end timings remain in the same noisy roughly **0.5s** warm band and P5 remains open
- follow-up goal-mode P5 measurement on **2026-06-11** after adding bounded pure-helper caches for display formatting and contributor display-type classification:
  - focused verification: `tests/app` passed (**257 passed**) and `python -m app.run_stats --out <temp>` passed
  - warmed second-execute cProfile: **1.127s** total, **1.151s** `RunStatsSession.execute` cumulative, **0.092s** `annotate_display_fields` cumulative, and **1,156,141** function calls
  - comparison context: the preceding warmed profile before this display slice measured about **0.115s** in `annotate_display_fields` and about **1,200,789** function calls; total session time remains noisy because QE/progression costs varied upward in the sample
  - interpretation: display publication now reuses cached pure formatting/classification results for hashable primitive values without changing stat truth or app wiring, but P5 remains open because the broader hot path is still dominated by QE resolution, dashboard publication, JSON, and filesystem writes
- follow-up goal-mode P5 measurement on **2026-06-11** after caching QE publication display-text normalization:
  - focused verification: `tests/qe/test_contracts_models_smoke.py`, `tests/app/test_stats_dashboard_contract.py`, and focused `run_stats` output tests passed (**94 passed**), and `python -m app.run_stats --out <temp>` passed
  - warmed profile comparison: `_normalize_display_text` dropped from about **0.040s** to about **0.002s**, function calls dropped from **1,156,141** to **1,102,652**, and `RunStatsSession.execute` stayed in the same noisy band (**0.939s** before, **0.929s** after)
  - interpretation: QE publication now caches the pure string normalization helper used for repeated display labels/values, preserving publication semantics while removing a small repeated text-normalization hotspot; P5 remains open because end-to-end cost is still dominated by QE resolution, dashboard publication, JSON, and filesystem writes
- follow-up Boss Waves milestone-matrix measurement on **2026-06-11** after caching simulator perk-contribution validation/grouping:
  - preceding cProfile hotspot before this slice: `_validate_perk_contributions` was called **469,640** times and `_perk_contribution_effect_id` about **6.77M** times inside the default matrix run
  - rejected compact-candidate diagnostics experiment measured **83,535.36 ms** and **83,403.41 ms** and was removed
  - kept evaluator-cache matrix run: `app.run_stats --include-boss-wave-milestone-matrix --out <temp>` completed in **68.615s**
  - artifact comparison: temp `boss_wave_milestone_matrix.json` and `run_stats.json` matched committed outputs, so the cache is performance-only for the measured path
  - interpretation: the simulator evaluator now reuses validated perk-contribution summaries across repeated row overlays without changing KB/QE/app ownership or matrix values; this narrows the matrix hot path but does not certify source-owned non-boss terminal-pressure formulas or close the broader v49 performance gate
- rejected QE planner default-kernel reuse candidate on **2026-06-11**:
  - candidate no-matrix CLI probe improved from the local **1.567s** baseline to **1.495s**, but matrix probes regressed to **82.648s** and **77.581s**
  - A/B rollback to the existing planner-owned kernel construction measured **69.464s** for the same matrix command under the same conditions, with semantic artifacts matching committed outputs except timing-only diagnostics
  - decision: candidate removed; do not treat default-kernel reuse in QE family-query builders as a safe P5 shortcut without a new matrix-specific design and proof
- follow-up QE trace-step publication fast path on **2026-06-11**:
  - kept patch: `_surface_step` skips duplicate publication for already-published `requested_surface_ids`, `direct_upstream_node_ids`, and `direct_downstream_node_ids`
  - focused verification: `tests/qe/test_contracts_models_smoke.py`, `tests/app/test_pipeline_functional.py::test_run_stats_canonical_output_filenames`, and `tests/app/test_stats_dashboard_contract.py` passed (**93 passed**)
  - warm profile sample: `RunStatsSession.execute` dropped from **1.238s / 1,102,714 calls** to **0.706s / 808,618 calls**; `kernel.resolve_surfaces` dropped from **0.421s** to **0.146s**
  - matrix verification: `app.run_stats --include-boss-wave-milestone-matrix --out <temp>` completed in **68.058s** and all committed semantic artifacts, including `boss_wave_milestone_matrix.json`, matched byte-for-byte
  - interpretation: this preserves the QE trace contract while removing redundant publication work; P5 remains open
- follow-up Stats dashboard workshop reconciliation reuse on **2026-06-12**:
  - kept patch: workshop reconciliation payloads are built once per preset rather than once per preset/state-mode variant
  - focused verification: dashboard reuse test, panel contract, and `run_stats` filename contract passed; full `tests/app/test_stats_dashboard_contract.py` passed (**54 passed**)
  - warm profile sample after the patch: `RunStatsSession.execute` measured **0.655s / 755,153 calls**, `build_stats_dashboard_payload` measured **0.135s**, and workshop reconciliation row builds dropped to **94**
  - matrix verification: `app.run_stats --include-boss-wave-milestone-matrix --out <temp>` completed in **69.074s** and all committed semantic artifacts matched byte-for-byte
  - interpretation: this removes duplicate QE publication work without changing the Stats dashboard contract; P5 remains open
- follow-up bounded run_stats output-write hot-path cleanup on **2026-06-12**:
  - kept patch: bounded JSON artifact writes now pass `encoding='utf-8'` explicitly, and `_stable_run_stats_payload_for_commit(...)` avoids deep-copying the full stats payload when stripping volatile timing diagnostics
  - focused verification: run_stats output contract/timing tests and main-path smoke passed (**5 passed**); full `tests/app/test_pipeline_functional.py` passed (**124 passed**); full `tests/app/test_stats_dashboard_contract.py` passed (**54 passed**)
  - warm profile sample: preceding warmed profile measured **0.834s / 754,967 calls** with about **0.038s** in Windows `cp1252` encoding and **0.036s** in the stable-payload deep copy; after the patch the warmed profile measured **0.723s / 707,991 calls**, with those hotspots absent from the top profile
  - matrix verification: `app.run_stats --include-boss-wave-milestone-matrix --out <temp>` completed in the local probe and all committed semantic artifacts, including `boss_wave_milestone_matrix.json`, matched byte-for-byte
  - interpretation: this is app-owned output publication cleanup only; P5 remains open because QE resolution, stats dashboard publication, JSON serialization, and filesystem writes still dominate the bounded path
- follow-up intermediate query-statbook display-annotation skip on **2026-06-12**:
  - kept patch: `_query_response_to_statbook_dict(...)` now supports `annotate_display=False`, `build_run_stats_artifacts(...)` uses it for intermediate progression/timing query statbooks, and `_merge_query_statbooks(...)` no longer annotates before final QE publication
  - focused verification: focused output/dashboard contracts passed (**4 passed**); full `tests/app/test_pipeline_functional.py` plus `tests/app/test_stats_dashboard_contract.py` passed (**178 passed**)
  - warm profile sample: `RunStatsSession.execute` measured **0.759s / 649,202 calls** in the local sample, with `annotate_display_fields` reduced to **9** calls and about **0.045s** cumulative time
  - matrix verification: a temp optional-matrix run compared against committed outputs with no semantic mismatches after excluding volatile timing fields; the only raw differences were diagnostics timing telemetry, and the canonical matrix output was regenerated
  - interpretation: this removes redundant app-owned display decoration from non-emitted intermediate query statbooks; P5 remains open because QE resolution, final dashboard publication, JSON serialization, and filesystem writes remain material
- follow-up selected-run terminal-pressure requirement visibility on **2026-06-12**:
  - kept patch: `app.streamlit_inspector` now factors terminal-pressure requirement rendering into a shared helper and renders the selected Boss Waves run's `terminal_pressure_runtime_override_status.required_fields_by_pressure` / `missing_fields_by_pressure` in the `Model assumptions` panel
  - focused verification: selected-run/matrix terminal-pressure UI helper tests and Boss Waves render contract passed (**4 passed**); full `tests/app/test_pipeline_ui_contracts.py` passed (**56 passed**)
  - output verification: no generated artifact rebuild was required because this slice renders existing payload diagnostics without changing pipeline output or simulator semantics
  - interpretation: this closes a focused disco-debugging visibility gap for selected-tier runs while leaving the source-owned non-boss terminal-pressure formula blocker and default matrix certification unchanged
- follow-up selected-run pressure-factor hint carry-through on **2026-06-12**:
  - kept patch: `_apply_unsupported_terminal_pressure_reference_limit(...)` now attaches the existing `raw_calculated_wave_to_reference_ratio_hint` payload to selected-run summaries/diagnostics, and `app.streamlit_inspector` renders the published hint in `Model assumptions`
  - focused verification: selected-run reference cap/alignment/missing-reference tests, high-tier Dissonant payload test, and pressure-hint UI tests passed (**6 passed**); full `tests/app/test_pipeline_functional.py` plus `tests/app/test_pipeline_ui_contracts.py` passed (**181 passed**)
  - payload probe: T16 Ultimate Weapon Dissonant selected wave stayed aligned to IDS/PB `780`, while diagnostics published a comparison-only pressure-factor hint of **1.086** from raw calculated `847` vs reference `780`
  - output verification: optional-matrix temp artifact comparison found no semantic mismatches after excluding volatile timing fields, so no generated artifact rebuild was required
  - interpretation: this makes focused disco runs expose the same pressure-factor analysis already available in the all-tier matrix without changing default assumptions, certification status, selected-wave semantics, or source-owned formula blockers
- follow-up selected-run clean-reference pressure-factor hint carry-through on **2026-06-12**:
  - kept patch: selected Boss Waves summaries now upgrade the default disabled pressure-factor hint when there are no unsupported terminal pressures, no explicit pressure factor, and a positive active IDS/PB reference exists; unsupported-pressure hint behavior remains unchanged
  - focused verification: clean-reference selected payload test plus unsupported-reference cap/alignment/missing-reference and UI hint tests passed (**5 passed**); full `tests/app/test_pipeline_functional.py` plus `tests/app/test_pipeline_ui_contracts.py` passed (**181 passed**)
  - payload probe: T14 Farming selected run reported selected wave `7389`, IDS reference `5761`, and comparison-only pressure-factor hint **1.283** with `increase_pressure`
  - output verification: optional-matrix temp artifact comparison found no semantic mismatches after excluding volatile timing fields, so no generated artifact rebuild was required
  - interpretation: this closes the selected-run side of matrix-equivalent pressure-factor hint visibility for clean IDS/PB references; it does not align selected waves by default or close non-boss formula blockers
- follow-up Boss Waves matrix raw-calculation visibility on **2026-06-12**:
  - kept patch: matrix long rows, wide rows, `ids_reference_alignment`, and Streamlit's all-tier frame now distinguish selected/aligned reference delta from raw calculated delta/ratio
  - focused verification: Boss Waves matrix reference-alignment tests and Streamlit matrix contract tests passed (**8 passed**)
  - output verification: `app.run_stats --include-boss-wave-milestone-matrix` rebuilt generated artifacts in **91.483s** on the local cold sample; T14 Utility Dissonant now shows selected/reference `4402`, raw calculated `3699`, selected delta `0`, raw calculated delta `-703`, and raw ratio `0.840299863698319`
  - interpretation: this prevents empirical IDS/PB alignment from hiding raw calculator mismatch; it does not close source-owned non-boss terminal-pressure formulas or the broader full max-wave caveat
- follow-up Boss Waves matrix alignment-summary visibility on **2026-06-12**:
  - kept patch: `reference_alignment_summary` is emitted by the app-owned matrix payload and rendered by Streamlit without UI-local recomputation
  - focused verification: Boss Waves matrix/reference and Streamlit contract tests passed (**8 passed**)
  - output verification: `app.run_stats --include-boss-wave-milestone-matrix` rebuilt generated artifacts in **68.702s**; generated summary reports 105 rows, 76 reference-backed calculated deltas, 60 IDS/PB-aligned rows, 59 raw overestimates, 1 raw underestimate, 16 raw matches, and worst raw delta 24,910 on Tier 1 Attack Dissonant
  - interpretation: this makes Dissonant/IDS mismatch distribution visible at the matrix level while preserving the current partial boss-contact-model certification
- follow-up Boss Waves comparison pressure-factor overlay on **2026-06-12**:
  - kept patch: CLI and Streamlit can now build a comparison-only pressure-factor overlay while leaving the default matrix at account-truth/runtime-input values
  - focused verification: comparison input helper/label, pressure-factor comparison matrix, Streamlit source contract, and CLI parser smoke passed (**4 passed**)
  - real CLI verification: `app.run_stats --include-boss-wave-milestone-matrix --boss-wave-comparison-pressure-factor 1.25 --out <temp>` completed in **161.105s** and wrote a comparison payload labeled `pressure_factor_assumptions` with `boss_wave_pressure_factor = 1.25`
  - interpretation: this supports explicit difficulty-factor experiments for disco/max-wave mismatch analysis without changing default simulator truth or source-owned formula claims
- follow-up Boss Waves Streamlit comparison overlay rendering on **2026-06-12**:
  - kept patch: Streamlit renders `comparison.wide_rows` from the app-owned all-tier matrix payload as a default-vs-comparison long table
  - focused verification: bridge comparison, pressure-factor comparison, comparison frame, Streamlit source contract, and CLI parser tests passed (**8 passed**)
  - interpretation: comparison overlays now carry through to the product surface without UI-local truth or default matrix behavior changes
- follow-up Boss Waves run_stats diagnostics carry-through on **2026-06-12**:
  - kept patch: both run_stats matrix build paths now use `_boss_wave_milestone_matrix_diagnostics_payload(...)`
  - focused verification: matrix timing/diagnostics, bridge comparison, pressure-factor comparison, Streamlit comparison frame, and CLI parser tests passed (**9 passed**)
  - output verification: `app.run_stats --include-boss-wave-milestone-matrix` regenerated committed artifacts in **67.895s**; `out/run_stats.json` now carries `reference_alignment_summary` with 105 rows, 76 reference-backed calculated deltas, 60 IDS/PB-aligned rows, 59 raw overestimates, 1 raw underestimate, 16 raw matches, and worst raw delta 24,910
  - interpretation: generated run_stats diagnostics now summarize the Boss Waves matrix state without duplicating simulator truth or changing the matrix artifact contract
- follow-up Boss Waves selected-tier Streamlit matrix control on **2026-06-12**:
  - kept patch: Streamlit matrix controls now include `All tiers`; default remains all-tier, while unchecked runs only the selected tier through the existing matrix builder
  - focused verification: Streamlit source/AppTest contracts passed (**2 passed**)
  - timing probe: direct T14, all-run-type, 4-preset pressure-factor comparison completed in **4.826s**, producing 5 base rows and 1 comparison wide row
  - interpretation: this makes disco/max-wave comparison debugging practical for a focused tier without changing default all-tier behavior or simulator truth
- follow-up Boss Waves CLI tier-scoped matrix routing on **2026-06-12**:
  - kept patch: run_stats matrix build paths now pass `tiers=(args.tier,)` when the existing CLI `--tier` option is supplied; omitted tier still builds the all-tier matrix
  - focused verification: matrix timing/default-all-tier and tier override tests passed (**2 passed**)
  - real CLI verification: `app.run_stats --tier 14 --include-boss-wave-milestone-matrix --out <temp>` completed in **4.247s** and wrote `boss_wave_milestone_matrix.json` with `tiers=[14]` and 5 rows
  - interpretation: CLI Dissonant/IDS audits can now use the same focused-tier matrix loop as Streamlit without introducing new flags or simulator behavior
- follow-up Boss Waves CLI Dissonant-category matrix routing on **2026-06-12**:
  - kept patch: run_stats matrix build paths now pass a single non-`none` Dissonant category to the matrix builder when the existing CLI `--dissonance-run-category` override is supplied; omitted/`none` still builds all categories
  - focused verification: default all-category and tier/category routing tests passed (**2 passed**)
  - real CLI verification: `app.run_stats --tier 14 --dissonance-run-category utility --include-boss-wave-milestone-matrix --out <temp>` completed in **1.738s** and wrote `boss_wave_milestone_matrix.json` with `tiers=[14]` and one Utility row
  - interpretation: CLI disco audits can now focus a single tier and Dissonant category without adding new flags or changing simulator truth
- follow-up Boss Waves Streamlit comparison alignment-summary rendering on **2026-06-12**:
  - kept patch: Streamlit renders `comparison.matrix.reference_alignment_summary` from the app-published matrix payload when a comparison overlay is present
  - focused verification: alignment-summary, comparison-summary, comparison-frame, and render source-contract tests passed (**4 passed**)
  - interpretation: pressure-factor and bridge comparison overlays now show raw over/under/match and worst-delta counts in the product surface without UI-local recomputation or simulator semantic changes
- follow-up Boss Waves raw comparison calculated-wave carry-through on **2026-06-12**:
  - kept patch: `comparison.wide_rows` now carries base/comparison calculated wave, calculated delta, raw delta-vs-reference, and calculated/reference ratio from the underlying matrix rows
  - focused verification: pressure-factor comparison functional test plus comparison frame/summary/source-contract tests passed (**4 passed**); a direct T14 Utility pressure-factor probe showed selected waves aligned at **4402** while raw calculated waves moved from **3699** to **3609**
  - interpretation: selected IDS/PB alignment can no longer hide whether an explicit pressure-factor or bridge overlay improves or worsens the raw boss-contact calculation
- follow-up Boss Waves run_stats comparison raw-movement diagnostics on **2026-06-12**:
  - kept patch: run_stats diagnostics publish `comparison_calculated_delta_summary` with raw higher/lower/match counts and worst-row selected/raw context
  - focused verification: matrix diagnostics test passed, and a focused T14 Utility CLI comparison wrote `row_count=1`, `comparison_raw_wave_lower_count=1`, `calculated_delta_wave=-90`, `default_calculated_wave=3699`, and `comparison_calculated_wave=3609`
  - interpretation: CLI/output consumers can now see raw pressure-factor or bridge movement directly from `run_stats.json` without parsing the full matrix artifact
- follow-up Boss Waves matrix-payload raw comparison movement summary on **2026-06-12**:
  - kept patch: the matrix payload now emits `comparison.calculated_delta_summary`, Streamlit renders it, and run_stats diagnostics mirror it from the payload
  - focused verification: comparison payload/UI/diagnostics tests passed (**5 passed**), and a focused T14 Utility CLI probe showed the matrix artifact and run_stats diagnostics summaries are identical with raw movement **3699 -> 3609**
  - interpretation: matrix artifact, Streamlit product surface, and generated diagnostics now carry the same raw base-vs-comparison movement summary
- follow-up Boss Waves v28 Damage/Health Decay closure-detail visibility on **2026-06-12**:
  - kept patch: Streamlit's Boss Waves assumptions table renders source-default availability, supplied magnitude fields, and supplied start-wave fields from `model_certification.v28_damage_health_decay_closure`
  - focused verification: Boss Waves assumptions/source-contract UI tests passed (**4 passed**)
  - interpretation: tournament Damage/Health Decay remains explicit-input-only until source-owned magnitudes are available, but the product surface now shows exactly which closure details are present
- follow-up QE/KB Enemy Level Skip Reduction metadata closure on **2026-06-12**:
  - kept patch: `kb/global-rules/contracts/environment-params.yaml` now declares `bc.enemy_level_skip_reduction_pp` as a `pct` environment param resolved by `bc_effective_value`
  - focused verification: targeted QE metadata/resolver tests and the existing lab-routing test passed (**3 passed**); simulator ELS reduction smoke test passed (**1 passed**); temp `app.run_stats --out <temp>` completed
  - interpretation: the routed IDS Enemy Level Skip Reduction surface no longer depends on `unknown` bounded metadata when consumed through QE; this is schema/contract closure only, not a Boss Waves ELS formula change
- follow-up Boss Waves matrix diagnostic-lane carry-through on **2026-06-12**:
  - kept patch: matrix rows, wide rows, comparison rows, and Streamlit's all-tier frame now expose selected-candidate hit-by-hit, contact-envelope, pre-contact boss-kill, and GC pre-contact waves
  - focused verification: targeted matrix payload/UI tests passed (**4 passed**); `app.run_stats --include-boss-wave-milestone-matrix` regenerated outputs
  - output verification: T14 Utility Dissonant now reports selected/reference `4402`, calculated/hit-by-hit `3699`, contact envelope `4419`, and calculated delta `-703`
  - interpretation: this makes the current IDS/PB mismatch diagnosable in the product surface while preserving the existing unified hit-by-hit selected-lane contract
- follow-up Boss Waves nearest-reference-lane diagnostics on **2026-06-12**:
  - kept patch: matrix rows, wide rows, comparison rows, reference-alignment summaries, generated outputs, and Streamlit matrix/summary frames now carry the candidate lane closest to the active IDS/PB reference
  - focused verification: targeted matrix payload/UI tests passed (**6 passed**); `app.run_stats --include-boss-wave-milestone-matrix` regenerated outputs; `git diff --check` passed for the touched code/tests/artifacts/governance files
  - output verification: T14 Utility Dissonant now reports reference `4402`, calculated/hit-by-hit `3699`, contact envelope `4419`, nearest lane `contact_envelope`, nearest lane delta `+17`, and calculated delta `-703`
  - interpretation: this does not change selected max-wave semantics; it makes the disco/IDS mismatch classifiable as reference-near-contact-envelope rather than hiding the raw hit-by-hit under-reference result
- follow-up Boss Waves matrix model-scope/blocker-summary carry-through on **2026-06-12**:
  - kept patch: matrix artifacts and run_stats diagnostics now publish top-level `model_scope`, `not_full_max_wave_model`, `model_certification_status`, `certified_full_max_wave_model`, and `model_blocker_summary`
  - focused verification: targeted matrix/diagnostics tests passed (**2 passed**); `app.run_stats --include-boss-wave-milestone-matrix` regenerated outputs; `git diff --check` passed for touched code/tests/artifacts
  - output verification: current matrix/run_stats diagnostics report 105 rows, `partial_boss_contact_model`, `not_full_max_wave_model = true`, 35 rows with `source_owned_non_boss_terminal_pressure_formulas`, and unsupported-pressure counts by pressure id
  - interpretation: this keeps the remaining non-boss pressure blocker machine-readable at the artifact/diagnostic level without inventing formulas or changing default account-truth pressure assumptions
- follow-up Boss Waves Streamlit model-blocker summary rendering on **2026-06-12**:
  - kept patch: Streamlit's all-tier preset matrix now renders the app-published model scope, partial-model status, blocker counts, and unsupported-pressure counts before the matrix rows
  - focused verification: Streamlit matrix render/helper contract tests passed (**4 passed**)
  - interpretation: the product surface now exposes the same model-scope caveat and blocker distribution as the generated artifacts, without UI-local recomputation or a second truth path
- follow-up Boss Waves terminal-pressure reference-status carry-through on **2026-06-12**:
  - kept patch: matrix candidates, selected rows, wide rows, comparison rows, generated outputs, and Streamlit's matrix frame now carry `terminal_pressure_reference_status`
  - focused verification: targeted capped/aligned pressure-reference and UI matrix tests passed (**5 passed**); `app.run_stats --include-boss-wave-milestone-matrix` regenerated outputs; `git diff --check` passed for touched code/tests/artifacts
  - output verification: current matrix reports 9 `empirical_reference_limited` rows, 7 `empirical_reference_aligned` rows, 19 `missing_empirical_reference_blocked` rows, and 70 unaffected rows
  - interpretation: raised-to-reference unsupported-pressure rows no longer appear as blank-limiter rows in the product matrix; the status is explicit without redefining `terminal_pressure_limiter` or changing selected wave semantics
- follow-up QE runtime surface value-type metadata closure on **2026-06-12**:
  - kept patch: `mechanic-params.yaml` now declares bounded metadata for the remaining generated runtime surfaces that were falling back to `unknown`, including UW/bot owned flags, Energy Shield charge/capability rows, card runtime effects, Chain Thunder max enemy damage reduction, shock damage multiplier, and Flame Bot burn-stack lab output
  - focused verification: targeted QE metadata/resolution tests passed (**6 passed**); `tests/qe/test_contracts_models_smoke.py` passed (**41 passed**); `app.run_stats --include-boss-wave-milestone-matrix` regenerated outputs
  - output verification: regenerated `out/run_stats.json`, `out/run_stats_query_rows_start_of_run.json`, and `out/run_stats_query_rows_max_progression.json` each report zero `value_type: unknown` rows
  - interpretation: this is schema/contract closure for already-routed runtime surfaces; it does not change simulator formulas, selected max-wave semantics, or the remaining source-owned non-boss terminal-pressure blocker
- follow-up Rend Armor start-of-run resolution closure on **2026-06-12**:
  - kept patch: blank/unpurchased Rend Armor workshop levels now materialize as `0.0` in the compiler, and QE resolves `tower_rend_armor_multiplier` with base-times-post-multiplier semantics rather than generic multiplier fallback
  - focused verification: Rend progression bundle test plus Stats dashboard contracts passed (**55 passed**); QE contract smoke passed (**41 passed**); `app.run_stats --include-boss-wave-milestone-matrix` regenerated outputs
  - output verification: regenerated start query rows publish Rend Armor Chance as `0%` and Rend Armor Multiplier as `x0`; max progression still publishes `36%` and `x0.367`
  - interpretation: this removes a false `mapped_not_resolved` start-state block for a Boss Waves-relevant attack surface without changing Dissonant masks, max-rend formula ownership, or pressure-factor semantics
- follow-up Interest / Wave status-resolution closure on **2026-06-12**:
  - kept patch: QE's generic percent resolver now consumes vault-owned percent contributors before enhancement multipliers, so the zero vault/base `Interest / Wave` contributor is counted with the lab and workshop contributors
  - focused verification: focused simulator progression assertions passed (**2 passed**); Stats dashboard contracts passed (**54 passed**); QE contract smoke passed (**41 passed**); runtime surface metadata tests passed (**81 passed**); `app.run_stats --include-boss-wave-milestone-matrix` regenerated outputs
  - output verification: regenerated start and max query rows each publish `state::economy.interest_per_wave_pct = 7.16%` as `resolved` for Farming and Tourney, with only `resolved`/`gated_off` row statuses and zero `value_type: unknown` query rows
  - interpretation: this removes a false `partially_resolved` status for an already-correct economy surface without changing the Interest formula, display value, Boss Waves selection, or pressure-factor semantics
- follow-up Boss Waves matrix certification and pressure-factor hint carry-through on **2026-06-12**:
  - kept patch: `app.pipeline` now publishes normalized row-level `model_certification_status` / `model_certification` and an explicit `pressure_factor_reference_hint` payload for reference-backed matrix rows; `app.streamlit_inspector` renders the pressure hint and direction from the app-owned payload
  - focused verification: focused Boss Waves matrix/UI tests passed (**6 passed**); matrix diagnostics tests passed (**3 passed**); full app pipeline functional tests passed (**118 passed**); full app pipeline UI contracts passed (**49 passed**); `app.run_stats --include-boss-wave-milestone-matrix` regenerated outputs
  - output verification: regenerated `out/boss_wave_milestone_matrix.json` reports 105 rows with row-level `model_certification_status = partial_boss_contact_model`; `pressure_factor_hint_summary` reports 76 hinted rows, direction counts of 60 `increase_pressure` and 16 `no_adjustment`, and `out/run_stats.json` carries the same hint summary in diagnostics
  - interpretation: this makes disco/IDS pressure-factor analysis copyable and inspectable without turning the heuristic into KB truth, changing default account-truth runs, or closing the source-owned non-boss terminal-pressure blocker
- follow-up Boss Waves missing-reference gap summary on **2026-06-12**:
  - kept patch: `app.pipeline` now publishes `reference_gap_summary` in the milestone matrix and generated run_stats diagnostics, and `app.streamlit_inspector` renders missing-reference row/kind counts in the model/blocker table
  - focused verification: missing-reference matrix test passed (**1 passed**); model-blocker UI tests passed (**2 passed**); full app pipeline functional + UI contract suite passed (**168 passed**); `app.run_stats --include-boss-wave-milestone-matrix` regenerated outputs
  - output verification: regenerated `out/boss_wave_milestone_matrix.json` and `out/run_stats.json` report 19 missing-reference-blocked rows, with `by_reference_kind = {ids_dissonant_pb_wave: 18, ids_milestone_wave: 1}` and run-type tier lists for Regular T21 plus Dissonant high-tier gaps
  - interpretation: this separates missing IDS/PB data from source-owned formula work, making the remaining max-wave blocker more actionable without changing simulator selection, pressure-factor semantics, or certification status
- follow-up Boss Waves missing-reference row visibility on **2026-06-12**:
  - kept patch: Streamlit's all-tier preset matrix now renders `_boss_wave_matrix_reference_gap_frame(...)` from `reference_gap_summary.missing_references`, showing tier, run type, reference kind, calculated wave, and unsupported-pressure ids for rows blocked by absent IDS/PB references
  - focused verification: focused matrix render/reference-gap UI tests passed (**3 passed**); full Boss Waves UI contract suite passed (**51 passed**); `git diff --check` passed
  - output verification: no generated artifact rebuild was required because this slice renders existing app-published `reference_gap_summary` fields
  - interpretation: this closes the product-surface visibility gap for high-tier missing-reference rows without changing simulator formulas, selected-wave semantics, pressure-factor semantics, or certification status
- follow-up Boss Waves enemy-speed contact timing carry-through on **2026-06-12**:
  - kept patch: `simulators.timing.boss_contact_time_seconds(...)` now applies source-owned enemy-speed increase and boss-speed multiplier to derived boss contact timing, and `app.pipeline` passes the scenario-owned surfaces into the sanctioned Boss Waves payload path
  - focused verification: timing contact test passed (**1 passed**); tournament payload contact-time test passed (**1 passed**); full simulator core smoke passed (**29 passed**); full app pipeline functional suite passed (**119 passed**); app main-path smoke passed (**21 passed**); `app.run_stats --include-boss-wave-milestone-matrix` regenerated outputs; `git diff --check` passed
  - output verification: regenerated `out/boss_wave_milestone_matrix.json` still reports 105 rows, `partial_boss_contact_model`, 35 rows with `source_owned_non_boss_terminal_pressure_formulas`, and 19 missing-reference-blocked rows
  - interpretation: this closes boss-movement speed carry-through only; enemy-speed non-boss fleet pressure remains an unsupported terminal-pressure blocker unless explicitly closed by terminal max-wave inputs or the pressure-factor approximation
- follow-up Boss Waves Dissonant boost carry-through on **2026-06-12**:
  - kept patch: Boss Waves now requests all KB/QE-owned Dissonant Echo/active-boost source surfaces for Attack, Defense, Utility, and Ultimate Weapons instead of Defense-only, and publishes the resolved active boost, Echo, total, eDamage, and eHP factors in replacement primitive diagnostics
  - focused verification: new Dissonant boost payload test passed (**1 passed**); adjacent Dissonant mask/contact tests passed (**2 passed**); full app pipeline functional suite passed (**120 passed**); QE Dissonant/composite and simulator contract snapshot checks passed (**6 passed**); app main-path smoke passed (**21 passed**); `app.run_stats --include-boss-wave-milestone-matrix` regenerated outputs
  - output verification: regenerated `out/boss_wave_milestone_matrix.json` reports 105 rows, `partial_boss_contact_model`, 76 reference-backed calculated deltas, 60 raw overestimates, 0 raw underestimates, 16 raw matches, and T14 Utility Dissonant now raw-calculates `4509` vs IDS/PB `4402` with pressure hint `1.024`
  - interpretation: this closes a real Dissonant stat carry-through bug at the Boss Waves QE boundary; the low-tier raw overestimates remain, with worst delta still Tier 1 Attack Dissonant `+24910`, so the broader full-run/non-boss pressure blocker is unchanged
- follow-up Boss Waves Boss Ultimate explicit terminal closure on **2026-06-12**:
  - kept patch: `boss_terminal_max_wave` is now a KB-declared scenario runtime input, CLI/Streamlit control, app runtime mapping, and exact terminal-pressure closure field for `boss_ultimate_deferred`
  - focused verification: runtime-input field-set, boss-terminal certification mapping, CLI matrix-input conversion, main-path CLI pass-through, and Streamlit manual-runtime wiring tests passed (**5 passed**); broader app/input suite passed (**203 passed**); `app.run_stats --include-boss-wave-milestone-matrix` regenerated outputs
  - output verification: direct T17 Boss Waves payload reports `boss_ultimate_deferred -> boss_terminal_max_wave` in `required_fields_by_pressure` with no unmapped pressures; regenerated `out/boss_wave_milestone_matrix.json` lists `boss_terminal_max_wave` as a supported explicit runtime override
  - interpretation: this does not invent Boss Ultimate math or certify full max-wave modeling; it removes an unnecessary unmapped-pressure gap so exact external terminal-wave observations can close boss-specific deferred pressure through the sanctioned runtime-input path
- follow-up Boss Waves missing-reference terminal-input carry-through on **2026-06-12**:
  - kept patch: `app.pipeline` now carries `terminal_pressure_runtime_override_status` from each matrix candidate into selected rows, row-level certification, top-level matrix certification, and `reference_gap_summary.missing_references`; `app.streamlit_inspector` renders required and missing terminal inputs in the missing-reference frame
  - focused verification: focused missing-reference matrix/UI tests passed (**2 passed**); full app pipeline functional + UI contract suite passed (**172 passed**); `app.run_stats --include-boss-wave-milestone-matrix` regenerated outputs
  - output verification: regenerated `out/boss_wave_milestone_matrix.json` reports active unsupported-pressure input requirements at top level and per missing-reference row, including `armored_terminal_max_wave`, `boss_terminal_max_wave`, `fleet_terminal_max_wave`, and `protector_terminal_max_wave`, with no unmapped pressures
  - interpretation: this is diagnostic/product carry-through only; it does not add source-owned non-boss pressure formulas, alter the pressure-factor approximation path, change selected-wave semantics, or certify a full max-wave model
- follow-up Boss Waves run-stats matrix certification carry-through on **2026-06-12**:
  - kept patch: generated `run_stats` diagnostics now include the matrix `model_certification` block plus `runtime_override_closure`, `terminal_pressure_runtime_override_status`, and `non_boss_terminal_pressure_closure` shortcuts, so output consumers do not need to open the full matrix artifact to see terminal-pressure closure state
  - kept patch: matrix selected-row certification aggregation now reports non-boss terminal pressure closure as `missing` when unsupported pressure rows are selected and no exact terminal max-wave input or explicit pressure-factor approximation closes them
  - focused verification: run-stats diagnostics timing/contract test and missing-reference matrix test passed (**2 passed**); full app pipeline functional suite passed (**121 passed**); `app.run_stats --include-boss-wave-milestone-matrix` regenerated outputs
  - output verification: regenerated `out/boss_wave_milestone_matrix.json` and `out/run_stats.json` both report `non_boss_terminal_pressure_closure.mode = missing`, required terminal inputs `armored_terminal_max_wave`, `boss_terminal_max_wave`, `fleet_terminal_max_wave`, and `protector_terminal_max_wave`, and zero unmapped pressures
  - interpretation: this is certification and diagnostics honesty; it does not create non-boss pressure formulas, change selected waves, or make the partial matrix a certified full max-wave model
- follow-up Boss Waves missing-reference uncapped-wave visibility on **2026-06-12**:
  - kept patch: `app.pipeline` now carries `unsupported_pressure_uncapped_selected_max_wave` into `reference_gap_summary.missing_references`, and `app.streamlit_inspector` renders it as an `Uncapped` column beside the fail-closed calculated value
  - focused verification: focused missing-reference matrix/UI tests passed (**2 passed**); full app pipeline functional + UI contract suite passed (**172 passed**); `app.run_stats --include-boss-wave-milestone-matrix` regenerated outputs
  - output verification: regenerated `out/boss_wave_milestone_matrix.json` and `out/run_stats.json` both show the first missing-reference row as T16 Utility with `best_calculated_selected_max_wave = 0` and `unsupported_pressure_uncapped_selected_max_wave = 1827`, preserving the blocked selected value while exposing the raw boss-contact estimate
  - interpretation: this is disco/debug carry-through only; it does not change selected-wave semantics, fabricate missing IDS/PB references, or close source-owned non-boss pressure formulas
- follow-up Boss Waves effective certification closure on **2026-06-12**:
  - kept patch: `app.pipeline` now publishes `model_certification.effective_model_closure`, distinguishing requirements that are satisfied or not applicable from requirements closed specifically by runtime overrides; matrix rows, generated `run_stats` diagnostics, and Streamlit assumptions carry the same map
  - focused verification: focused certification/matrix/run-stats/UI tests passed (**5 passed**); full app pipeline functional + UI contract suite passed (**172 passed**); `app.run_stats --include-boss-wave-milestone-matrix` regenerated outputs
  - output verification: regenerated matrix and run-stats diagnostics report effective closure as `{non_boss_terminal_pressure: false, v28_damage_health_decay_magnitudes: true, boss_applicable_damage_semantics: true, gc_boss_applicable_damage_semantics: true}` for the default matrix, while runtime override closure remains false for non-applicable fields
  - interpretation: this is model-certification readability and product honesty; it does not change selected waves, change requirement applicability, or close the source-owned non-boss pressure formula blocker
- follow-up Boss Waves matrix root certification shortcuts on **2026-06-12**:
  - kept patch: `build_boss_wave_milestone_matrix(...)` now mirrors selected-row certification shortcuts at the matrix artifact root, including blockers, runtime override closure, effective model closure, terminal-pressure override status, and non-boss pressure closure
  - focused verification: missing-reference matrix and run-stats diagnostics tests passed (**2 passed**); full app pipeline functional suite passed (**121 passed**); `app.run_stats --include-boss-wave-milestone-matrix` regenerated outputs
  - output verification: regenerated `out/boss_wave_milestone_matrix.json` and `out/run_stats.json` now both expose the same top-level certification shortcuts, and both mirror their nested `model_certification.effective_model_closure`
  - interpretation: this is artifact contract carry-through for downstream consumers; it does not change simulator semantics, selected waves, or the remaining source-owned non-boss pressure blocker
- follow-up Boss Waves pressure-factor reference-blocker closure on **2026-06-12**:
  - kept patch: unsupported terminal-pressure empirical reference limiting now skips when a non-default `boss_wave_pressure_factor` is supplied, matching the existing model-certification closure mode `boss_wave_pressure_factor_approximation`
  - focused verification: missing-reference blocker, pressure-factor closure, and certification tests passed (**3 passed**); full app pipeline functional suite passed (**122 passed**); `app.run_stats --include-boss-wave-milestone-matrix` regenerated default outputs
  - output verification: default regenerated matrix still reports `source_owned_non_boss_terminal_pressure_formulas` and 19 missing-reference rows; a direct T16 Utility pressure-factor probe reports no blockers, effective non-boss closure true, selected/calculated wave `1687`, complete status, no limiter, and zero missing-reference rows
  - interpretation: this makes the user-approved pressure-factor approximation internally consistent without making it a hidden default or source-owned formula
- follow-up EP compare verdict publication honesty on **2026-06-12**:
  - kept patch: `evaluators.compare` and `evaluators.compare_core` now map `matched_exact` and `matched_close` compare statuses to `aligned` KB status and `pass` verdicts, and normalize verdict fields from status before publication; bounded `app.run_stats` cleanup also removes stale full-pipeline artifacts such as `ep_oracle_compare.json`, `line_by_line_verification.json`, and `pipeline_trace.json` so fast-path outputs cannot preserve old compare evidence
  - focused verification: EP verdict/status mapping and stale-artifact cleanup tests passed (**6 passed**); full app pipeline functional + UI contract suite passed (**188 passed**); Stats dashboard plus Boss Waves simulator gates passed (**121 passed**); QE/contract smoke gates passed (**131 passed**); `app.run_stats --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs
  - output verification: regenerated `out/ep_oracle_compare.json` reports 49 rows with status counts `matched_exact=17`, `matched_close=9`, `stage_scope_mismatch=23`; KB alignment counts are `aligned=26`, `not_comparable=23`; verdict counts are `pass=26`, `pass_with_compare_limitations=23`; true formula mismatches remain `0`
  - interpretation: this repairs false fail evidence in the EP compare publication contract and prevents stale full-pipeline artifacts from being misread as bounded `run_stats` output; it does not change formulas, invent missing workbook assumptions, close stage-scope rows, or certify a full non-boss max-wave model
- follow-up Dissonance scenario-gate boolean resolution on **2026-06-12**:
  - kept patch: `qe.routing` now treats `scenario_gate_bool` and `_active` destination ids as boolean semantic surfaces, so Dissonant Run active support rows with `False` contributors resolve as real false rows instead of `mapped_not_resolved` numeric semantic failures
  - focused verification: Dissonant Run scenario-context QE tests and Boss Waves primitive contract test passed (**3 passed**); broadened QE/app/Boss Waves suite passed (**439 passed**); `app.run_stats --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs
  - output verification: regenerated `out/line_by_line_verification.json` now reports the four `support_surface::dissonance.*_run_active` rows as `resolved / publishable / False / pass`; line-verification verdict counts moved to `pass=594`, `fail=29`, `needs_work=12`
  - interpretation: this closes a false Dissonant carry-through failure in the QE scenario support path; it does not add new Dissonant Run restrictions, change selected Boss Waves rows, or close the remaining source-owned non-boss terminal-pressure formula blocker
- follow-up enemy ultimate-enabled context boolean resolution on **2026-06-12**:
  - kept patch: `qe.routing` now treats `.enabled` and `_enabled` destination ids as boolean semantic surfaces, so the six `context::enemy.*.ultimate_enabled` rows routed from IDS battle-condition labs resolve through QE as boolean context rows instead of numeric semantic failures
  - focused verification: enemy ultimate-enabled report-snapshot and Dissonant Run scenario-context QE tests passed (**2 passed**); broadened QE/app/Boss Waves suite passed (**440 passed**); `app.run_stats --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs
  - output verification: regenerated `out/line_by_line_verification.json` now reports all six `context::enemy.{fast,ranged,boss,basic,tank,protector}.ultimate_enabled` rows as `resolved / publishable / False / pass`; line-verification verdict counts moved to `pass=600`, `fail=23`, `needs_work=12`, with `not_resolved=0`
  - interpretation: this closes the remaining false not-resolved context rows in the QE publication path; it does not implement source-owned Boss Ultimate behavior or certify a full all-enemy max-wave model
- follow-up level/duration semantic publication repair on **2026-06-12**:
  - kept patch: `qe.routing` now treats `level` contributors as publishable only when the destination itself is a level surface, and treats `duration_seconds` as a seconds-compatible contributor value type; `evaluators.compare` and `evaluators.verification_engine` mirror that level-surface rule in line verification
  - focused verification: QE report-snapshot level/duration tests, non-level formula guard test, enemy ultimate-enabled test, and line-verification level-surface guard passed (**4 passed**); broadened QE/app/Boss Waves suite passed (**443 passed**); `app.run_stats --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs
  - output verification: regenerated `out/line_by_line_verification.json` now reports Dissonant Echo lab levels, Range selected level, Shockwave Size selected level, and Amplifying Strike duration as `publishable / pass`; line-verification verdict counts moved to `pass=607`, `fail=23`, `needs_work=5`
  - interpretation: this closes false publication failures for raw level state surfaces and seconds-duration module effects without allowing raw lab levels to satisfy non-level formula destinations such as Super Tower Bonus
- follow-up Super Tower formula and raw-text passthrough closure on **2026-06-12**:
  - kept patch: `kb/global-rules/tables/runtime-formula-authority.csv` and `workshop-formula-params-canonical.csv` now route `Super Tower Bonus` to the existing canonical formula `wiki_verified_super_tower_bonus_lab_multiplier`, `qe.kb_surfaces` accepts runtime-authority-mapped `exact_linear_generator_from_live_wiki_rule` formulas, and `qe.routing`/line verification now preserve raw-text enum capability/account-context passthrough rows without allowing raw text on normal formula surfaces
  - focused verification: Super Tower formula authority/compiler tests and raw-text passthrough guard tests passed; broadened QE/app/Boss Waves suite passed (**445 passed**); `app.run_stats --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs
  - output verification: regenerated `out/line_by_line_verification.json` now reports Super Tower Bonus, Target Priority, First Perk Choice, Auto Pick Ranking, and Farming Tier as `publishable / pass`; line-verification verdict counts moved to `pass=612`, `fail=23`, `needs_work=0`, `not_resolved=0`
  - interpretation: this closes the remaining current line-verification wiring gaps while preserving the 23 known EP stage-scope comparison limitations as explicit blocked/fail rows; it does not remove the Boss Waves partial-model caveat or invent non-boss terminal-pressure formulas
- follow-up line-verification stage-scope severity alignment on **2026-06-12**:
  - kept patch: `evaluators.compare` and `evaluators.verification_engine` no longer treat EP `stage_scope_mismatch` rows as line-verification blockers; they remain visible through `kb_alignment_status=not_comparable`, `ep_compare_status=stage_scope_mismatch`, and `verdict=pass_with_compare_limitations`
  - focused verification: line-verification stage-scope severity regression and EP verdict mapping tests passed (**4 passed**); broadened QE/app/Boss Waves suite passed (**446 passed**); `app.run_stats --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs
  - output verification: regenerated `out/line_by_line_verification.json` now reports `pass=612`, `pass_with_compare_limitations=23`, `fail=0`, `needs_work=0`, and `not_resolved=0`
  - interpretation: this removes the last false failure signal from line verification without hiding EP stage-scope limitations or reclassifying them as formula matches; it does not certify missing workbook stage inputs or the full all-enemy Boss Waves model
- follow-up Boss Waves primitive-family carry-through evidence on **2026-06-12**:
  - kept patch: `app.pipeline` now builds `replacement_primitive_family_coverage` from QE statbook surface/contributor rows during Boss Waves primitive resolution, carries a compact selected-row aggregate into `out/boss_wave_milestone_matrix.json`, mirrors it in `out/run_stats.json` / `out/diagnostics.json`, and `app.streamlit_inspector` renders the matrix coverage summary from the app-published payload
  - focused verification: touched Boss Waves payload/matrix/UI tests passed; full `tests/app/test_pipeline_functional.py tests/app/test_pipeline_ui_contracts.py` passed (**192 passed**); `tests/app/test_stats_dashboard_contract.py` passed (**55 passed**); temp `app.run_stats --include-boss-wave-milestone-matrix`, root `app.run_stats --include-boss-wave-milestone-matrix`, and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` completed successfully
  - output verification: regenerated `out/boss_wave_milestone_matrix.json`, `out/run_stats.json`, and `out/diagnostics.json` report `replacement_primitive_family_coverage_summary.status = covered`, `selected_row_count = 105`, `rows_with_coverage = 105`, and no missing requested families for bot, card base, card mastery, workshop, enhancement, module, or relic; regenerated `out/family_completeness_matrix.json` still reports requested effect-route closure `closed`
  - interpretation: this proves the currently selected Boss Waves rows are fed by the requested effect families through the sanctioned QE/app primitive boundary. It does not invent non-boss enemy formulas, change selected max-wave math, or remove the remaining full-model blocker `source_owned_non_boss_terminal_pressure_formulas`.
- follow-up EP limitation and ranged tradeoff carry-through repair on **2026-06-12**:
  - kept patch: `evaluators.compare` now keeps known EP export defects visible as mismatch deltas while publishing them as `not_comparable / pass_with_compare_limitations`, and marks `derived::ehp` plus `derived::edamage.super_tower_factor` as EP shortcut-scope limitations instead of formula failures
  - kept patch: `qe.stat_input_compiler` now routes the ranged-distance/ranged-damage tradeoff perk through the existing KB perk target overrides; `enemy.ranged.attack_distance_rule` is declared as a raw-text environment passthrough and resolves as `context::enemy.ranged.attack_distance_rule`
  - focused verification: compare/routing regression tests and full `tests/app/test_pipeline_functional.py` plus new QE checks passed (**135 passed**); `tests/app/test_stats_dashboard_contract.py tests/app/test_pipeline_ui_contracts.py` passed (**114 passed**); `app.run_stats --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs
  - output verification: regenerated `out/line_by_line_verification.json` reports `pass=622`, `pass_with_compare_limitations=23`, and no fail/needs-work/trace-only rows; `out/diagnostics.json` reports EP compare `matched_exact=17`, `matched_close=9`, `mismatch=3` known export defects, `stage_scope_mismatch=20`, and `ep_unknown_formula_mismatch_count=0`
  - interpretation: this removes stale false-fail evidence and wires the named ranged tradeoff through the sanctioned QE/context path; it does not add source-owned non-boss terminal-pressure formulas, apply a hidden difficulty factor, or certify the full all-enemy max-wave model.
- follow-up current-scope effect-family evidence summary on **2026-06-12**:
  - kept patch: full-pipeline `out/diagnostics.json` now publishes `current_scope_effect_family_evidence`, a compact app diagnostics summary combining existing `family_completeness_matrix.json` route-closure evidence with existing Boss Waves milestone-matrix selected-row primitive-family coverage
  - focused verification: current-scope effect-family contract test passed (**1 passed**); full app pipeline functional + Stats dashboard + UI contract suite passed (**248 passed**); `app.run_stats --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs
  - output verification: regenerated diagnostics report `status = covered`, requested families `bot`, `card_base`, `card_mastery`, `workshop`, `enhancement`, `module`, and `relic`, no missing route/generated/Boss Waves families, and `boss_wave_rows_with_coverage = 105`
  - interpretation: this makes the current evidence answer auditable from the main diagnostics artifact without moving mechanic truth out of KB/QE artifacts or inventing max-wave formulas; the full all-enemy model blocker `source_owned_non_boss_terminal_pressure_formulas` remains open.
- follow-up Boss Waves Dissonant calibration-exclusion evidence on **2026-06-12**:
  - kept patch: `pressure_factor_hint_summary` now publishes disabled pressure-hint modes plus calibration excluded-caveat reason counts, and Streamlit renders those app-published counts in the Boss Waves pressure-factor summary frame
  - focused verification: pressure-factor summary functional/UI tests passed (**3 passed**); full app pipeline functional + Stats dashboard + UI contract suite passed (**248 passed**); `app.run_stats --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs
  - output verification: regenerated Boss Waves matrix and diagnostics report disabled hint modes `dissonance_pb_bonus_cap_not_exact_reference = 42` and `no_positive_reference_wave = 29`; calibration excludes enabled caveated hints for `below_3000_wave_perk_volatility = 11` and `pb_age_unknown_no_source_timestamp = 14`
  - interpretation: this makes the Disco/Dissonant caveats machine-readable in the same pressure-factor evidence surface the operator uses for max-wave debugging. It does not apply a hidden difficulty factor, treat Dissonant PB `5000` as exact, or close `source_owned_non_boss_terminal_pressure_formulas`.
- follow-up Boss Waves selected-run pressure-factor closure carry-through on **2026-06-12**:
  - kept patch: selected Boss Waves payload diagnostics now mirror model-certification shortcut fields at the top level, including certification status, full-model flag, blockers, runtime/effective closure, terminal-pressure override status, and non-boss pressure closure
  - focused verification: paired selected-run default-blocked / explicit-pressure-factor-closed tests and pressure-factor HP/damage test passed (**3 passed**); full app pipeline functional + Stats dashboard + UI contract suite passed (**249 passed**); `app.run_stats --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs
  - output verification: direct T16 Ultimate Weapon Dissonant selected payload with `boss_wave_pressure_factor = 1.25` reports `context_status = complete`, selected wave `987`, top-level blockers `[]`, and top-level/nested `non_boss_terminal_pressure_closure.mode = boss_wave_pressure_factor_approximation`
  - interpretation: this proves the user-approved simple difficulty-factor approximation closes the selected Streamlit Boss Waves path when supplied explicitly. It does not make the factor a hidden default or close the default account-truth blocker `source_owned_non_boss_terminal_pressure_formulas`.
- follow-up Boss Waves recommended-assumption pressure-factor boundary on **2026-06-12**:
  - kept patch: Streamlit's recommended model assumptions table now includes a `boss_wave_pressure_factor` disclosure row stating that pressure factor is not a recommended default and that the matrix calibration median input is comparison-only; the recommended runtime-input map remains unchanged
  - focused verification: recommended-assumptions source/runtime contract tests passed (**2 passed**); full Boss Waves/UI contract suite passed (**59 passed**); touched-file `py_compile` and `git diff --check` passed
  - interpretation: this prevents the operator-facing recommended-assumptions panel from implying a hidden difficulty-factor default. It does not change Boss Waves math, generated outputs, KB/QE truth, or the remaining full-model blocker `source_owned_non_boss_terminal_pressure_formulas`.
- follow-up run_stats current-scope family evidence carry-through on **2026-06-12**:
  - kept patch: when bounded `app.run_stats` builds the optional Boss Waves milestone matrix, `run_stats.json` now carries `current_scope_effect_family_evidence` in its embedded diagnostics, recomputed from the current bounded stat-input set and the freshly built Boss Waves matrix
  - focused verification: bounded matrix timing/evidence contract plus current-scope Stats evidence contract passed (**2 passed**); touched `py_compile` passed; `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix` passed; full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` passed with `exit = 0` and `files = 34`; `git diff --check` passed
  - output verification: regenerated `out/run_stats.json` reports current-scope effect-family evidence `status = covered`, requested families `bot`, `card_base`, `card_mastery`, `workshop`, `enhancement`, `module`, and `relic`, and `boss_wave_rows_with_coverage = 105`; regenerated full `out/diagnostics.json` still reports `ep_true_formula_mismatch_count = 0` and `ep_unknown_formula_mismatch_count = 0`
  - interpretation: this makes the main committed product artifact answer the requested family-to-Boss-Waves evidence question directly while preserving full EP compare evidence ownership in full-pipeline diagnostics / `ep_oracle_compare.json`. It does not change Boss Waves math, apply a hidden pressure factor, or close `source_owned_non_boss_terminal_pressure_formulas`.
- follow-up requested-family value-type carry-through on **2026-06-12**:
  - kept patch: QE native and compat statbook resolution now publish concrete row value types when KB metadata is absent instead of carrying literal `unknown`; missing card runtime units, account relic counts, and relic support ratio/multiplier semantics are typed at their owning KB/compiler surfaces
  - focused verification: current-scope Stats evidence, run_stats timing/evidence, and QE owner tests passed (**5 passed**); full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` passed with `exit = 0` and `files = 34`
  - output verification: regenerated `out/statbook_publishable.json` reports zero global `unknown` value types; regenerated `out/diagnostics.json` reports current-scope effect-family evidence `status = covered`, `line_verification_status = covered`, `unknown_value_type_families = []`, and per-family unknown counts of zero for bot, card_base, card_mastery, workshop, enhancement, module, and relic
  - interpretation: this strengthens the requested "all stats calculated properly" evidence without changing simulator math, applying a hidden pressure factor, or certifying the remaining all-enemy max-wave blocker.
- follow-up Boss Waves explicit approximation-closure contract on **2026-06-12**:
  - kept patch: `app.pipeline` now publishes `model_closure_status` and `accepted_approximation_closure` on selected Boss Waves payload diagnostics, matrix candidates, matrix rows, top-level matrix certification, and `out/diagnostics.json`; `app.streamlit_inspector` renders the same model-certification closure fields in the operator assumptions table
  - focused verification: full `tests/app/test_pipeline_functional.py` passed (**135 passed**); full `tests/app/test_pipeline_ui_contracts.py` passed (**59 passed**); `tests/simulators/test_evaluator_kernel.py` passed (**35 passed**); touched `py_compile` and `git diff --check` passed; `tests/simulators/test_run_executor.py` is absent in this replacement-era repo state
  - output verification: regenerated `out/boss_wave_milestone_matrix.json` and `out/diagnostics.json` report default top-level `model_closure_status = partial_missing_required_model_inputs`, selected-row closure counts `closed_for_applicable_requirements = 70` and `partial_missing_required_model_inputs = 35`, and no hidden accepted pressure-factor approximation in the default committed matrix
  - interpretation: this makes the user-approved simple difficulty-factor path explicit and auditable when supplied, while preserving the honest default blocker and the existing `certified_full_max_wave_model = false` / `partial_boss_contact_model` certification.
- follow-up Boss Waves accuracy-posture summary on **2026-06-13**:
  - kept patch: `app.pipeline` now publishes `model_accuracy_summary` in the Boss Waves milestone matrix and diagnostics, combining certification closure, blocker counts, comparison-only pressure-factor calibration input, missing empirical references, and the reference caveat counts for sub-3000 volatility, unknown PB age, and Dissonant PB 5000 cap rows; `app.streamlit_inspector` renders the same summary for default and comparison matrices
  - focused verification: full `tests/app/test_pipeline_functional.py tests/app/test_pipeline_ui_contracts.py` passed (**196 passed**); `tests/simulators/test_evaluator_kernel.py` passed (**35 passed**); `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs; touched `py_compile` and `git diff --check` passed
  - output verification: regenerated `out/boss_wave_milestone_matrix.json` and `out/diagnostics.json` report `model_accuracy_summary.status = default_partial_comparison_calibration_available`, comparison-only pressure-factor input `boss_wave_pressure_factor = 2.606384292771721`, `missing_reference_blocked_count = 19`, and caveat counts `below_3000_wave_perk_volatility = 11`, `pb_age_unknown_no_source_timestamp = 56`, and `dissonance_pb_5000_bonus_cap_floor = 42`
  - interpretation: this makes the current Boss Waves accuracy posture explicit in the artifact without changing selected waves, applying a hidden factor, or converting the partial Boss Waves model into a full all-enemy certification.
- follow-up current-scope effect-row carry-through evidence on **2026-06-13**:
  - kept patch: `app.pipeline` now publishes `generated_mapping_status`, `effect_row_carrythrough_status`, per-family carry-through status counts, and per-family `effect_row_carrythrough` blocks inside `current_scope_effect_family_evidence`, derived from the existing family completeness matrix, KB route ledger closure, statbook/line-verification evidence when available, and Boss Waves selected-row primitive coverage
  - focused verification: current-scope Stats evidence and bounded run_stats matrix/evidence tests passed (**2 passed**); full `tests/app/test_stats_dashboard_contract.py` passed (**55 passed**); `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs; touched `py_compile` and `git diff --check` passed
  - output verification: regenerated `out/diagnostics.json` reports `generated_mapping_status = closed`, `effect_row_carrythrough_status = covered`, `effect_row_carrythrough_status_counts = {'covered': 7}`, `boss_wave_rows_with_coverage = 105`, and `line_verification_status = covered`; regenerated `out/run_stats.json` carries the same generated/Boss-Waves carry-through fields while keeping `line_verification_status = not_evaluated` for the bounded diagnostics context
  - interpretation: this makes every requested current-scope effect family visibly mapped and carried through to Boss Waves selected rows in the main diagnostics contract. It does not change Boss Waves math, apply a hidden pressure factor, or close the remaining full all-enemy blocker `source_owned_non_boss_terminal_pressure_formulas`.
- follow-up Dissonant pressure-factor evidence quality on **2026-06-13**:
  - kept patch: `app.pipeline` now publishes per-run-type pressure-factor distributions, evidence-quality labels, explicit manual/comparison review input hints only when the median factor is a non-`1.0` factor, and a structured comparison-only review request for each such hint; `model_accuracy_summary` carries compact `pressure_factor_by_run_type` plus `dissonance_pressure_factor_evidence`, and `app.streamlit_inspector` renders the app-published by-run-type median, review-input, clean-median, clean-hint, and caveated-hint fields
  - focused verification: pressure-factor summary, model-accuracy posture, matrix comparison, and Streamlit pressure-frame tests passed (**9 passed**); `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs; touched `py_compile` and `git diff --check` passed
  - output verification: regenerated `out/boss_wave_milestone_matrix.json`, `out/run_stats.json`, and `out/diagnostics.json` report `dissonance_pressure_factor_evidence.status = caveated_dissonance_hints_only`, `rows_with_pressure_factor_hint = 14`, `calibration_quality_hint_count = 0`, caveat counts `below_3000_wave_perk_volatility = 7` and `pb_age_unknown_no_source_timestamp = 14`, disabled hint counts `dissonance_pb_bonus_cap_not_exact_reference = 42` plus `no_positive_reference_wave = 28`, and one non-`1.0` Dissonant explicit review input / comparison-only review request for Utility Dissonant Run with `boss_wave_pressure_factor = 1.0243071331213085`
  - interpretation: this prevents the regular-row global pressure-factor calibration from being mistaken as clean Dissonant calibration, and prevents `1.0` no-adjustment rows from being advertised as explicit closure inputs. Dissonant pressure-factor values remain explicit manual/comparison hints only and still do not change selected waves, apply a hidden factor, or certify the full all-enemy model.
- follow-up Dissonant Echo formula-bridge line verification on **2026-06-13**:
  - kept patch: `kb/ledgers/formula_surface_policy.yaml` now names the four `derived::dissonance.*.echo_multiplier` formula bridges as KB/QE-authorized level-consuming formulas, `qe.query_derived_composites` now routes `echo_bonus_multiplier` through the derived Echo ratio and `total_multiplier` through the derived Echo bonus, and line verification only permits raw level contributors when the formula ledger explicitly allows them
  - focused verification: Dissonant Echo QE contributor-contract, line-verification authorized-formula guard, generic non-level formula level-block guard, T14 Dissonant UI contract, current-scope Stats evidence, and full-pipeline artifact contract tests passed (**8 passed**); `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs; touched `py_compile` passed
  - output verification: regenerated `out/line_by_line_verification.json` reports `publishable = 614`, `not_applicable = 31`, verdict counts `pass = 622` and `pass_with_compare_limitations = 23`, and `blocked_or_fail_count = 0`; the Dissonant Echo multiplier, bonus, and total rows are `publishable / pass` with formula-ledger provenance
  - interpretation: this closes the false `level_contributor_present` blocker on the 12 Dissonant Echo-derived rows without allowing raw levels on generic formula rows, changing Dissonant math, applying a hidden pressure factor, or certifying the full all-enemy Boss Waves model.
- follow-up EP-compared helper formula-policy closure on **2026-06-13**:
  - kept patch: `kb/ledgers/formula_surface_policy.yaml` now classifies the 27 previously unclassified EP-compared `derived::edamage.*` and `derived::ehp.*` helper/objective rows as `ep_helper_composite`, with rationale for normal aligned rows and explicit stage-scope compare limitations
  - focused verification: EP compare population, explicit formula-policy, no-unknown-formula-mismatch, exact/close pass-verdict, and current-scope Stats evidence tests passed (**5 passed**); `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs; touched `py_compile` passed
  - output verification: regenerated `out/ep_oracle_compare.json` still reports 49 EP rows with unchanged status counts (`matched_exact = 17`, `matched_close = 9`, `mismatch = 3`, `stage_scope_mismatch = 20`), while `unclassified_ep_compare_count = 0`; regenerated line verification still reports `pass = 622`, `pass_with_compare_limitations = 23`, and `blocked_or_fail_count = 0`
  - interpretation: this improves KB/QE formula-policy evidence for EP-aligned and EP-limited helper rows without changing stat math, hiding the 20 stage-scope limitations, reclassifying the 3 known EP export defects, or closing the source-owned non-boss Boss Waves blocker.
- follow-up Boss Waves Dissonant PB cap-omission classification on **2026-06-13**:
  - kept patch: `app.pipeline` now annotates Boss Waves matrix rows whose imported `ids_dissonant_pb_wave` raw reference is zero after the same run category already reached the 5000 Dissonant PB bonus cap in `account_state.dissonance_pbs_by_tier`; `reference_gap_summary` splits those cap-omitted Disco PB zeros from ordinary missing empirical references, and `app.streamlit_inspector` renders the split in the model/blocker and missing-reference frames
  - focused verification: cap-omission summary/UI tests and adjacent T16 Utility Dissonant missing-reference, pressure-factor closure, terminal-override closure, and capped-reference quality tests passed (**8 passed**); full touched app contract suites passed (**198 passed**); `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs; touched `py_compile` passed
  - output verification: regenerated `out/boss_wave_milestone_matrix.json`, `out/run_stats.json`, and `out/diagnostics.json` still report `missing_reference_blocked_count = 19`, now split into `dissonance_pb_cap_omitted_reference_count = 18` and `ordinary_missing_reference_blocked_count = 1`, with reference kinds unchanged (`ids_dissonant_pb_wave = 18`, `ids_milestone_wave = 1`)
  - interpretation: this makes the user's "I do not fill Disco PBs above 5000" convention machine-readable in the Boss Waves evidence path. It does not create positive PB references, change selected waves, apply a hidden pressure factor, invent non-boss terminal-pressure formulas, or certify the remaining full all-enemy max-wave blocker.
- follow-up enemy-context schema-unit closure on **2026-06-13**:
  - kept patch: `kb/global-rules/contracts/environment-params.yaml` now declares units/resolvers for the routed enemy context surfaces used by Boss Waves, including enemy attack/health/speed/range multipliers, protector DR/radius, and enemy ultimate-enabled flags; QE tests prove the bounded metadata and resolved `context::enemy.*` rows publish concrete schema units instead of `unknown`
  - focused verification: enemy-context metadata, published schema-unit, and adjacent enemy/BC routing tests passed (**3 passed**); broader QE unmapped/reclassification plus contract smoke suites passed (**139 passed**); `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs; `environment-params.yaml` parsed and `git diff --check` passed
  - output verification: regenerated full `out/diagnostics.json` reports `resolved_unknown_schema_unit_count = 148` (down from 175 before this slice) and `0` remaining `context::enemy.*` rows in `kb_incomplete_areas.resolved_unknown_schema_units`
  - interpretation: this closes a KB/QE metadata incompleteness signal for Boss Waves enemy context carry-through without changing formulas, selected waves, pressure-factor behavior, EP compare classification, or the remaining `source_owned_non_boss_terminal_pressure_formulas` model blocker.
- follow-up Dissonance schema-unit closure on **2026-06-13**:
  - kept patch: `kb/global-rules/contracts/mechanic-params.yaml` now declares units/resolvers for active-tier Dissonant PBs, active boost multipliers, Echo source additive ratios, and Dissonant Run active support gates; QE tests prove metadata and resolved Boss Waves scenario rows publish concrete schema units instead of `unknown`
  - focused verification: Dissonance metadata, published schema-unit, active PB routing, and progression publication tests passed (**4 passed**); broader QE unmapped/reclassification plus contract smoke suites passed (**140 passed**); `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs; `mechanic-params.yaml` parsed
  - output verification: regenerated full `out/diagnostics.json` reports `resolved_unknown_schema_unit_count = 132` (down from 148 before this slice) and `0` remaining `dissonance` rows in `kb_incomplete_areas.resolved_unknown_schema_units`; Boss Waves primitive-family coverage remains `105` rows and line verification remains `604 publishable / 31 not_applicable`
  - interpretation: this closes the Disco/Dissonance KB/QE schema-unit incompleteness signal without changing formulas, selected waves, pressure-factor behavior, EP compare classification, Dissonant PB cap-omission semantics, or the remaining `source_owned_non_boss_terminal_pressure_formulas` model blocker.
- follow-up module auxiliary/lab schema-unit closure on **2026-06-13**:
  - kept patch: `kb/global-rules/contracts/mechanic-params.yaml` now declares units/resolvers for module assist lab bonuses and module lab progression surfaces, including reroll shards, daily mission shards, drop chance bonuses, shatter bonus, and module cost reductions; QE tests prove metadata and resolved report rows publish concrete schema units instead of `unknown`
  - focused verification: module metadata, published schema-unit, governed numeric routing, active module unique routing, and module unique publication tests passed (**5 passed**); broader QE unmapped/reclassification plus contract smoke suites passed (**141 passed**); `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs; `mechanic-params.yaml` parsed
  - output verification: regenerated full `out/diagnostics.json` reports `resolved_unknown_schema_unit_count = 117` (down from 132 before this slice) and `0` remaining `state::module.*` rows in `kb_incomplete_areas.resolved_unknown_schema_units`; Boss Waves primitive-family coverage remains `105` rows and line verification remains `604 publishable / 31 not_applicable`
  - interpretation: this closes the module auxiliary/lab KB/QE schema-unit incompleteness signal without changing formulas, selected waves, pressure-factor behavior, EP compare classification, module unique semantics, or the remaining `source_owned_non_boss_terminal_pressure_formulas` model blocker.
- follow-up Bot Bot lab route normalization on **2026-06-13**:
  - kept patch: `kb/global-rules/contracts/query-routing-mappings.yaml` now routes `Bot Bot - Cooldown` and `Bot Bot - Duration` lab rows to the canonical `bot.bot_bot.*` surfaces already owned by the KB/QE bot contract instead of producing duplicate legacy-shaped `state::bot.bot.*` rows
  - focused verification: Bot Bot track/lab routing, locked bot gate behavior, and bounded metadata tests passed (**4 passed**); broader QE unmapped/reclassification plus contract smoke suites passed (**142 passed**); `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs; `query-routing-mappings.yaml` parsed
  - output verification: regenerated full `out/diagnostics.json` reports `resolved_unknown_schema_unit_count = 115` (down from 117 before this slice) and `0` remaining `state::bot.*` rows in `kb_incomplete_areas.resolved_unknown_schema_units`; line verification is now `602 publishable / 31 not_applicable` because the two duplicate legacy Bot Bot rows are no longer published
  - interpretation: this closes the bot KB/QE schema-unit incompleteness signal by removing duplicate route drift, not by adding a second bot truth path; it does not change selected waves, pressure-factor behavior, EP compare classification, canonical `bot.bot_bot.*` ownership, or the remaining `source_owned_non_boss_terminal_pressure_formulas` model blocker.
- follow-up eHP/eEcon support-surface schema-unit closure on **2026-06-13**:
  - kept patch: `kb/global-rules/contracts/mechanic-params.yaml` now declares units/resolvers for the query-owned eHP Black Hole timing helpers and relic/free-upgrade eHP/eEcon support surfaces; `qe.kernel` routes only declared support surfaces through bounded metadata and carries the returned schema through `ResolvedSurfaceRow`; `qe.routing` preserves that schema when converting query responses to statbooks
  - focused verification: Black Hole support-surface and relic support-surface schema tests passed (**2 passed**); native-family routing compatibility passed (**4 passed**); broader QE unmapped/reclassification plus contract smoke/native-family suites passed (**146 passed**); simulator core smoke passed (**29 passed**); `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs; YAML parse and `git diff --check` passed
  - output verification: regenerated full `out/diagnostics.json` reports `resolved_unknown_schema_unit_count = 106` (down from 115 before this slice) and `0` remaining targeted `support_surface::ehp.*` / `support_surface::eecon.*` rows in `kb_incomplete_areas.resolved_unknown_schema_units`; line verification remains `602 publishable / 31 not_applicable`
  - interpretation: this closes eHP/eEcon support-surface KB/QE schema provenance for already-published Boss Waves support rows without changing formulas, selected waves, pressure-factor behavior, EP compare classification, or the remaining `source_owned_non_boss_terminal_pressure_formulas` model blocker.
- follow-up scalar runtime/mechanic schema-unit closure on **2026-06-13**:
  - kept patch: `kb/global-rules/contracts/mechanic-params.yaml` now declares units/resolvers for `economy.starting_cash_bonus`, `economy.max_interest_bonus`, `perk.standard_perks_bonus_pct`, `perk.tradeoff_bonus_pct`, `combat.orb_boss_hit_pct`, and `shock.chance_pct`; QE tests prove those already-routed rows publish concrete `cash`/`pct` schema units instead of `unknown`
  - focused verification: scalar runtime schema, governed numeric routing, and materialized lab routing tests passed (**3 passed**); broader QE unmapped/reclassification plus contract smoke/native-family suites passed (**147 passed**); `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs; `mechanic-params.yaml` parsed
  - output verification: regenerated full `out/diagnostics.json` reports `resolved_unknown_schema_unit_count = 100` (down from 106 before this slice) and `0` remaining targeted economy/perk/combat/shock rows in `kb_incomplete_areas.resolved_unknown_schema_units`; remaining unknown-schema clusters are account/capability/meta/lab/UW surfaces
  - interpretation: this closes a small KB/QE metadata incompleteness signal for already-resolved runtime/mechanic rows without changing formulas, selected waves, pressure-factor behavior, EP compare classification, or the remaining `source_owned_non_boss_terminal_pressure_formulas` model blocker.
- follow-up lab/UW/UW+ schema-unit closure on **2026-06-13**:
  - kept patch: `kb/global-rules/contracts/mechanic-params.yaml` now declares units/resolvers for the remaining owner-clear lab, UW, and UW+ mechanic parameter rows already published into the report statbook, including lab recharge/blast/size/bonus rows, UW damage/amplifier/quantity-like rows, and UW+ effect parameters
  - focused verification: lab and UW/UW+ published schema-unit tests plus materialized lab routing passed (**3 passed**); broader QE unmapped/reclassification, QE contract smoke, and current-scope Stats effect-family evidence tests passed (**146 passed**); `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs; `mechanic-params.yaml` parsed
  - output verification: regenerated full `out/diagnostics.json` reports `resolved_unknown_schema_unit_count = 74` (down from 100 before this slice), `0` remaining targeted `state::lab.*`, `state::uw.*`, or `state::uw_plus.*` rows in `kb_incomplete_areas.resolved_unknown_schema_units`, and current-scope Boss Waves effect-family evidence remains `covered` with `105` selected rows carrying coverage
  - interpretation: this closes KB/QE metadata incompleteness for already-routed lab, UW, and UW+ mechanic rows without changing formulas, selected waves, pressure-factor behavior, EP compare classification, Dissonant PB cap-omission semantics, or the remaining `source_owned_non_boss_terminal_pressure_formulas` model blocker. The current Stats evidence contract now reflects the active Farming loadout's 31 card-mastery lab rows plus 2 applied card-mastery runtime rows; dedicated Super Tower mastery tests still cover its equipped active/uw split branch.
- follow-up capability schema-unit closure on **2026-06-13**:
  - kept patch: `kb/global-rules/contracts/capabilities.yaml` now declares missing capability units for perks, module effect-ban counts, tower/lab unlock capabilities, UW lab toggles, and workshop enhancement unlock state; `qe.routing` and the compat/report resolver now include the existing capability contract in bounded metadata
  - focused verification: capability metadata-loader and published schema-unit tests passed (**2 passed**); broader QE unmapped/reclassification, QE contract smoke, and current-scope Stats effect-family evidence tests passed (**148 passed**); `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs; `capabilities.yaml` parsed
  - output verification: regenerated full `out/diagnostics.json` reports `resolved_unknown_schema_unit_count = 47` (down from 74 before this slice), `0` remaining `state::capability.*` rows in `kb_incomplete_areas.resolved_unknown_schema_units`, and current-scope Boss Waves effect-family evidence remains `covered` with `105` selected rows carrying coverage
  - interpretation: this closes capability KB/QE metadata incompleteness through the existing capability contract instead of duplicating capability truth in mechanic params. It does not change formulas, selected waves, pressure-factor behavior, EP compare classification, Dissonant PB cap-omission semantics, or the remaining `source_owned_non_boss_terminal_pressure_formulas` model blocker.
- follow-up account/meta schema-unit closure on **2026-06-13**:
  - kept patch: added `kb/global-rules/contracts/account-metadata.yaml` as the typed owner for account flags, account metadata settings, account context rows, cosmetic bonus helper rows, and meta progression parameters that are not canonical target stats or mechanic parameters; `qe.routing` and the compat/report resolver now load generic entry-shaped contracts through bounded metadata
  - focused verification: account/meta metadata-loader and published schema-unit tests passed (**3 passed**); broader QE unmapped/reclassification, QE contract smoke, and current-scope Stats effect-family evidence tests passed (**151 passed**); `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs; `account-metadata.yaml` parsed
  - output verification: regenerated full `out/diagnostics.json` reports `resolved_unknown_schema_unit_count = 0` and an empty `kb_incomplete_areas.resolved_unknown_schema_units` list; current-scope Boss Waves effect-family evidence remains `covered` with `105` selected rows carrying coverage
  - interpretation: this closes the remaining account/meta KB/QE schema-unit incompleteness without putting account metadata into mechanic params or creating UI-local truth. It does not change formulas, selected waves, pressure-factor behavior, EP compare classification, Dissonant PB cap-omission semantics, or the remaining `source_owned_non_boss_terminal_pressure_formulas` model blocker.
- follow-up ambiguous relic semantic closure on **2026-06-13**:
  - kept patch: `kb/global-rules/tables/relic-input-registry.csv` now replaces the remaining `percent_points_or_pct_bonus` hints with explicit `pct_bonus` or `percent_points` labels based on the canonical destination unit and resolver behavior; this is KB semantic labeling only and does not change relic math
  - focused verification: relic semantic and registry-closure tests passed (**2 passed**); broader QE unmapped/reclassification, QE contract smoke, and current-scope Stats effect-family evidence tests passed (**152 passed**); `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs
  - output verification: regenerated full `out/diagnostics.json` reports `active_unmapped_input_count = 0`, `resolved_unknown_schema_unit_count = 0`, `ambiguous_relic_semantic_hint_count = 0`, and current-scope Boss Waves effect-family evidence remains `covered` with `105` selected rows carrying coverage
  - interpretation: this closes the relic KB semantic ambiguity signal without changing formulas, selected waves, pressure-factor behavior, EP compare classification, Dissonant PB cap-omission semantics, or the remaining `source_owned_non_boss_terminal_pressure_formulas` model blocker.
- follow-up EP stage-scope evidence closure on **2026-06-13**:
  - kept patch: `evaluators.compare` and `evaluators.compare_core` now report all accounted unsupported facets for `stage_scope_mismatch` rows, split user-guess and EP-shortcut facets, and flag any stage-scope row without an accounted facet; this is evaluator diagnostics only and does not change QE stat values
  - focused verification: compare summary, known-export-defect, no-unknown-formula-mismatch, and current-scope Stats effect-family evidence tests passed (**4 passed**); `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix` and full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs
  - output verification: regenerated full `out/diagnostics.json` reports EP compare `matched_exact = 17`, `matched_close = 9`, `stage_scope_mismatch = 23`, `ep_true_formula_mismatch_count = 0`, `ep_unknown_formula_mismatch_count = 0`, `ep_stage_scope_rows_with_accounted_facets = 23`, and `ep_stage_scope_rows_without_accounted_facets = 0`
  - interpretation: this makes the remaining EP compare deltas explicitly attributable to stage policy, EP shortcut scope, or user-provided workbook assumptions. It does not change formulas, selected waves, pressure-factor behavior, Dissonant PB cap-omission semantics, or the remaining `source_owned_non_boss_terminal_pressure_formulas` model blocker.
- follow-up Boss Waves pressure-factor closure proof on **2026-06-13**:
  - kept patch: no product-semantics patch was made; this is evidence-only proof of the existing explicit `boss_wave_pressure_factor` runtime path
  - focused verification: Boss Waves pressure-factor closure, all-Dissonant-category closure, comparison overlay, CLI input mapping, and Streamlit pressure/closure evidence tests passed (**8 passed**); v49 app dashboard gates passed (**64 passed**); v49 replacement simulator gates passed (**66 passed**); an in-memory all-tier matrix probe with `boss_wave_pressure_factor = 2.606384292771721` and `orb_boss_total_damage_pct = 6.0` produced `105` rows, `0` model-completion blocker rows, and `model_closure_status = closed_with_pressure_factor_approximation`
  - refreshed current-worktree verification: `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix --boss-wave-pressure-factor 2.606384292771721 --out <temp>` passed on **2026-06-13**; generated temp artifacts report `105` rows, `21` rows for each Regular/Attack/Defense/Utility/Ultimate Weapons category, `105` complete rows, `0` row blockers, top-level `model_closure_status = closed_with_pressure_factor_approximation`, accepted approximation factor `2.606384292771721`, `certified_full_max_wave_model = false`, `105/105` Boss Waves family-coverage rows, and matching matrix closure in `run_stats.json` and `diagnostics.json`
  - output comparison: committed raw `out/boss_wave_milestone_matrix.json` remains default/raw account truth with `35/105` rows blocked by `source_owned_non_boss_terminal_pressure_formulas` (`7` per regular/attack/defense/utility/ultimate-weapons category), while the explicit pressure-factor path closes those same rows as an accepted approximation without claiming `certified_full_max_wave_model`
  - interpretation: the simple difficulty-factor option is already wired through Boss Waves and carries to Dissonant categories when supplied, but the calibration median remains comparison/review input until the operator approves promoting it into the accepted default matrix policy.
- follow-up Bot+ boolean carry-through closure on **2026-06-13**:
  - kept patch: `qe.routing` now treats `standard_bool`, `unit: bool`, and `.unlocked` destinations as boolean semantics in the bounded resolver, including the bot-specific mechanic branch; `simulators.progression.resolve_run_stats_progression_bundle` now explicitly requests the five Bot+ support rows needed by the bounded `run_stats` derived-query publisher
  - focused verification: Bot+ bool resolver, run-stats progression bundle, and current-scope artifact evidence tests passed (**3 passed**); broader QE unmapped/reclassification plus contract smoke passed (**152 passed**); app dashboard contracts passed (**64 passed**); replacement simulator gates passed (**66 passed**); Boss Waves pressure-factor proof tests still passed (**8 passed**); `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix`, full `execute_pipeline(... include_boss_wave_milestone_matrix=True)`, and temp-output `app.run_stats --perk-mode max_progression_policy --out <temp>` passed
  - output verification: regenerated `out/statbook_publishable.json`, `out/run_stats_query_rows_start_of_run.json`, and `out/run_stats_query_rows_max_progression.json` now resolve all five `state::bot.plus.*.unlocked` rows as `bool`; current IDS resolves Maximum Power as `true`, the derived Bot+ unlocked count as `1.0`, and Synchronicity base slots as `0.0` in both Farming/Tourney query artifacts; regenerated diagnostics still report `active_unmapped_input_count = 0`, `resolved_unknown_schema_unit_count = 0`, `ambiguous_relic_semantic_hint_count = 0`, and `105` Boss Waves selected rows covered; line verification now reports `607` publishable rows, `26` not-applicable descriptor rows, `610` pass rows, `23` pass-with-compare-limitations rows, and no issue/non-pass rows
  - interpretation: this closes a real Bot+ carry-through bug in the QE-owned stats path without changing formulas, selected waves, pressure-factor behavior, EP compare classification, Dissonant PB cap-omission semantics, or the remaining `source_owned_non_boss_terminal_pressure_formulas` model blocker.
- follow-up Boss Waves default posture artifact guard on **2026-06-13**:
  - kept patch: `tests/app/test_stats_dashboard_contract.py` now pins the committed Boss Waves matrix, `run_stats.json`, and full diagnostics to the same explicit default posture: `partial_missing_required_model_inputs`, blocker `source_owned_non_boss_terminal_pressure_formulas`, no accepted default pressure-factor approximation, and comparison-only review input `boss_wave_pressure_factor = 2.606384292771721`
  - focused verification: the new artifact posture test passed (**1 passed**), explicit pressure-factor/default-vs-comparison closure tests passed (**3 passed**), and the full Stats dashboard contract passed (**56 passed**)
  - interpretation: this hardens the evidence boundary for the remaining Boss Waves decision. The default matrix remains honest account truth, while the already-wired explicit pressure-factor path remains the sanctioned approximation closure when the operator supplies a non-default factor.
- follow-up Dissonant PB cap caveat artifact guard on **2026-06-13**:
  - kept patch: `tests/app/test_stats_dashboard_contract.py` now requires all `42` committed Boss Waves rows with the `dissonance_pb_5000_bonus_cap_floor` caveat to remain lower-bound Dissonant PB references, non-exact references, non-calibration candidates, and disabled pressure-factor hints with mode `dissonance_pb_bonus_cap_not_exact_reference`
  - focused verification: the strengthened default-posture artifact test passed (**1 passed**) and the full Stats dashboard contract passed (**56 passed**)
  - interpretation: this prevents the user's intentionally omitted above-5000 Dissonant PB values from becoming false calibration evidence or hidden pressure-factor defaults, without changing selected waves, formulas, pressure-factor default behavior, or the remaining `source_owned_non_boss_terminal_pressure_formulas` model blocker.
- follow-up Boss Waves row-level family-coverage guard on **2026-06-13**:
  - kept patch: `tests/app/test_stats_dashboard_contract.py` now checks the committed Boss Waves matrix rows directly, requiring all `105` selected rows to publish `replacement_primitive_family_coverage.status = covered`, no missing requested families, and covered QE surface/contributor evidence for the requested `bot`, `card_base`, `card_mastery`, `workshop`, `enhancement`, `module`, and `relic` families
  - focused verification: the current-scope artifact evidence test passed (**1 passed**) and the full Stats dashboard contract passed (**56 passed**)
  - interpretation: this hardens the Boss Waves carry-through proof from aggregate diagnostics into row-level artifact evidence without changing formulas, selected waves, pressure-factor default behavior, EP compare classification, Dissonant PB cap-omission semantics, or the remaining `source_owned_non_boss_terminal_pressure_formulas` model blocker.
- follow-up KB/QE incomplete-signal artifact guard on **2026-06-13**:
  - kept patch: `tests/app/test_stats_dashboard_contract.py` now requires committed diagnostics to report `active_unmapped_input_count = 0`, `resolved_unknown_schema_unit_count = 0`, `ambiguous_relic_semantic_hint_count = 0`, no priority gaps, no active unmapped family buckets, and empty detail lists for active unmapped inputs, resolved unknown-schema rows, and ambiguous relic semantic hints
  - focused verification: the current-scope artifact evidence test passed (**1 passed**) and the full Stats dashboard contract passed (**57 passed**)
  - interpretation: this hardens the "all requested stats are calculated and KB/QE-aligned" evidence boundary without changing formulas, source routing, selected waves, pressure-factor default behavior, EP compare classification, Dissonant PB cap-omission semantics, or the remaining `source_owned_non_boss_terminal_pressure_formulas` model blocker.
- broad current-worktree verification on **2026-06-13**:
  - high-signal gate passed (**284 passed**) across QE unmapped/reclassification, QE contract smoke, app input contract, app Stats contract, and replacement simulator gates
  - full suite passed (**707 passed**)
  - temp default `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix --out <temp>` passed and generated `105` matrix rows with `105/105` family coverage, default `model_closure_status = partial_missing_required_model_inputs`, and `35` rows still blocked by `source_owned_non_boss_terminal_pressure_formulas`
  - temp explicit pressure-factor `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix --boss-wave-pressure-factor 2.606384292771721 --out <temp>` passed and generated `105` complete rows, `0` row blockers, `105/105` family coverage, and top-level `model_closure_status = closed_with_pressure_factor_approximation`
  - temp full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` passed and full diagnostics reported `active_unmapped_input_count = 0`, `resolved_unknown_schema_unit_count = 0`, `ambiguous_relic_semantic_hint_count = 0`, current-scope family evidence `covered` with `105` Boss Waves rows, EP compare `17` exact / `9` close / `23` accounted stage-scope mismatches, `0` true/unknown formula mismatches, and line verification `610` pass / `23` pass-with-compare-limitations rows
  - interpretation: broad current evidence supports the stats/EP/family carry-through claim and the explicit pressure-factor approximation path; it still does not certify the default matrix as a full all-enemy max-wave model because the default path intentionally leaves `source_owned_non_boss_terminal_pressure_formulas` unresolved.
- follow-up EP compare committed-artifact guard on **2026-06-13**:
  - kept patch: `tests/app/test_stats_dashboard_contract.py` now requires committed `out/diagnostics.json`, `out/ep_oracle_compare.json`, and `out/line_by_line_verification.json` to agree on the current EP compare evidence: `49` compared rows, `17` exact matches, `9` close matches, `23` stage-scope mismatches, zero raw/true/known/unknown formula mismatches, zero missing package rows, and zero unaccounted stage-scope destinations
  - focused verification: the new EP compare artifact test passed (**1 passed**) and the full Stats dashboard contract passed (**57 passed**)
  - interpretation: this hardens the EP-alignment claim as "aligned where comparable; documented stage/workbook-scope limitations where not comparable" without changing formula policy, runtime stat truth, Boss Waves selected waves, pressure-factor default behavior, or the remaining `source_owned_non_boss_terminal_pressure_formulas` model blocker.
- follow-up pressure-driver evidence and schema/EP alignment on **2026-06-13**:
  - kept patch: Boss Waves `model_accuracy_summary` now publishes `non_boss_pressure_driver_model`, a driver-aware diagnostic that names spawn-rate, Wave Accelerator Mastery spawn acceleration, Enemy Balance Mastery/double-elite pressure, fleet pressure, tier, and wave as the intended non-boss pressure drivers while keeping `default_pressure_factor_derived = false` and `pressure_factor_policy = manual_or_comparison_only_until_source_owned_formula`
  - kept patch: KB metadata now declares the routed enemy/perk/capability schema units that full-pipeline diagnostics had exposed as unknown (`context::enemy.*`, `state::perk.*`, and `state::capability.random_uw_unlock.enabled`), and EP known export-defect rows are treated as accounted stage-scope facets in live compare artifacts instead of raw formula mismatches
  - focused verification: app UI/Stats contracts passed (**117 passed**); targeted EP publication tests passed (**7 passed**); QE contract and unmapped/routing suites passed (**152 passed**); replacement simulator gates passed (**66 passed**); app import-boundary and focused artifact guards passed (**4 passed**); full suite passed (**707 passed**); `git diff --check` passed on touched files with CRLF warnings only
  - runtime verification: temp default `app.run_stats --perk-mode max_progression_policy --include-boss-wave-milestone-matrix --out <temp>` passed with `105` rows, `model_closure_status = partial_missing_required_model_inputs`, and `source_owned_non_boss_terminal_pressure_formulas` still active; temp explicit `--boss-wave-pressure-factor 2.606384292771721` passed with `105` rows, `model_closure_status = closed_with_pressure_factor_approximation`, `0` blockers, and driver status `explicit_pressure_factor_override_active_driver_formula_not_source_owned`
  - output verification: full `execute_pipeline(... include_boss_wave_milestone_matrix=True)` regenerated outputs with `active_unmapped_input_count = 0`, `resolved_unknown_schema_unit_count = 0`, `ambiguous_relic_semantic_hint_count = 0`, EP compare `17` exact / `9` close / `23` accounted stage-scope rows, `0` raw/true/known/unknown formula mismatches, and `current_scope_effect_family_evidence.status = covered`
  - interpretation: this aligns the pressure-factor decision with the user's requested drivers without inventing a default formula. The default Boss Waves matrix remains honest partial account truth; a certified all-enemy max-wave model still requires source-owned spawn, elite, fleet, tier, and wave pressure formulas.
- rebuilt-worktree verification after the 2026-04-15 final product-correctness hardening tranche:
  - `python -m pytest -q` -> **399 passed**
  - legacy-era targeted gate with `tests/simulators/test_run_executor.py` is superseded by replacement-era gates using `tests/simulators/test_evaluator_kernel.py` and `tests/simulators/test_boss_waves_parity_shadow.py`
  - `python -m app.run_stats` -> **passed**
- live product acceptance verification after the 2026-04-15 final product-correctness hardening tranche:
  - Streamlit harness preset switching -> **passed**
  - primary Stats surfaces remain Workshop / Ultimate Weapons / Bots / Guardians / Modules -> **passed**
  - Boss Waves surface exposes cadence and regen auditability in the visible operator contract -> **passed**
- sanctioned Boss Waves payload benchmark (`build_boss_wave_payload`, Tier 14, wave 500, boss step 10, `stop_on_failure=false`) on **2026-04-10** after the explicit payload contract landed:
  - run 1: **2655.53 ms**
  - run 2: **1763.50 ms**
  - run 3: **1799.30 ms**
- sanctioned Boss Waves payload benchmark (`build_boss_wave_payload`, Tier 14, wave 500, boss step 10, `stop_on_failure=false`) on **2026-04-10** after the workshop-mutation trigger closure:
  - run 1: **2913.87 ms**
  - run 2: **2166.65 ms**
  - run 3: **2006.79 ms**
- sanctioned Boss Waves payload benchmark (`build_boss_wave_payload`, Tier 14, wave 500, boss step 10, `stop_on_failure=true`) on **2026-04-10** with the new interactive default:
  - run 1: **570.16 ms**
  - run 2: **95.76 ms**
  - run 3: **101.10 ms**
- follow-up current-snapshot EP sync exact-result cache on **2026-07-13**:
  - pre-patch warmed profile: **3.275s**, **3,429,996** calls, with `_ep_sync` at **2.043s cumulative / 1.546s self** across 12 calls and about **2.27M** list appends
  - retained patch: bounded `lru_cache(maxsize=128)` on the pure QE `_ep_sync` calculation; eight-run probe recorded **92 hits / 4 misses**
  - controlled direct-interpreter warm A/B: control median **2097.19ms** versus retained-cache median **859.92ms**
  - current direct cold CLI samples: **2747.12ms**, **2979.34ms**, **2912.18ms**
  - warmed post-patch profile: **1.925s**, **1,237,986** calls, with `_ep_sync` absent from the top 30 cumulative costs after the cache was populated
  - semantic proof: every non-diagnostic bounded artifact matched byte-for-byte; diagnostics matched after timing-only fields were removed
  - verification: focused QE/app contract gate **152 passed**, explicit max-progression `app.run_stats` passed, and full pytest **746 passed**
- interpretation: sanctioned-path caching, compiler/session reuse, publication hardening, and write-path cleanup have materially reduced several repeated costs without changing authority or outputs. The current July snapshot still measures roughly **2.75s-2.98s** for a fresh CLI run and **0.86s** for the controlled warmed median after the latest retained patch, so P5 remains open against the preserved `<100ms` refresh direction. The next measured warm costs are farming sync-window integration, QE resolution, and final dashboard publication; no claim is made that those should be changed without a new bounded profile-driven tranche. Boss Waves remains replacement-owned and Boss Ultimate remains explicitly deferred.

### Verification record

- targeted tranche-alignment suites passed on **2026-04-14** across Stats contract, pipeline functional, simulator, UI contract, and import-boundary coverage
- visual/semantic acceptance recovery passed on **2026-04-14** for the live primary Stats/Boss Waves surface:
  - preset switching updates the visible product state without a rebuild
  - primary Stats tables now follow the shared display grammar (`—`, max 2 decimals, normalized units, no mojibake)
  - Bots now use start-of-run-only primary semantics with truthful cumulative medal spend and no misleading effective-range column
  - Guardians now use truthful cumulative-bit and start-state semantics with simple green recon where base equals start state
  - workshop `Cap` wiring now includes ceiling/floor truth for `Defense %`, `Shockwave Interval`, and `Wall Rebuild`
  - full `pytest -q` passed on the repaired baseline (`397 passed`)
- refreshed full suite passed on **2026-04-07** (`349 passed`) in **44.85s**
- corrective recovery tranche targeted suites passed on **2026-04-12**, including:
  - `tests/app/test_stats_dashboard_contract.py`
  - `tests/app/test_pipeline_functional.py`
  - `tests/app/test_pipeline_ui_contracts.py`
  - `tests/qe/test_generic_perk_multiplier_resolution.py`
  - `tests/qe/test_routing_checkpoint_consumer_bundle.py`
  - `tests/simulators/test_evaluator_kernel.py`
  - `tests/shared/test_import_boundaries.py`
- `python -m app.run_stats` completed successfully on **2026-04-12**
- UI-level Streamlit harness verification passed on **2026-04-12** with:
  - primary Stats surface labels `Workshop`, `Ultimate Weapons`, `Bots`, `Guardians`, `Modules`
  - no `Canonical Overview` on the main Stats workflow
  - Boss Waves runtime-basis caption present
  - sanctioned Boss Waves run showing `Max surviving wave = 130`, `First failed wave = 140`, `Rows = 14`
- refreshed full suite passed on **2026-04-12** (`390 passed`)
- refreshed Boss Waves semantic/product recertification suites passed on **2026-04-13**:
  - legacy-era `pytest -q tests/simulators/test_run_executor.py` (`15 passed`) is superseded after executor deletion by replacement kernel and certification tests
  - `pytest -q tests/app/test_pipeline_functional.py` (`29 passed`)
  - `pytest -q tests/app/test_pipeline_ui_contracts.py` (`19 passed`)
  - `python -m app.run_stats`
- repaired current-worktree full suite passed on **2026-04-13** (`394 passed`) in **25.59s**
- corrected-recovery tranche targeted suites passed on **2026-04-11**:
  - `pytest -q tests/app/test_stats_dashboard_contract.py` -> `43 passed`
  - `pytest -q tests/app/test_pipeline_functional.py` -> `29 passed`
  - `pytest -q tests/qe/test_generic_perk_multiplier_resolution.py` -> `8 passed`
  - `pytest -q tests/qe/test_routing_checkpoint_consumer_bundle.py` -> `4 passed`
  - legacy-era `pytest -q tests/simulators/test_run_executor.py` -> `15 passed` before executor deletion
  - `pytest -q tests/shared/test_import_boundaries.py` -> `51 passed`
  - `pytest -q tests/app/test_pipeline_ui_contracts.py` -> `19 passed`
- repaired current-worktree full suite passed on **2026-04-11** (`390 passed`) in **24.34s**
- UI-level Streamlit harness passed on **2026-04-11** with no exceptions before or after running the Boss Waves request, recovered primary Stats surfaces present (`Workshop`, `Ultimate Weapons`, `Bots`, `Guardians`, `Modules`), and Boss Waves producing `Max surviving wave = 130`, `First failed wave = 140`, `Rows = 14` for the repaired sanctioned scenario
- refreshed `out/` inspection confirmed:
  - query-row artifacts remain bounded by state mode
  - query-plan artifacts remain bounded by state mode
  - `diagnostics.json` now publishes the explicit bounded run_stats output contract
  - committed `run_stats.json` excludes volatile timing telemetry; rebuild-stable review content remains separated from local diagnostics timing data
  - `account_state.json` refreshed through the sanctioned `run_stats` path
- ad hoc tracked output directories were removed and moved under ignore policy
- unrelated user-owned local edits outside this tranche were intentionally left untouched rather than folded into hygiene work

---

## Decision freeze (still active)

1. **Boss Waves remains interactive**, but only through a sanctioned app-level runtime facade
2. **Streamlit remains optional/import-safe**
3. **Legacy start/max statbooks are permanently removed**, not restored
4. **Formula surface policy remains active for compare/publication semantics after bridge-residue retirement**
5. **Repo should not be described as fully re-certified green** without a durable full-suite artifact for the current snapshot

---

## Stop conditions

Stop and report rather than improvising if any of the following occur:

- governance files disagree on active tranche or hardening order
- code and current artifacts disagree materially about a current-scope visible or simulator surface
- a requested change would require UI-local truth invention or a second active authority path
- performance claims would rely on hidden benchmark shortcuts
- evaluator or optimiser work starts to creep back into scope
- proposed changes force broad refactors instead of tranche-scoped progress

---

## What not to do next

Do **not** start with:

- evaluator delivery
- optimiser delivery
- UI-local truth patches
- speculative precompute or CI work
- broad rewrite for aesthetic cleanup
- broad cleanup outside the active owner/path surfaces

Those are sequencing mistakes at the current repo stage.
