# REPO_INDEX.yaml
#
# Whitelist-rebuild file-status ledger.
# Every non-KB file is classified here.
#
# Status values:
#   active_candidate  — survives in the new active spine
#   archived          — exists only on the separate archive branch
#   generated         — runtime output, not owned by active code
#
# version: 1

# ─── ROOT CONTROL FILES ───────────────────────────────────────────────────────

root:
  README.md:
    status: active_candidate
    layer: root

  ARCHITECTURE.md:
    status: active_candidate
    layer: root

  ACTIVE_TRANCHE.md:
    status: active_candidate
    layer: root

  AGENTS.md:
    status: active_candidate
    layer: root

  BURNDOWN.yaml:
    status: active_candidate
    layer: root

  RTK.md:
    status: active_candidate
    layer: root

  CODEX_SUPERVISOR_PROMPT.txt:
    status: active_candidate
    layer: root

  TowerSim_bible_v49.md:
    status: active_candidate
    layer: root

  TowerSim_bible_v49_reconciliation_review.md:
    status: active_candidate
    layer: root

  REPO_INDEX.yaml:
    status: active_candidate
    layer: root

  pyproject.toml:
    status: active_candidate
    layer: root

  requirements.txt:
    status: active_candidate
    layer: root

  .gitignore:
    status: active_candidate
    layer: root

generated_outputs:
  out/account_state.json:
    status: generated
    note: committed bounded run_stats maintenance baseline artifact

  out/run_stats.json:
    status: generated
    note: committed bounded run_stats maintenance baseline artifact

  out/run_stats_query_plan_start_of_run.json:
    status: generated
    note: committed bounded run_stats maintenance baseline artifact

  out/run_stats_query_plan_max_progression.json:
    status: generated
    note: committed bounded run_stats maintenance baseline artifact

  out/run_stats_query_rows_start_of_run.json:
    status: generated
    note: committed bounded run_stats maintenance baseline artifact

  out/run_stats_query_rows_max_progression.json:
    status: generated
    note: committed bounded run_stats maintenance baseline artifact

  out/diagnostics.json:
    status: generated
    note: local run_stats support artifact; not part of the sanctioned committed baseline

  out/module_card_payloads.json:
    status: generated
    note: local run_stats support artifact; not part of the sanctioned committed baseline

  out_analysis_check/:
    status: generated
    note: ad hoc analysis output residue; maintenance stabilization policy is remove and ignore

  out_direct/:
    status: generated
    note: ad hoc run_stats output residue; maintenance stabilization policy is remove and ignore

  out_module_test/:
    status: generated
    note: ad hoc run_stats output residue; maintenance stabilization policy is remove and ignore

  out_runpy_test/:
    status: generated
    note: ad hoc run_stats output residue; maintenance stabilization policy is remove and ignore

  out_script_test/:
    status: generated
    note: ad hoc run_stats output residue; maintenance stabilization policy is remove and ignore

# ─── app/ ────────────────────────────────────────────────────────────────────

app:
  app/__init__.py:
    status: active_candidate
    layer: app/

  app/boss_waves_phase2a_field_map.yaml:
    status: active_artifact
    layer: app/
    note: Boss Waves product field map derived from current product consumption; covers replacement-sourced table, summary, export, and diagnostics; legacy is no longer exposed by the product payload

  app/display.py:
    status: active_candidate
    layer: app/

  app/inspector_data.py:
    status: active_candidate
    layer: app/

  app/pipeline.py:
    status: active_candidate
    layer: app/
    note: orchestration authority; utilities extracted in T12

  app/run_analysis.py:
    status: active_candidate
    layer: app/

  app/run_stats.py:
    status: active_candidate
    layer: app/
    note: thin CLI entrypoint

  app/streamlit_inspector.py:
    status: active_candidate
    layer: app/

  app/models.py:
    status: active_candidate
    layer: app/
    note: T12 extraction — pipeline dataclasses and trace models

  app/publication.py:
    status: active_candidate
    layer: app/
    note: thin artifact writer/render wrapper; dashboard payload assembly delegated to qe.publication

  app/tools/:
    status: active_candidate
    layer: app/tools/
    note: standalone operator tools only; module reroll tool orchestrates QE-owned calculator sources and writes quarantined experimental reports without Streamlit or production pipeline routing

# ─── input/ ───────────────────────────────────────────────────────────────────

input:
  input/*.py:
    status: active_candidate

  input/loadout.json:
    status: archive_candidate
    target: archive/legacy/
    reason: purpose unclear; not referenced by supported path

  input/perks_max_progression_policy.README.md:
    status: archive_candidate
    target: archive/legacy/
    reason: old doc for a superseded derived artifact pattern

  input/manual_advisory_inputs.example.json:
    status: archive_candidate
    target: archive/legacy/

  input/ids_parser.py:
    status: active_candidate
    layer: input/

  input/loader.py:
    status: active_candidate
    layer: input/

  input/manual_inputs.yaml:
    status: active_candidate
    layer: input/

  input/runtime_state.py:
    status: active_candidate
    layer: input/

  input/state_builder.py:
    status: active_candidate
    layer: input/

  input/state_types.py:
    status: active_candidate
    layer: input/

  # imports/ — survive as-is
  input/imports/ids.csv:
    status: active_candidate

# ─── qe/ ──────────────────────────────────────────────────────────────────────

qe:
  qe/__init__.py:
    status: active_candidate
    layer: qe/

  qe/consumer_registry.py:
    status: active_candidate
    layer: qe/

  qe/contracts.py:
    status: active_candidate
    layer: qe/
    note: "T12: absorbed _json_sanitize and _contract_json_payload from pipeline"

  qe/dependency_registry.py:
    status: active_candidate
    layer: qe/

  qe/kb_surfaces.py:
    status: active_candidate
    layer: qe/

  qe/kernel.py:
    status: active_candidate
    layer: qe/

  qe/materializer.py:
    status: active_candidate
    layer: qe/

  qe/models.py:
    status: active_candidate
    layer: qe/

  qe/perk_tables.py:
    status: active_candidate
    layer: qe/

  qe/run_plan.py:
    status: active_candidate
    layer: qe/
    note: Boss Waves compiled RunPlan and Table 1 common trajectory registry owner for the staged replacement kernel

  qe/publication.py:
    status: active_candidate
    layer: qe/
    note: sanctioned publication coordinator and dashboard payload assembly owner

  qe/query_currency_income.py:
    status: active_candidate
    layer: qe/

  qe/query_derived_composites.py:
    status: active_candidate
    layer: qe/

  qe/query_module_policy.py:
    status: active_candidate
    layer: qe/

  qe/query_perk_compiler.py:
    status: active_candidate
    layer: qe/

  qe/query_routing.py:
    status: active_candidate
    layer: qe/

  qe/module_reroll/:
    status: active_candidate
    layer: qe/module_reroll/
    note: experimental QE-owned module reroll calculator mechanics and KB source loading; not used by production query/app paths

  qe/query_state_mode_policy.py:
    status: active_candidate
    layer: qe/

  qe/routing.py:
    status: active_candidate
    layer: qe/

  qe/shared_runtime_context.py:
    status: active_candidate
    layer: qe/

  qe/stat_input_compiler.py:
    status: active_candidate
    layer: qe/

  qe/stat_resolution.py:
    status: active_candidate
    layer: qe/

  qe/workshop_stat_rows.py:
    status: active_candidate
    layer: qe/

# ─── simulators/ ─────────────────────────────────────────────────────────────

simulators:
  simulators/__init__.py:
    status: active_candidate
    layer: simulators/

  simulators/contracts.py:
    status: active_candidate
    layer: simulators/

  simulators/evaluator_kernel.py:
    status: active_candidate
    layer: simulators/
    note: Boss Waves Table 2 scenario overlay, v21 event-only combat kernel, lanes, and operator handles; consumes staged outputs

  simulators/geometry.py:
    status: active_candidate
    layer: simulators/
    note: Displayed/proxy geometry engine for range transforms, wall-travel contact-time proxy base, spatial overlaps, consumer payloads, and proxy-governed geometry diagnostics

  simulators/incremental_cache_fingerprint.py:
    status: active_candidate
    layer: simulators/

  simulators/incremental_cache_validator.py:
    status: active_candidate
    layer: simulators/

  simulators/incremental_overlay_publisher.py:
    status: active_candidate
    layer: simulators/

  simulators/incremental_parity_harness.py:
    status: active_candidate
    layer: simulators/

  simulators/incremental_recalc_runtime.py:
    status: active_candidate
    layer: simulators/

  simulators/incremental_subset_executor.py:
    status: active_candidate
    layer: simulators/

  simulators/performance.py:
    status: active_candidate
    layer: simulators/

  simulators/perk_timeline_generator.py:
    status: active_candidate
    layer: simulators/

  simulators/perk_timeline_state.py:
    status: active_candidate
    layer: simulators/

  simulators/perks.py:
    status: active_candidate
    layer: simulators/

  simulators/progression.py:
    status: active_candidate
    layer: simulators/

  simulators/runtime_consumer_executor.py:
    status: active_candidate
    layer: simulators/

  simulators/scenario.py:
    status: active_candidate
    layer: simulators/

  simulators/snapshot_resolver.py:
    status: active_candidate
    layer: simulators/

  simulators/timing.py:
    status: active_candidate
    layer: simulators/
    note: Timing owner for contact/hit intervals and timed effects; consumes geometry displayed wall-travel proxy only as labeled boss contact-time base when no manual override is supplied

  simulators/wave_progression_policy.py:
    status: active_candidate
    layer: simulators/

# ─── evaluators/ ─────────────────────────────────────────────────────────────

evaluators:
  evaluators/__init__.py:
    status: active_candidate
    layer: evaluators/

  evaluators/compare.py:
    status: active_candidate
    layer: evaluators/
    note: thin facade re-exporting sharded logic (T12)

  evaluators/compare_core.py:
    status: active_candidate
    layer: evaluators/
    note: T12 shard — core scoring and EP comparison

  evaluators/audit_engine.py:
    status: active_candidate
    layer: evaluators/
    note: T12 shard — publishing gates and KB audits

  evaluators/residue_analysis.py:
    status: active_candidate
    layer: evaluators/
    note: T12 shard — gap analysis and closure reports

  evaluators/verification_engine.py:
    status: active_candidate
    layer: evaluators/
    note: T12 shard — oracle parity and loading logic

  evaluators/objectives.py:
    status: active_candidate
    layer: evaluators/

  evaluators/ranker.py:
    status: active_candidate
    layer: evaluators/

  evaluators/scorer.py:
    status: active_candidate
    layer: evaluators/

# ─── advisors/ ───────────────────────────────────────────────────────────────

advisors:
  advisors/__init__.py:
    status: active_candidate
    layer: advisors/

  advisors/recommendation_policy.py:
    status: active_candidate
    layer: advisors/

  advisors/upgrade_advisor.py:
    status: active_candidate
    layer: advisors/

  # derived/ — single canonical artifact (duplicate removed in T11)
  # input/derived/perks_max_progression_policy.runtime.json: DELETED (T11) — was identical to perks_derived.json

  input/derived/perks_projected_max.json:
    status: generated

  input/derived/perks_projected_max.diagnostics.json:
    status: generated

  input/derived/perks_projected_max.final_state.json:
    status: generated

  input/derived/perks_projected_max.timeline.json:
    status: generated

  input/derived/.gitkeep:
    status: delete_later

# ─── tests/ ──────────────────────────────────────────────────────────────────

tests:
  tests/*.py:
    status: active_candidate

  tests/shared/test_import_boundaries.py:
    status: active_candidate
    layer: tests/shared/

  tests/test_*.py:
    status: active_candidate
    layer: tests/
