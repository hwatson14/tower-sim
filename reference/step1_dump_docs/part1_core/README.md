
TOWERSIM FULL HANDOVER (v20.0.0)

This bundle is the authoritative, fail-closed handover of the Tower simulator.

Single runtime input:
- _IDS.csv

Everything else is baked, versioned, and validated.

If something is missing, the simulator MUST fail.
